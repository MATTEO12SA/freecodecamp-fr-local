---
id: 66edcd875b0d91de1fbbb492
title: Quiz sur les fondamentaux JavaScript
challengeType: 8
dashedName: quiz-javascript-fundamentals
---

# --description--

Pour réussir le quiz, tu dois répondre correctement à au moins 18 des 20 questions ci-dessous.

# --quizzes--

## --quiz--

### --question--

#### --text--

Lequel de ces éléments crée un objet String ?

#### --distractors--

```js
String({ "Hello World" });
```

---

```js
String("Hello World");
```

---

```js
"Hello World".toString();
```

#### --answer--

```js
new String("Hello World");
```

### --question--

#### --text--

Comment peux-tu convertir un objet String en littéral de chaîne ?

#### --distractors--

Avec le constructeur `String()`.

---

Avec la méthode `.stringify()`.

---

En entourant l'objet `String` de guillemets doubles.

#### --answer--

Avec la méthode `.toString()`.

### --question--

#### --text--

Pourquoi peux-tu utiliser des méthodes comme `.length`, `.repeat()`, `.concat()` et `.slice()` sur une primitive de type chaîne en JavaScript ?

#### --distractors--

Ces méthodes existent nativement sur les primitives de type chaîne.

---

Les primitives de type chaîne sont stockées en interne comme des objets.

---

JavaScript convertit temporairement la primitive de type chaîne en tableau.

#### --answer--

JavaScript enveloppe temporairement la primitive de type chaîne dans un objet String.

### --question--

#### --text--

Que va afficher le code suivant dans la console ?

```js
let stringArray = [8, 9, 10].toString();
console.log(stringArray);
```

#### --distractors--

`[8,9,10]`

---

`["8", "9", "10"]`

---

`"8", "9", "10"`

#### --answer--

`"8,9,10"`

### --question--

#### --text--

Lequel des éléments suivants renvoie un objet `Number` à partir de la chaîne `"123"` ?

#### --distractors--

`Object("123")`

---

`"123".toNumber()`

---

`Number("123")`

#### --answer--

`new Number("123")`

### --question--

#### --text--

Que renverront respectivement `Number(true)` et `Number(false)` ?

#### --distractors--

`undefined` et `undefined`

---

`NaN` et `NaN`

---

`true` et `false`

#### --answer--

`1` et `0`

### --question--

#### --text--

Que renverront respectivement `Number(undefined)` et `Number(null)` ?

#### --distractors--

`NaN` et `NaN`

---

`0` et `0`

---

`undefined` et `null`

#### --answer--

`NaN` et `0`

### --question--

#### --text--

Lequel des éléments suivants est le meilleur nom pour une variable booléenne qui vérifie si un utilisateur est autorisé à faire quelque chose sur ton site web ?

#### --distractors--

`getPermission`

---

`handlePermission`

---

`permission`

#### --answer--

`hasPermission`

### --question--

#### --text--

Lequel des éléments suivants est le meilleur nom pour une fonction qui s'exécute lorsqu'un utilisateur soumet un formulaire ?

#### --distractors--

`hasSubmitted`

---

`formSubmit`

---

`didSubmit`

#### --answer--

`handleSubmit`

### --question--

#### --text--

Qu'est-ce qu'un tableau clairsemé ?

#### --distractors--

Un tableau où un ou plusieurs indices ont la valeur `null`.

---

Un tableau où un ou plusieurs indices ont la valeur `undefined`.

---

Un tableau où un ou plusieurs indices ont la valeur d'un littéral d'objet vide.

#### --answer--

Un tableau où un ou plusieurs indices ne se voient pas attribuer de valeur.

### --question--

#### --text--

Dans lequel des exemples suivants `arr` ne sera PAS un tableau clairsemé ?

#### --distractors--

```js
const arr = new Array(5);
```

---

```js
const arr = [1, 2, 3];
arr[4] = 4;
```

---

```js
const arr = [1, 2, 3, , 5];
```

#### --answer--

```js
const arr = Array.from({ length: 5 });
```

### --question--

#### --text--

Quel est l'objectif principal d'un linter ?

#### --distractors--

Formater le code automatiquement.

---

Optimiser le code pour les performances.

---

Créer de la documentation pour le code.

#### --answer--

Détecter les erreurs potentielles et les problèmes dans le code.

### --question--

#### --text--

Quel est un avantage d'utiliser un formateur dans une base de code ?

#### --distractors--

Ils permettent aux développeurs de compiler le code plus efficacement.

---

Ils convertissent le code vers un langage de programmation spécifique.

---

Ils détectent les erreurs logiques.

#### --answer--

Ils garantissent un style de code cohérent.

### --question--

#### --text--

En JavaScript, qui ou quoi est généralement responsable d'allouer de la mémoire aux variables ?

#### --distractors--

Le développeur.

---

Le système d'exploitation.

---

Le serveur.

#### --answer--

Le moteur d'exécution.

### --question--

#### --text--

Que sera affiché dans la console avec le code suivant ?

```js
function trackTotal(initialValue) {
  let total = initialValue;
  return function(increment) {
    total += increment;
    return total;
  };
}

let track = trackTotal(1);
console.log(track(2));
console.log(track(3));
```

#### --distractors--

```js
2
5
```

---

```js
2
3
```

---

```js
5
6
```

#### --answer--

```js
3
6
```

### --question--

#### --text--

Qu'est-ce que le hoisting en JavaScript ?

#### --distractors--

Remonter les erreurs en haut du code.

---

Le processus de création de nouvelles variables.

---

Déplacer automatiquement les déclarations `var` en haut du bloc.

#### --answer--

Le processus qui consiste à déplacer les déclarations de variables en haut de leur portée.

### --question--

#### --text--

Pourquoi l'utilisation du mot-clé `var` est-elle considérée comme une mauvaise pratique en JavaScript moderne ?

#### --distractors--

Elles sont plus lentes que `let` et `const`.

---

Elles provoquent des erreurs de syntaxe.

---

Elles ne permettent pas la vérification de type.

#### --answer--

Elles sont accessibles en dehors de leur bloc.

### --question--

#### --text--

Quel est l'objectif des modules en JavaScript ?

#### --distractors--

Améliorer les performances du code.

---

Rendre le code plus difficile à comprendre.

---

Créer de nouveaux langages de programmation.

#### --answer--

Organiser le code en unités réutilisables.

### --question--

#### --text--

Comment ajoutes-tu un script `app.js` en tant que module dans un document HTML ?

#### --distractors--

```html
<script type="module/javascript" src="app.js"></script>
```

---

```html
<script src="app.js"></script>
```

---

```html
<script src="app.js" module></script>
```

#### --answer--

```html
<script type="module" src="app.js"></script>
```

### --question--

#### --text--

Lequel des exemples suivants exporte la fonction `calculateSum` depuis un module `utils.js` et l'importe et l'utilise correctement dans un fichier `app.js` ?

#### --distractors--

```js
// utils.js
export default function calculateSum(a, b) {
  return a + b;
}

// app.js
import { calculateSum } from './utils.js';
console.log(calculateSum(2, 3));
```

---

```js
// utils.js
export function calculateSum(a, b) {
  return a + b;
}

// app.js
import calculateSum from './utils.js';
console.log(calculateSum(2, 3));
```

---

```js
// utils.js
function calculateSum(a, b) {
  return a + b;
}

export default calculateSum;

// app.js
import * as utils from './utils.js';
console.log(utils.calculateSum(2, 3));
```

#### --answer--

```js
// utils.js
export function calculateSum(a, b) {
  return a + b;
}

// app.js
import { calculateSum } from './utils.js';
console.log(calculateSum(2, 3));
```
