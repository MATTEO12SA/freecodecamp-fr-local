---
id: 66f1ad049d7a6ac0886cc2ba
title: 'Quiz : formulaires React, récupération de données et routage'
challengeType: 8
dashedName: quiz-react-forms-data-fetching-and-routing
---

# --description--

Pour réussir le quiz, tu dois répondre correctement à au moins 18 des 20 questions ci-dessous.

# --quizzes--

## --quiz--

### --question--

#### --text--

Lequel des éléments suivants est un exemple d'entrée contrôlée ?

#### --distractors--

```jsx
<input id="name" type="text" />
```

---

```jsx
<input id="name" onChange={handleChange} type="text" />
```

---

```jsx
<input value="" id="name" type="text" />
```

#### --answer--

```jsx
<input value={name} id="name" onChange={handleChange} type="text" />
```

### --question--

#### --text--

Lequel des éléments suivants est un exemple d'entrée non contrôlée ?

#### --distractors--

```jsx
<input type="text" references={nameRef} id="name" />
```

---

```jsx
<input type="text" refs={nameRef} id="name" />
```

---

```jsx
<input type="text" reference={nameRef} id="name" />
```

#### --answer--

```jsx
<input type="text" ref={nameRef} id="name" />
```

### --question--

#### --text--

Lequel des éléments suivants N'EST PAS une façon couramment utilisée de récupérer des données en React ?

#### --distractors--

SWR

---

Axios

---

Fetch API

#### --answer--

Redux

### --question--

#### --text--

Lequel des Hooks suivants est couramment associé à la récupération de données en React ?

#### --distractors--

`useParams`

---

`useContext`

---

`useReducer`

#### --answer--

`useEffect`

### --question--

#### --text--

Qu'est-ce que la mémoïsation ?

#### --distractors--

Un algorithme courant utilisé pour trouver le plus court chemin entre deux sommets dans un graphe.

---

Une technique spéciale utilisée pour optimiser les performances de toutes les entrées contrôlées et non contrôlées.

---

Un algorithme divide and conquer utilisé pour trier récursivement des sous-tableaux et les fusionner.

#### --answer--

Une technique d'optimisation où le résultat d'appels de fonctions coûteux est mis en cache (mémorisé) selon des arguments précis.

### --question--

#### --text--

Lequel des Hooks suivants sert à mémoïser des valeurs calculées ?

#### --distractors--

`useParams`

---

`useEffect`

---

`useState`

#### --answer--

`useMemo`

### --question--

#### --text--

Lequel des éléments suivants utilise le bon Hook pour mémoïser des références de fonctions ?

#### --distractors--

```js
useContext(() => {
 // code goes here
}, [dependency]);
```

---

```js
useParams(() => {
 // code goes here
}, [dependency]);
```

---

```js
useState(() => {
 // code goes here
}, [dependency]);
```

#### --answer--

```js
useCallback(() => {
 // code goes here
}, [dependency]);
```

### --question--

#### --text--

Qu'est-ce qu'une dépendance ?

#### --distractors--

Une dépendance est une bibliothèque de tests couramment utilisée dans les applications React.

---

Une dépendance est un type de composant React qui ne s'exécute que sur le serveur.

---

Une dépendance est un compilateur spécial utilisé dans les applications React pour compiler le code React en JavaScript.

#### --answer--

Une dépendance est le cas où un composant ou module d'une application s'appuie sur un autre pour fonctionner correctement.

### --question--

#### --text--

Qu'est-ce qu'un gestionnaire de paquets ?

#### --distractors--

Un outil utilisé pour mettre à jour des paquets tiers installés dans les applications.

---

Un outil utilisé pour gérer des paquets uniquement créés au sein d'un projet.

---

Un outil utilisé pour déplacer les dépendances hors du dossier `node_modules` vers un dossier de config spécial.

#### --answer--

Un outil utilisé pour l'installation, les mises à jour et la suppression des dépendances.

### --question--

#### --text--

Lequel des éléments suivants N'EST PAS un gestionnaire de paquets couramment utilisé dans l'écosystème JavaScript ?

#### --distractors--

pnpm

---

Yarn

---

npm

#### --answer--

Jest

### --question--

#### --text--

Lequel des éléments suivants est un fichier de configuration clé qui contient des métadonnées sur ton projet ?

#### --distractors--

`config.json`

---

`lint.json`

---

`node_modules.json`

#### --answer--

`package.json`

### --question--

#### --text--

Lequel des éléments suivants est un framework React couramment utilisé ?

#### --distractors--

Django

---

Webpack

---

Gulp

#### --answer--

Remix

### --question--

#### --text--

Laquelle des façons suivantes est la bonne pour ajouter des routes à ton application React avec React Router ?

#### --distractors--

```jsx
<StrictMode>
  <AddRouter>
    <Routes>
      <Route path="/" element={<App />} />
    </Routes>
  </AddRouter>
</StrictMode>
```

---

```jsx
<StrictMode>
  <ContextRouter>
    <Routes>
      <Route path="/" element={<App />} />
    </Routes>
  </ContextRouter>
</StrictMode>
```

---

```jsx
<StrictMode>
  <SetRoute>
    <Routes>
      <Route path="/" element={<App />} />
    </Routes>
  </SetRoute>
</StrictMode>
```

#### --answer--

```jsx
<StrictMode>
  <BrowserRouter>
    <Routes>
      <Route path="/" element={<App />} />
    </Routes>
  </BrowserRouter>
</StrictMode>
```

### --question--

#### --text--

Laquelle des façons suivantes est la bonne pour créer une route dynamique dans React Router ?

#### --distractors--

```jsx
<Route path="?category" element={<Category />} />
```

---

```jsx
<Route path="<category" element={<Category />} />
```

---

```jsx
<Route path="/category" element={<Category />} />
```

#### --answer--

```jsx
<Route path=":category" element={<Category />} />
```

### --question--

#### --text--

Lequel des Hooks suivants sert à accéder aux paramètres dynamiques d'un chemin URL ?

#### --distractors--

`useParameterized`

---

`usePar`

---

`useParameters`

#### --answer--

`useParams`

### --question--

#### --text--

Laquelle des façons suivantes est la bonne pour créer un gestionnaire de requête personnalisé dans Next.js ?

#### --distractors--

```js
export async function SERVE() {
  const res = await fetch("https://example-api.com");
  const data = await res.json();

  return Response.json({ data });
}
```

---

```js
export async function FETCH() {
  const res = await fetch("https://example-api.com");
  const data = await res.json();

  return Response.json({ data });
}
```

---

```js
export async function SET() {
  const res = await fetch("https://example-api.com");
  const data = await res.json();

  return Response.json({ data });
}
```

#### --answer--

```js
export async function GET() {
  const res = await fetch("https://example-api.com");
  const data = await res.json();

  return Response.json({ data });
}
```

### --question--

#### --text--

Qu'est-ce que le prop drilling ?

#### --distractors--

Le processus de créer un reducer qui spécifie comment l'état doit changer en réponse à des props particulières utilisées.

---

Le processus de passer des props d'un composant enfant vers le composant parent.

---

Le processus de créer un context provider pour une application React et de l'utiliser pour envoyer des props du parent aux enfants.

#### --answer--

Le processus de passer des props d'un composant parent à des composants enfants profondément imbriqués, même quand certains des composants enfants n'ont pas besoin des props.

### --question--

#### --text--

Lequel des éléments suivants N'EST PAS une solution de gestion d'état couramment utilisée pour les applications React ?

#### --distractors--

Context API

---

Zustand

---

Redux

#### --answer--

Spring

### --question--

#### --text--

À quoi servent les React Developer Tools ?

#### --distractors--

Ces outils servent seulement à vérifier les problèmes d'accessibilité dans les applications React.

---

Ces outils servent à ajouter des tests supplémentaires pour les composants React.

---

Ces outils servent à mémoïser tous les composants React à l'intérieur de l'application.

#### --answer--

Ces outils servent à inspecter les composants React et identifier les problèmes de performance.

### --question--

#### --text--

Que sont les React Server Components ?

#### --distractors--

Ce sont des composants qui gèrent le context exclusivement sur le serveur.

---

Ce sont des composants mémoïsés qui se rendent exclusivement sur le serveur.

---

Ce sont des composants qui gèrent l'état exclusivement sur le serveur.

#### --answer--

Ce sont des composants qui se rendent exclusivement sur le serveur.
