---
id: 672a4cf959443073a6774908
title: Quels sont les différents états de formulaire et pourquoi sont-ils importants ?
challengeType: 19
dashedName: what-are-the-different-form-states
---

# --interactive--

En HTML, les contrôles de formulaire, comme les inputs, peuvent être dans différents états ou conditions, comme l'état `focused`, l'état `readonly` ou l'état `disabled`.

Le premier état serait considéré comme l'état `default`. L'état par défaut d'un input d'adresse e-mail est un input vide. C'est à cela que ressemble l'input e-mail quand il est affiché pour la première fois sur la page.

:::interactive_editor

```html
<input type="email" name="email" id="email" />
```

:::

Quand l'utilisateur clique sur un contrôle de formulaire ou le sélectionne avec la touche tabulation du clavier, cela signifie qu'il est dans l'état `focused`. Quand un input est dans l'état `focused`, la plupart des navigateurs affichent une bordure bleue mise en évidence autour de l'input. Mais tu peux choisir d'ajouter des styles supplémentaires en CSS.

Clique sur une zone vide dans la fenêtre d'aperçu, puis appuie sur la touche `tab` pour voir l'état de focus. Pour voir les aperçus, tu dois activer l'éditeur interactif.

:::interactive_editor

```html
<input type="email" name="email" id="email" />
```

:::

Un autre état de formulaire est l'état `disabled`. Cet état indique aux utilisateurs qu'un input ne peut pas recevoir le focus ni être activé.

Active l'éditeur interactif et essaie de cliquer sur l'input e-mail : tu remarqueras qu'il ne reçoit plus le focus.

:::interactive_editor

```html
<input disabled type="email" name="email" id="email" />
```

:::

Comme pour l'état `focused`, tu peux choisir d'ajouter des styles supplémentaires pour l'état `disabled` avec CSS.

Un autre type d'état de formulaire est l'état `readonly`. C'est quand un contrôle de formulaire, comme un input, n'est pas modifiable par l'utilisateur. Voici un exemple d'input e-mail défini en lecture seule. L'attribut `value` sert à définir la valeur affichée dans le champ input.

Active l'éditeur interactif et essaie de modifier la valeur actuelle `example@email.com` dans la fenêtre d'aperçu : tu remarqueras que ce n'est pas possible.

:::interactive_editor

```html
<input
  readonly
  type="email"
  name="email"
  id="email"
  value="example@email.com"
/>
```

:::

Une différence clé entre l'état `disabled` et l'état `readonly` est que `readonly` peut recevoir le focus, alors que l'état `disabled` ne le peut pas.

Comprendre les différents états de formulaire est important, car ils garantissent une expérience utilisateur fluide en fournissant des retours et des indications clairs pendant la gestion des erreurs.

# --questions--

## --text--

Quel attribut sert à désactiver un input ?

## --answers--

`readonly`

### --feedback--

Le nom de l'attribut suggère fortement la réponse.

---

`required`

### --feedback--

Le nom de l'attribut suggère fortement la réponse.

---

`checked`

### --feedback--

Le nom de l'attribut suggère fortement la réponse.

---

`disabled`

## --video-solution--

4

## --text--

Quel attribut sert à marquer un input comme étant en lecture seule ?

## --answers--

`checked`

### --feedback--

Le nom de l'attribut suggère fortement la réponse.

---

`readonly`

---

`size`

### --feedback--

Le nom de l'attribut suggère fortement la réponse.

---

`capture`

### --feedback--

Le nom de l'attribut suggère fortement la réponse.

## --video-solution--

2

## --text--

Quand l'état de focus se produit-il ?

## --answers--

Quand un champ input est désactivé et ne peut pas être utilisé.

### --feedback--

Reviens au début de la leçon où l'état focused a été abordé.

---

Quand un champ input est validé et affiche un message d'erreur.

### --feedback--

Reviens au début de la leçon où l'état focused a été abordé.

---

Quand l'utilisateur clique sur un input ou le sélectionne avec la touche tabulation pour lui donner le focus.

---

Quand un champ input a été prérempli avec des valeurs par défaut par le navigateur.

### --feedback--

Reviens au début de la leçon où l'état focused a été abordé.

## --video-solution--

3
