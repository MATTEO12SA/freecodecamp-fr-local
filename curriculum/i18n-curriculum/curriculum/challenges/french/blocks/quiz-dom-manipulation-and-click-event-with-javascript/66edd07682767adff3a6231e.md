---
id: 66edd07682767adff3a6231e
title: 'Quiz : manipulation du DOM et événements de clic avec JavaScript'
challengeType: 8
dashedName: quiz-dom-manipulation-and-click-event-with-javascript
---

# --description--

Pour réussir le quiz, tu dois répondre correctement à au moins 18 des 20 questions ci-dessous.

# --quizzes--

## --quiz--

### --question--

#### --text--

Qu'est-ce que le Document Object Model ?

#### --distractors--

Un langage de programmation utilisé pour interagir avec une page HTML.

---

Un ensemble d'objets qui ne contiennent que des nombres à virgule flottante.

---

Un ensemble de classes JavaScript représentant des éléments HTML.

#### --answer--

Une interface de programmation qui te permet d'interagir avec des documents HTML.

### --question--

#### --text--

Que signifie API ?

#### --distractors--

Additional Programming Interface

---

Adding Programming Interface

---

Another Programming Interface

#### --answer--

Application Programming Interface

### --question--

#### --text--

Comment utiliser JavaScript pour trouver toutes les ancres HTML sans hyperlien ?

#### --distractors--

```js
document.querySelectorAll("link![href]");
```

---

```js
document.querySelectorAll("link:not([href])");
```

---

```js
document.querySelectorAll("a![href]");
```

#### --answer--

```js
document.querySelectorAll("a:not([href])");
```

### --question--

#### --text--

Que fait la propriété `textContent` ?

#### --distractors--

Elle retire le contenu texte de tous les éléments HTML.

---

Elle retire le contenu texte de ton fichier JavaScript.

---

Elle ajoute du texte à tous les tableaux et objets de ton fichier JavaScript.

#### --answer--

Elle renvoie le contenu texte brut d'un élément.

### --question--

#### --text--

Que fait la propriété `innerHTML` ?

#### --distractors--

C'est une propriété de `Element` utilisée pour obtenir tout le contenu texte d'un élément HTML.

---

C'est une propriété de `Element` utilisée pour ajouter des annotations à ton HTML.

---

C'est une propriété de `Element` utilisée pour déclencher des événements dans ton HTML.

#### --answer--

C'est une propriété de `Element` utilisée pour définir ou mettre à jour des parties du marquage HTML.

### --question--

#### --text--

Lequel des suivants ajoutera un écouteur d'événement valide ?

#### --distractors--

```js
btn.addEventListener("accept", () => alert("You clicked the button"));
```

---

```js
btn.addEventListener("allow", () => alert("You clicked the button"));
```

---

```js
btn.addEventListener("trigger", () => alert("You clicked the button"));
```

#### --answer--

```js
btn.addEventListener("click", () => alert("You clicked the button"));
```

### --question--

#### --text--

Lequel des suivants N'EST PAS une méthode valide de l'objet `document` ?

#### --distractors--

`getElementById()`

---

`querySelector()`

---

`createElement()`

#### --answer--

`addElementToDOM()`

### --question--

#### --text--

Lequel des suivants définira l'attribut `class` à `my-class` ?

#### --distractors--

```js
const para = document.getElementById("para");
para.setAttribute("set", "class");
```

---

```js
const para = document.getElementById("para");
para.setAttribute("remove", "my-class");
```

---

```js
const para = document.getElementById("para");
para.setAttribute("add", "class");
```

#### --answer--

```js
const para = document.getElementById("para");
para.setAttribute("class", "my-class");
```

### --question--

#### --text--

Qu'est-ce que l'objet `Event` ?

#### --distractors--

C'est un objet spécial utilisé pour retirer des événements de ton projet JavaScript.

---

Il sert à ajouter automatiquement des événements à tes projets JavaScript.

---

C'est un objet spécial utilisé pour travailler avec des tableaux.

#### --answer--

C'est une charge utile déclenchée quand un utilisateur interagit d'une manière ou d'une autre avec ta page web.

### --question--

#### --text--

Lequel des suivants est une méthode utilisée pour retirer un écouteur d'événement précédemment ajouté à un élément avec `addEventListener()` ?

#### --distractors--

`removalOfEventListener()`

---

`removedEventListener()`

---

`removingEventListener()`

#### --answer--

`removeEventListener()`

### --question--

#### --text--

Que sont les gestionnaires d'événements en ligne ?

#### --distractors--

Des attributs spéciaux sur un élément HTML utilisés pour annuler tous les événements.

---

Des attributs spéciaux sur un élément HTML utilisés uniquement pour déclencher des soumissions de formulaire dans ton JavaScript.

---

Des attributs spéciaux sur un élément HTML utilisés pour retirer des événements.

#### --answer--

Des attributs spéciaux sur un élément HTML utilisés pour exécuter du code JavaScript quand un événement se produit.

### --question--

#### --text--

Quelles sont les deux catégories principales des API web ?

#### --distractors--

Les API Java et les API tierces.

---

Les API navigateur et les API Rust.

---

Les API système et les API tierces.

#### --answer--

Les API navigateur et les API tierces.

### --question--

#### --text--

Quel événement est déclenché quand tous les éléments de la page HTML ont été analysés par le navigateur ?

#### --distractors--

`load`

---

`DOMLoaded`

---

`HTMLContentLoaded`

#### --answer--

`DOMContentLoaded`

### --question--

#### --text--

Lequel des suivants est une propriété qui représente le style en ligne d'un élément ?

#### --distractors--

`content`

---

`event`

---

`load`

#### --answer--

`style`

### --question--

#### --text--

Lequel des suivants est la bonne façon d'utiliser un gestionnaire d'événement en ligne ?

#### --distractors--

`<button eventclick="validate()">Validate</button>`

---

`<button click="validate()">Validate</button>`

---

`<button clickevent="validate()">Validate</button>`

#### --answer--

`<button onclick="validate()">Validate</button>`

### --question--

#### --text--

Lequel des méthodes suivantes sert à retarder une action pendant un temps donné ?

#### --distractors--

`setInterval()`

---

`delay()`

---

`sleep()`

#### --answer--

`setTimeout()`

### --question--

#### --text--

Lequel des suivants peut servir à créer et contrôler des animations directement en JavaScript ?

#### --distractors--

Allow Animations API

---

Control Animations API

---

Create Animations API

#### --answer--

Web Animations API

### --question--

#### --text--

Que fait l'API Canvas ?

#### --distractors--

Elle sert à animer des graphiques dans ton fichier CSS.

---

Elle sert uniquement à retirer des graphiques de ton fichier JavaScript.

---

Elle sert à retirer des graphiques de ton fichier HTML.

#### --answer--

Elle sert à manipuler des graphiques via ton fichier JavaScript.

### --question--

#### --text--

Qu'est-ce que la délégation d'événements ?

#### --distractors--

Le processus d'écouter des événements quand un événement est annulé.

---

Le processus d'écouter des événements quand un événement click se produit.

---

Le processus d'écouter des événements quand un événement change se produit.

#### --answer--

Le processus d'écouter des événements qui ont remonté vers un parent.

### --question--

#### --text--

Lequel des suivants N'EST PAS une méthode que tu peux utiliser avec des dialogs et des modales ?

#### --distractors--

`show()`

---

`close()`

---

`showModal()`

#### --answer--

`removeModal()`
