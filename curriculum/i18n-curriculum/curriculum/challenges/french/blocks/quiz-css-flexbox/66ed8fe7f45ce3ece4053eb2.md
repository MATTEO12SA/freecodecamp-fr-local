---
id: 66ed8fe7f45ce3ece4053eb2
title: Quiz CSS Flexbox
challengeType: 8
dashedName: quiz-css-flexbox
---

# --description--

Pour réussir le quiz, tu dois répondre correctement à au moins 18 des 20 questions ci-dessous.

# --quizzes--

## --quiz--

### --question--

#### --text--

Qu'est-ce que Flexbox ?

#### --distractors--

Un modèle bidimensionnel pour la mise en page.

---

Un modèle tridimensionnel pour la mise en page.

---

Un modèle multidimensionnel pour la mise en page.

#### --answer--

Un modèle unidimensionnel pour la mise en page.

### --question--

#### --text--

Quelle propriété CSS est définie pour activer la mise en page Flexbox pour l'élément `div` ?

#### --distractors--

```css
div {
  flex: 1;
}
```

---

```css
div {
  flex-direction: row;
}
```

---

```css
div {
  flex-wrap: wrap;
}
```

#### --answer--

```css
div {
  display: flex;
}
```

### --question--

#### --text--

Quelle propriété CSS permet aux éléments flex de passer sur une nouvelle ligne quand la ligne actuelle est pleine ?

#### --distractors--

`flex-direction`

---

`justify-content`

---

`align-items`

#### --answer--

`flex-wrap`

### --question--

#### --text--

Que contrôle la propriété Flexbox `justify-content` ?

#### --distractors--

Elle contrôle la direction des éléments enfants du conteneur Flexbox.

---

Elle contrôle le style de texte des éléments enfants du conteneur Flexbox.

---

Elle contrôle l'alignement des éléments enfants dans le conteneur Flexbox.

#### --answer--

Elle contrôle la distribution des éléments enfants sur l'axe principal.

### --question--

#### --text--

Quelle propriété contrôle la direction des éléments dans le conteneur Flexbox ?

#### --distractors--

`flex-wrap`

---

`flex`

---

`justify-content`

#### --answer--

`flex-direction`

### --question--

#### --text--

Quelles propriétés CSS peuvent être utilisées pour faire s'écouler les éléments flex de gauche à droite, et faire apparaître le nouvel élément flex au-dessus du précédent si la largeur de la ligne actuelle est dépassée ?

#### --distractors--

```css
.container {
  display: flex;
  flex-direction: column;
  flex-wrap: wrap-reverse;
}
```

---

```css
.container {
  display: flex;
  flex-direction: row;
  flex-wrap: wrap;
}
```

---

```css
.container {
  display: flex;
  flex-direction: column-reverse;
  flex-wrap: wrap;
}
```

#### --answer--

```css
.container {
  display: flex;
  flex-direction: row;
  flex-wrap: wrap-reverse;
}
```

### --question--

#### --text--

Quelle propriété contrôle le positionnement des éléments sur l'axe transversal ?

#### --distractors--

`flex-wrap`

---

`flex-direction`

---

`justify-content`

#### --answer--

`align-items`

### --question--

#### --text--

Laquelle n'est pas une valeur pour `justify-content` ?

#### --distractors--

`flex-start`

---

`center`

---

`space-between`

#### --answer--

`row`

### --question--

#### --text--

Quelles propriétés CSS utiliserais-tu pour centrer un élément dans un conteneur flex, horizontalement et verticalement ?

#### --distractors--

```css
.container {
  display: flex;
  justify-content: center;
}
```

---

```css
.container {
  display: flex;
  align-items: center;
}
```

---

```css
.container {
  display: flex;
  align-content: center;
}
```

#### --answer--

```css
.container {
  display: flex;
  justify-content: center;
  align-items: center;
}
```

### --question--

#### --text--

Quelle valeur de `flex-direction` fait aller les éléments flex de droite à gauche ?

#### --distractors--

`column-reverse`

---

`column`

---

`row`

#### --answer--

`row-reverse`

### --question--

#### --text--

Qu'est-ce qui n'est pas une valeur de la propriété `flex-wrap` ?

#### --distractors--

`nowrap`

---

`wrap`

---

`wrap-reverse`

#### --answer--

`row`

### --question--

#### --text--

Que fait la valeur `stretch` dans `align-items` ?

#### --distractors--

Les éléments sont serrés directement les uns contre les autres vers le bord de départ du conteneur.

---

Les éléments sont alignés de sorte que les lignes de base de leur conteneur flex s'alignent.

---

Les éléments sont serrés directement les uns contre les autres vers le bord de fin du conteneur.

#### --answer--

Elle redimensionne automatiquement les éléments pour qu'ils soient aussi grands les uns que les autres et remplissent le conteneur.

### --question--

#### --text--

Quelle valeur de `justify-content` place le premier élément au début, le dernier élément à la fin, et espace uniformément les autres éléments entre eux le long de l'axe principal ?

#### --distractors--

`space-evenly`

---

`space-around`

---

`center`

#### --answer--

`space-between`

### --question--

#### --text--

Quelle valeur de `justify-content` espace les éléments de sorte que les espaces entre chaque paire d'éléments adjacents et les bords du conteneur soient tous égaux ?

#### --distractors--

`space-between`

---

`space-around`

---

`center`

#### --answer--

`space-evenly`

### --question--

#### --text--

Quelle valeur de `justify-content` espace les éléments de façon égale le long de l'axe principal tout en laissant la moitié de cet espace aux bords du conteneur ?

#### --distractors--

`space-evenly`

---

`center`

---

`space-between`

#### --answer--

`space-around`

### --question--

#### --text--

Lequel des éléments suivants aligne les éléments au début de l'axe transversal ?

#### --distractors--

`align-items: flex-starts;`

---

`align-items: baseline;`

---

`align-items: first-baseline;`

#### --answer--

`align-items: flex-start;`

### --question--

#### --text--

Lequel des éléments suivants aligne les éléments à la fin de l'axe transversal ?

#### --distractors--

`align-items: flex-ends;`

---

`align-items: end;`

---

`align-items: flex-ending;`

#### --answer--

`align-items: flex-end;`

### --question--

#### --text--

Quelle valeur de `flex-direction` fait en sorte que les éléments soient alignés le long de l'axe principal de gauche à droite ?

#### --distractors--

`column`

---

`column-reverse`

---

`row-reverse`

#### --answer--

`row`

### --question--

#### --text--

Quelle valeur de `flex-direction` fait en sorte que les éléments soient alignés le long de l'axe transversal de haut en bas ?

#### --distractors--

`row`

---

`column-reverse`

---

`row-reverse`

#### --answer--

`column`

### --question--

#### --text--

Lequel des cas d'utilisation suivants n'est pas adapté à Flexbox ?

#### --distractors--

Centrer un élément dans un conteneur.

---

Aligner verticalement des éléments dans une ligne.

---

Ajuster dynamiquement la distribution et l'alignement des éléments pour différentes tailles d'écran.

#### --answer--

Un dimensionnement fixe et un espacement entre les éléments comme une grille.

## --quiz--

### --question--

#### --text--

Quel est le but d'un modèle de mise en page unidimensionnel comme Flexbox ?

#### --distractors--

Gérer la mise en page en deux dimensions, lignes et colonnes en même temps.

---

Contrôler l'ordre d'empilement des éléments positionnés avec `z-index`.

---

Créer des transformations 3D et des animations complexes sur les éléments de la page.

#### --answer--

Disposer les éléments le long d'une seule ligne ou d'un seul axe, soit comme une ligne, soit comme une colonne.

### --question--

#### --text--

Quelle propriété CSS alignera tous les éléments flex du côté droit de l'élément `.container` ?

```css
.container {
  display: flex;
  flex-direction: row;
}
```

#### --distractors--

`justify-content: flex-start;`

---

`justify-content: space-between;`

---

`align-items: flex-end;`

#### --answer--

`justify-content: flex-end;`

### --question--

#### --text--

Quels sont les deux axes qui définissent la direction de la mise en page des éléments dans le modèle flex ?

#### --distractors--

L'axe horizontal et l'axe vertical.

---

L'axe x et l'axe y.

---

L'axe primaire et l'axe secondaire.

#### --answer--

L'axe principal et l'axe transversal.

### --question--

#### --text--

Laquelle des règles CSS suivantes organise les éléments dans une colonne verticale et les centre aussi horizontalement dans leur conteneur ?

#### --distractors--

```css
.container {
  display: flex;
  flex-direction: column;
  justify-content: center;
}
```

---

```css
.container {
  display: flex;
  flex-direction: row;
  align-items: center;
}
```

---

```css
.container {
  display: flex;
  flex-direction: row;
  justify-content: center;
}
```

#### --answer--

```css
.container {
  display: flex;
  flex-direction: column;
  align-items: center;
}
```

### --question--

#### --text--

Quel est l'objectif principal de la propriété `flex-wrap` ?

#### --distractors--

Elle définit la direction dans laquelle les éléments flex sont placés dans le conteneur flex.

---

Elle aligne les éléments flex le long de l'axe principal du conteneur.

---

Elle aligne les éléments flex le long de l'axe transversal du conteneur.

#### --answer--

Elle détermine si les éléments flex doivent passer sur de nouvelles lignes pour tenir dans l'espace disponible.

### --question--

#### --text--

Quel est le comportement par défaut de la déclaration `flex-wrap: nowrap;` ?

#### --distractors--

Elle force les éléments flex à passer sur une nouvelle ligne s'ils sont trop larges pour le conteneur.

---

Elle fait passer les éléments sur une nouvelle ligne, mais dans l'ordre inverse.

---

Elle empêche le conteneur flex d'être plus grand que son élément parent.

#### --answer--

Elle force les éléments flex à rester sur une seule ligne, même s'ils débordent du conteneur.

### --question--

#### --text--

Quelle propriété CSS est une propriété raccourcie pour définir à la fois `flex-direction` et `flex-wrap` ?

#### --distractors--

`flex-box`

---

`flex-align`

---

`flex-container`

#### --answer--

`flex-flow`

### --question--

#### --text--

Quel est l'effet de `flex-direction: column-reverse;` sur un conteneur flex ?

#### --distractors--

Elle organise les éléments horizontalement de droite à gauche.

---

Elle inverse les éléments dans une ligne, mais les garde sur une seule ligne.

---

Elle organise les éléments verticalement de haut en bas.

#### --answer--

Elle organise les éléments verticalement dans l'ordre inverse, de bas en haut.

### --question--

#### --text--

Quel snippet CSS organise correctement les éléments flex dans une colonne verticale et leur permet de passer dans une nouvelle colonne s'ils dépassent la hauteur du conteneur ?

#### --distractors--

```css
.container {
  display: flex;
  flex-flow: row wrap;
}
```

---

```css
.container {
  display: flex;
  flex-direction: column;
  flex-wrap: nowrap;
}
```

---

```css
.container {
  display: flex;
  justify-content: center;
  align-items: center;
}
```

#### --answer--

```css
.container {
  display: flex;
  flex-flow: column wrap;
}
```

### --question--

#### --text--

Quelle valeur de `justify-content` aligne tous les éléments flex à la fin de l'axe principal du conteneur ?

#### --distractors--

`justify-content: end;`

---

`justify-content: center;`

---

`justify-content: flex-start;`

#### --answer--

`justify-content: flex-end;`

### --question--

#### --text--

Quel est l'effet de la règle CSS suivante sur les éléments flex ?

```css
.container {
  flex-flow: row nowrap;
}
```

#### --distractors--

Les éléments sont organisés en colonne et passeront dans une nouvelle colonne.

---

Les éléments sont organisés en ligne et passeront sur une nouvelle ligne.

---

Les éléments sont organisés en colonne et déborderont s'ils ne tiennent pas.

#### --answer--

Les éléments sont organisés en ligne et déborderont du conteneur s'ils ne tiennent pas.

### --question--

#### --text--

Comment `justify-content: space-evenly;` aligne-t-il les éléments flex ?

```css
.container {
  display: flex;
  justify-content: space-evenly;
}
```

#### --distractors--

Les éléments sont espacés uniformément, sans espace au début ni à la fin.

---

Les éléments sont regroupés au centre du conteneur.

---

Les éléments sont poussés vers le côté droit du conteneur, avec un espacement égal entre eux.

#### --answer--

Les éléments ont un espace égal entre eux et aux deux extrémités du conteneur.

### --question--

#### --text--

Quelle propriété utiliserais-tu pour centrer verticalement les éléments quand `flex-direction: row;` est défini ?

#### --distractors--

`justify-content: center;`

---

`vertical-align: middle;`

---

`text-align: center;`

#### --answer--

`align-items: center;`

### --question--

#### --text--

Que fait la valeur `flex-start` pour la propriété `justify-content` ?

#### --distractors--

Elle aligne les éléments au début de l'axe transversal.

---

Elle étire les éléments pour remplir l'axe principal du conteneur.

---

Elle centre tous les éléments le long de l'axe principal.

#### --answer--

Elle aligne les éléments au début de l'axe principal du conteneur.

### --question--

#### --text--

Comment configurerais-tu un conteneur flex pour organiser ses éléments de droite à gauche sur une seule ligne, et aussi les positionner en haut du conteneur ?

#### --distractors--

```css
.container {
  display: flex;
  flex-direction: row-reverse;
  align-items: center;
}
```

---

```css
.container {
  display: flex;
  flex-direction: column-reverse;
  align-items: flex-start;
}
```

---

```css
.container {
  display: flex;
  flex-wrap: wrap-reverse;
  align-items: flex-start;
}
```

#### --answer--

```css
.container {
  display: flex;
  flex-direction: row-reverse;
  align-items: flex-start;
}
```

### --question--

#### --text--

Laquelle des déclarations suivantes est valide pour la propriété `flex-flow` ?

#### --distractors--

`flex-flow: center wrap;`

---

`flex-flow: row space-between;`

---

`flex-flow: column reverse;`

#### --answer--

`flex-flow: column wrap-reverse;`

### --question--

#### --text--

Laquelle des règles CSS suivantes aura pour résultat de centrer les éléments flex horizontalement et de les étirer verticalement pour remplir la hauteur du conteneur ?

#### --distractors--

```css
.container {
  display: flex;
  justify-content: center;
  align-items: center;
}
```

---

```css
.container {
  display: flex;
  justify-content: stretch;
  align-items: center;
}
```

---

```css
.container {
  display: flex;
  justify-content: flex-start;
  align-items: stretch;
}
```

#### --answer--

```css
.container {
  display: flex;
  justify-content: center;
  align-items: stretch;
}
```

### --question--

#### --text--

Quel effet a `align-items: stretch;` quand `flex-direction` est défini à `column` ?

#### --distractors--

Cela étire les éléments verticalement pour remplir la hauteur du conteneur.

---

Cela n'a aucun effet quand la direction est `column`.

---

Cela étire les éléments verticalement pour correspondre à l'élément le plus haut.

#### --answer--

Cela étire les éléments horizontalement pour remplir la largeur du conteneur.

### --question--

#### --text--

Si tu veux inverser l'ordre des éléments dans une ligne horizontale, quelle déclaration devrais-tu utiliser ?

#### --distractors--

`flex-direction: column-reverse;`

---

`flex-wrap: wrap-reverse;`

---

`flex-direction: reverse-row;`

#### --answer--

`flex-direction: row-reverse;`

### --question--

#### --text--

Quelle valeur de `align-items` positionnera les éléments en haut d'un conteneur dont `flex-direction` est `row` ?

#### --distractors--

`align-items: top;`

---

`justify-content: flex-start;`

---

`align-items: stretch;`

#### --answer--

`align-items: flex-start;`
