---
id: 67f41242431cbf3db8ca79c7
title: Quiz sur les bases de Python
challengeType: 8
dashedName: quiz-python-basics
---

# --description--

Pour réussir le quiz, tu dois répondre correctement à au moins 18 des 20 questions ci-dessous.

# --quizzes--

## --quiz--

### --question--

#### --text--

Laquelle de ces fonctions sert à vérifier si une variable correspond à un type de données précis ?

#### --distractors--

`istype()`

---

`isdata()`

---

`isvariable()`

#### --answer--

`isinstance()`

### --question--

#### --text--

Laquelle de ces options N'EST PAS une forme de concaténation de chaînes ?

#### --distractors--

```py
greeting = 'My name is '
developer = 'Jessica'

greeting += developer
```

---

```py
developer = 'Jessica'
greeting = 'My name is ' + developer + '.'
```

---

```py
developer = 'Jessica'
age = 30
greeting = 'My name is ' + developer + ' and I am ' + str(age) + ' years old.'
```

#### --answer--

```py
developer = 'Jessica'
greeting = x'My name is {developer}.'
```

### --question--

#### --text--

Laquelle de ces fonctions sert à renvoyer le nombre de caractères dans la chaîne ?

#### --distractors--

`counting()`

---

`length()`

---

`iscount()`

#### --answer--

`len()`

### --question--

#### --text--

Que vaudra `result` dans cet exemple ?

```py
developer = 'Naomi'

result = developer.endswith('N') # ?
```

#### --distractors--

`Undefined`

---

`True`

---

`None`

#### --answer--

`False`

### --question--

#### --text--

Que se passe-t-il quand tu additionnes un float et un entier ?

#### --distractors--

Le résultat sera un message d'erreur.

---

Le résultat sera `None`.

---

Le résultat sera un entier.

#### --answer--

Le résultat sera un float.

### --question--

#### --text--

Laquelle de ces options est la bonne façon de définir une fonction ?

#### --distractors--

```py
define get_sum(num_1, num_2) {
    return num_1 + num_2
}
```

---

```py
set get_sum(num_1, num_2):
    return num_1 + num_2
```

---

```py
function get_sum(num_1, num_2) {
    return num_1 + num_2
}
```

#### --answer--

```py
def get_sum(num_1, num_2):
    return num_1 + num_2
```

### --question--

#### --text--

Qu'est-ce qui s'affichera dans la console ?

```py
def greet():
    pass

print(greet()) # ?
```

#### --distractors--

`TypeError`

---

`RangeError`

---

`Null`

#### --answer--

`None`

### --question--

#### --text--

Laquelle de ces affirmations est fausse pour le nommage des variables ?

#### --distractors--

Les noms de variables ne peuvent commencer que par une lettre ou un underscore (_), pas par un nombre.

---

Les noms de variables ne peuvent contenir que des caractères alphanumériques (a-z, A-Z, 0-9) et des underscores (_).

---

Les noms de variables ne peuvent pas être un mot-clé réservé de Python comme `if`, `class` ou `def`.

#### --answer--

Les noms de variables doivent avoir une longueur maximale de 10 caractères, sinon Python lèvera une erreur.

### --question--

#### --text--

Lequel de ces types de données N'EST PAS pris en charge en Python ?

#### --distractors--

`None`

---

`int`

---

`float`

#### --answer--

`Generic`

### --question--

#### --text--

Laquelle de ces options renvoie une nouvelle chaîne avec tous les caractères convertis en majuscules ?

#### --distractors--

```py
developer = 'Jessica'

print(developer.isupper()) # JESSICA
```

---

```py
developer = 'Jessica'

print(developer.up()) # JESSICA
```

---

```py
developer = 'Jessica'

print(developer.toUpper()) # JESSICA
```

#### --answer--

```py
developer = 'Jessica'

print(developer.upper()) # JESSICA
```

### --question--

#### --text--

Laquelle de ces fonctions sert à obtenir une saisie de l'utilisateur ?

#### --distractors--

`read()`

---

`cout()`

---

`prompt()`

#### --answer--

`input()`

### --question--

#### --text--

Quel sera le résultat du code suivant ?

```py
message = 'Python is fun!'

print(message[0:6])  # ?
```

#### --distractors--

`'Py'`

---

`'fun'`

---

`'is'`

#### --answer--

`'Python'`

### --question--

#### --text--

Que fait la méthode `split()` ?

#### --distractors--

Cette méthode sert à découper un tuple en une liste de sous-chaînes.

---

Cette méthode sert à découper un float en une liste de sous-chaînes.

---

Cette méthode sert à découper un dictionnaire en une liste de sous-chaînes.

#### --answer--

Cette méthode sert à découper une chaîne en une liste de sous-chaînes.

### --question--

#### --text--

Qu'affichera ce qui suit dans la console ?

```py
example_list = ['example', 'dashed', 'name']

joined_str = ' '.join(example_list)
print(joined_str)  # ?
```

#### --distractors--

`TypeError`

---

`'dashed name'`

---

`None`

#### --answer--

`'example dashed name'`

### --question--

#### --text--

Laquelle de ces fonctions sert à créer une table de correspondances caractère à caractère pour une traduction ?

#### --distractors--

`str.translations()`

---

`str.gettranslate()`

---

`str.tran()`

#### --answer--

`str.maketrans()`

### --question--

#### --text--

Quel sera le résultat du code suivant ?

```py
int_1 = 4
int_2 = 2

print(int_1 ** int_2) # ?
```

#### --distractors--

8

---

2

---

4

#### --answer--

16

### --question--

#### --text--

Que renvoie la méthode `find()` si aucune occurrence de la sous-chaîne n'est trouvée dans une chaîne ?

#### --distractors--

-2

---

1

---

0

#### --answer--

-1

### --question--

#### --text--

Laquelle de ces fonctions sert à compter le nombre de fois qu'une sous-chaîne apparaît dans une chaîne ?

#### --distractors--

`counter()`

---

`hascount()`

---

`counting()`

#### --answer--

`count()`

### --question--

#### --text--

Que fait la division entière en Python ?

#### --distractors--

Cet opérateur sert à multiplier deux nombres et à arrondir le résultat à l'entier supérieur.

---

Cet opérateur sert à convertir un float en entier.

---

Cet opérateur sert à élever un nombre à la puissance d'un autre.

#### --answer--

Cet opérateur sert à diviser deux nombres et à arrondir le résultat à l'entier inférieur.

### --question--

#### --text--

Laquelle de ces fonctions sert à arrondir un nombre à l'entier le plus proche ?

#### --distractors--

`floor()`

---

`ceil()`

---

`float()`

#### --answer--

`round()`
