---
id: 612e83ec2eca1e370f830511
title: Étape 6
challengeType: 0
dashedName: step-6
---

# --description--

Ajoute un élément `link` dans ton élément `head`. Définis son attribut `rel` sur `stylesheet` et son attribut `href` sur `styles.css`.

# --hints--

Ton code devrait contenir un élément `link`.

```js
assert.isNotNull(document.querySelector('link'));
```

Tu ne devrais pas modifier tes balises `head` existantes. Assure-toi de ne pas avoir supprimé la balise fermante.

```js
const headElements = document.querySelectorAll('head');
assert.strictEqual(headElements?.length, 1);
```

Tu devrais avoir un élément `link`.

```js
assert.strictEqual(document.querySelectorAll('link')?.length, 1);
```

Ton élément `link` devrait être dans ton élément `head`.

```js
const headContentRegex = /(?<=<head\s*>)[\S|\s]*(?=<\/head\s*>)/;
const headElementContent = code.match(headContentRegex);

const headElement = document.createElement("head");
headElement.innerHTML = headElementContent;
assert.isNotNull(headElement.querySelector('link'));
```

Ton élément `link` devrait avoir un attribut `rel` avec la valeur `stylesheet`.

```js
const linkElement = document.querySelector('link');
const rel = linkElement?.getAttribute("rel");
assert.strictEqual(rel, "stylesheet");
```

Ton élément `link` devrait avoir un attribut `href` avec la valeur `styles.css`.

```js
const linkElement = document.querySelector('link');
assert.strictEqual(linkElement?.dataset.href, "styles.css");
```

# --seed--

## --seed-contents--

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    --fcc-editable-region--
    <meta charset="UTF-8" />
    <title>Piano</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    --fcc-editable-region--
  </head>
  <body>
    <div id="piano">
      <div class="keys">
        <div class="key"></div>
        <div class="key black--key"></div>
        <div class="key black--key"></div>
        <div class="key"></div>
        <div class="key black--key"></div>
        <div class="key black--key"></div>
        <div class="key black--key"></div>

        <div class="key"></div>
        <div class="key black--key"></div>
        <div class="key black--key"></div>
        <div class="key"></div>
        <div class="key black--key"></div>
        <div class="key black--key"></div>
        <div class="key black--key"></div>

        <div class="key"></div>
        <div class="key black--key"></div>
        <div class="key black--key"></div>
        <div class="key"></div>
        <div class="key black--key"></div>
        <div class="key black--key"></div>
        <div class="key black--key"></div>
      </div>
    </div>
  </body>
</html>
```

```css

```
