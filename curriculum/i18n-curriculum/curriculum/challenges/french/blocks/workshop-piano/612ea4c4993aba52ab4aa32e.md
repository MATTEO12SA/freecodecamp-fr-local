---
id: 612ea4c4993aba52ab4aa32e
title: Étape 16
challengeType: 0
dashedName: step-16
---

# --description--

Il est maintenant temps d'utiliser les pseudo-sélecteurs que tu as configurés plus tôt. Pour créer les touches noires, ajoute un nouveau sélecteur `.key.black--key::after`. Il cible les éléments qui ont à la fois les classes `key` et `black--key`, puis sélectionne le pseudo-élément créé après ces éléments dans le HTML.

Dans le nouveau sélecteur, définis `background-color` sur `#1d1e22`. Définis aussi la propriété `content` sur `""`. Cela rendra les pseudo-éléments vides.

La propriété `content` sert à définir ou remplacer le contenu d'un élément. Par défaut, les pseudo-éléments créés par les sélecteurs `::before` et `::after` sont vides, ce qui signifie qu'ils ne sont pas rendus sur la page. En définissant la propriété `content` sur une chaîne vide `""`, tu t'assures que les pseudo-éléments sont rendus, tout en restant visuellement vides.

Si tu veux expérimenter, essaie de retirer la propriété `background-color` et de définir différentes valeurs pour la propriété `content`, comme `"♥"`. Pense à annuler ces changements quand tu as terminé pour que les tests passent.

# --hints--

Tu devrais avoir un sélecteur `.key.black--key::after`.

```js
assert.exists(new __helpers.CSSHelp(document).getStyle('.key.black--key::after'));
```

Ton sélecteur `.key.black--key::after` devrait avoir une propriété `background-color` définie sur `#1d1e22`.

```js
assert.equal(new __helpers.CSSHelp(document).getStyle('.key.black--key::after')?.backgroundColor, 'rgb(29, 30, 34)');
```

Ton sélecteur `.key.black--key::after` devrait avoir une propriété `content` définie sur `""`.

```js
assert.equal(new __helpers.CSSHelp(document).getStyle('.key.black--key::after')?.content, '""');
```

# --seed--

## --seed-contents--

```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Piano</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="./styles.css">
  </head>
  <body>
    <div id="piano">
      <div class="keys">
        <div class="key"></div>
        <div class="key black--key"></div>
        <div class="key black--key"></div>
        <div class="key"></div>
        <div class="key black--key"></div>
        <div class="key black--key"></div>
        <div class="key black--key"></div>

        <div class="key"></div>
        <div class="key black--key"></div>
        <div class="key black--key"></div>
        <div class="key"></div>
        <div class="key black--key"></div>
        <div class="key black--key"></div>
        <div class="key black--key"></div>

        <div class="key"></div>
        <div class="key black--key"></div>
        <div class="key black--key"></div>
        <div class="key"></div>
        <div class="key black--key"></div>
        <div class="key black--key"></div>
        <div class="key black--key"></div>
      </div>
    </div>
  </body>
</html>
```

```css
html {
  box-sizing: border-box;
}

*, *::before, *::after {
  box-sizing: inherit;
}

#piano {
  background-color: #00471b;
  width: 992px;
  height: 290px;
  margin: 80px auto;
  padding: 90px 20px 0 20px;
}

.keys {
  background-color: #040404;
  width: 949px;
  height: 180px;
  padding-left: 2px;
}

.key {
  background-color: #ffffff;
  position: relative;
  width: 41px;
  height: 175px;
  margin: 2px;
  float: left;
}

--fcc-editable-region--

--fcc-editable-region--
```
