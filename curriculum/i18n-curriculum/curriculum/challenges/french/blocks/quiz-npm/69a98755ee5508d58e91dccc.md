---
id: 69a98755ee5508d58e91dccc
title: Quiz NPM
challengeType: 8
dashedName: quiz-npm
---

# --description--

Pour réussir le quiz, tu dois répondre correctement à au moins 18 des 20 questions ci-dessous.

# --quizzes--

## --quiz--

### --question--

#### --text--

Que fournit npm aux développeurs ?

#### --distractors--

Une plateforme curatée qui n'héberge que des assets de design comme des polices et des icônes.

---

Un service d'hébergement qui déploie automatiquement ton projet dans le cloud.

---

Une solution de base de données qui gère les enregistrements utilisateur pour tes apps.

#### --answer--

Un registre de paquets logiciels préconstruits qui résolvent des problèmes courants.

### --question--

#### --text--

Lequel des éléments suivants n'est pas l'une des trois parties auxquelles npm fait référence ?

#### --distractors--

Le site web npmjs.com.

---

Le registre de paquets où les bibliothèques sont stockées.

---

L'outil en ligne de commande pour installer et gérer les paquets.

#### --answer--

L'environnement d'exécution Node.js.

### --question--

#### --text--

Que fait la commande `npm install` ?

#### --distractors--

Elle génère un tout nouveau projet Node.js à partir d'un template.

---

Elle exécute ton application Node.js sur un serveur de développement local.

---

Elle retire les paquets qui ne sont plus nécessaires dans ton projet.

#### --answer--

Elle ajoute des paquets préconstruits à ton projet pour des tâches courantes.

### --question--

#### --text--

Quel fichier stocke des métadonnées comme le nom du projet, la version, l'auteur et les dépendances pour un projet Node.js ?

#### --distractors--

`README.md`.

---

`node_modules/package.json`.

---

`package-lock.json`.

#### --answer--

`package.json`.

### --question--

#### --text--

Comment peux-tu créer interactivement un fichier `package.json` ?

#### --distractors--

Exécuter `npm install` sans spécifier de nom de paquet.

---

Télécharger un template depuis la page d'accueil du registre npm.

---

Copier le fichier depuis un autre projet et le renommer.

#### --answer--

Exécuter la commande `npm init`.

### --question--

#### --text--

Comment crées-tu un fichier `package.json` avec des valeurs par défaut sans répondre aux invites ?

#### --distractors--

Exécuter `npm init --interactive`.

---

Créer un fichier vide nommé `package.json` et le laisser vide.

---

Utiliser `npm init` et supprimer chaque invite après avoir ouvert l'éditeur.

#### --answer--

Exécuter `npm init -y` (ou `npm init --yes`) pour accepter automatiquement toutes les valeurs par défaut.

### --question--

#### --text--

Où npm liste-t-il les paquets nécessaires uniquement pendant le développement, comme les linters ou les test runners ?

#### --distractors--

Dans la section `"dependencies"`.

---

Dans la section `"scripts"` sous des commandes personnalisées.

---

Dans le dossier `node_modules` lui-même.

#### --answer--

Dans la section `"devDependencies"` de `package.json`.

### --question--

#### --text--

Quelle commande installe `nodemon` comme dépendance de développement uniquement ?

#### --distractors--

`npm uninstall nodemon`.

---

`npm save nodemon --dev`.

---

`npm install nodemon`.

#### --answer--

`npm install nodemon -D`.

### --question--

#### --text--

Quel dossier et quel fichier sont ajoutés à la racine de ton projet après l'installation d'une dépendance ?

#### --distractors--

Dossier `public` et `index.html`.

---

Dossier `dist` et `webpack.config.js`.

---

Dossier `src` et `README.md`.

#### --answer--

Dossier `node_modules` et fichier `package-lock.json`.

### --question--

#### --text--

Comment `package-lock.json` aide-t-il à garder les builds cohérents ?

#### --distractors--

En supprimant les dépendances inutilisées avant chaque installation.

---

En stockant uniquement les numéros de version majeurs des paquets.

---

En mettant automatiquement à niveau les paquets vers leurs dernières versions.

#### --answer--

En gelant les versions exactes de chaque dépendance qui a été installée.

### --question--

#### --text--

Pourquoi devrais-tu committer `package-lock.json` dans ton dépôt Git ?

#### --distractors--

Pour que npm tourne plus vite sur la machine d'un développeur.

---

Pour mettre automatiquement à jour les dépendances obsolètes.

---

Pour réduire la taille du dépôt.

#### --answer--

Pour que toute l'équipe installe les mêmes versions de dépendances et la même configuration.

### --question--

#### --text--

Comment installes-tu une version spécifique d'un paquet, comme `express@4.21.2` ?

#### --distractors--

`npm update express@4.21.2`.

---

`npm get express@4.21.2`.

---

`npm use express@4.21.2`.

#### --answer--

`npm install express@4.21.2`.

### --question--

#### --text--

Que permet le symbole caret (`^`) quand on spécifie des versions de dépendances ?

#### --distractors--

Il verrouille la dépendance à une version de patch exacte.

---

Il autorise les mises à jour de la version majeure uniquement.

---

Il exclut toutes les versions inférieures à celle spécifiée.

#### --answer--

Il autorise les versions mineures et de patch plus récentes mais bloque les nouvelles versions majeures.

### --question--

#### --text--

Que signifie le symbole tilde (`~`) dans une plage de versions comme `~1.2.3` ?

#### --distractors--

Il autorise les mises à jour des versions majeures et mineures.

---

Il installe la version majeure la plus récente quelle que soit la plage.

---

Il empêche npm d'installer toute mise à jour.

#### --answer--

Il autorise les mises à jour de la version de patch uniquement tout en gardant la version mineure fixe.

### --question--

#### --text--

Quel type de changements déclenche une montée de version majeure dans le versionnage sémantique ?

#### --distractors--

De petits ajustements stylistiques aux commentaires et au formatage.

---

De petites corrections de bugs qui ne changent pas le comportement.

---

Des fonctionnalités optionnelles qui ne cassent pas le code existant.

#### --answer--

Des breaking changes qui peuvent faire cesser de fonctionner le code existant.

### --question--

#### --text--

Quelle commande liste les dépendances qui ont de nouvelles versions publiées ?

#### --distractors--

`npm check`.

---

`npm list`.

---

`npm refresh`.

#### --answer--

`npm outdated`.

### --question--

#### --text--

Yarn, PNPM et Bun sont mentionnés comme alternatives à npm. Quel genre d'outils sont-ils ?

#### --distractors--

Des sites de registre où tu publies ou télécharges des paquets npm.

---

Des services de base de données qui stockent des données utilisateur pour les paquets npm.

---

Des plateformes d'hébergement cloud qui déploient automatiquement des projets npm.

#### --answer--

Des gestionnaires de paquets alternatifs.

### --question--

#### --text--

Où sont déclarés les scripts npm personnalisés dans un projet ?

#### --distractors--

Dans le fichier `.npmrc`.

---

Dans la section `"dependencies"` de `package.json`.

---

Dans un fichier séparé `scripts.json` à la racine.

#### --answer--

Dans l'objet `"scripts"` de `package.json`.

### --question--

#### --text--

Comment exécutes-tu un script npm personnalisé nommé `build` ?

#### --distractors--

`build npm`.

---

`node build`.

---

`run build`.

#### --answer--

`npm run build`.

### --question--

#### --text--

Que fait le double tiret (`--`) quand on exécute `npm run start -- 8000` ?

#### --distractors--

Il rend la sortie du script silencieuse.

---

Il force npm à exécuter le script en parallèle.

---

Il demande des privilèges administratifs avant d'exécuter le script.

#### --answer--

Il dit à npm de transmettre tout ce qui suit `--` comme arguments à la commande sous-jacente.
