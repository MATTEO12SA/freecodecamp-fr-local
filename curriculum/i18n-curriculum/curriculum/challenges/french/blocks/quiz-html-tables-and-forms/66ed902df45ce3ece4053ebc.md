---
id: 66ed902df45ce3ece4053ebc
title: Quiz sur les tableaux et formulaires HTML
challengeType: 8
dashedName: quiz-html-tables-and-forms
---

# --description--

Pour réussir le quiz, tu dois répondre correctement à au moins 18 des 20 questions ci-dessous.

# --quizzes--

## --quiz--

### --question--

#### --text--

Lequel des attributs suivants sert à indiquer l’URL où les données du formulaire doivent être envoyées ?

#### --distractors--

`capture`

---

`accept`

---

`lang`

#### --answer--

`action`

### --question--

#### --text--

Quels deux éléments servent à indiquer une ligne et un en-tête de ligne dans un tableau ?

#### --distractors--

L’élément `r` sert aux lignes et l’élément `thead` sert à l’en-tête.

---

L’élément `row` sert aux lignes et l’élément `th` sert à l’en-tête.

---

L’élément `tr` sert aux lignes et l’élément `head` sert à l’en-tête.

#### --answer--

L’élément `tr` sert aux lignes et l’élément `th` sert à l’en-tête.

### --question--

#### --text--

À quoi sert l’élément `td` ?

#### --distractors--

Il sert à définir un type de données de tableau.

---

Il sert à fusionner deux colonnes.

---

Il sert à fusionner deux lignes.

#### --answer--

Il sert à définir une cellule de tableau.

### --question--

#### --text--

Quelle est la fonction de l’attribut `colspan` ?

#### --distractors--

Il définit la longueur d’une colonne dans un tableau.

---

Il supprime le saut de ligne entre du texte dans une colonne.

---

Il sert à définir le nombre de colonnes.

#### --answer--

Il définit le nombre de colonnes sur lesquelles une cellule doit s’étendre.

### --question--

#### --text--

Quel est le rôle de l’attribut `placeholder` ?

#### --distractors--

Il sert à envoyer les données au serveur quand le formulaire est soumis.

---

Il sert à associer un élément `label` à un élément `input`.

---

Il sert à indiquer qu’un champ de saisie est en lecture seule pour l’utilisateur.

#### --answer--

Il sert à fournir une indication pour un champ de saisie.

### --question--

#### --text--

Quel est le rôle de l’élément `thead` ?

#### --distractors--

Il sert à regrouper le contenu du corps dans un tableau HTML.

---

Il sert à regrouper la description d’un tableau HTML.

---

Il sert à regrouper le contenu du pied dans un tableau HTML.

#### --answer--

Il sert à regrouper le contenu d’en-tête dans un tableau HTML.

### --question--

#### --text--

Lequel des exemples suivants montre une association explicite entre labels et inputs ?

#### --distractors--

```html
<label connect="age">Age:</label>

<input type="number" id="age" name="age">
```

---

```html
<label associate="age">Age:</label>

<input type="number" id="age" name="age">
```

---

```html
<label explicit="age">Age:</label>

<input type="number" id="age" name="age">
```

#### --answer--

```html
<label for="age">Age:</label>

<input type="number" id="age" name="age">
```

### --question--

#### --text--

À quoi sert un validateur HTML ?

#### --distractors--

Un validateur est un outil qui formate automatiquement ton code HTML.

---

Un validateur est un outil qui applique des styles à ton HTML.

---

Un validateur est un outil qui accélère l’exécution de ton code HTML.

#### --answer--

Un validateur est un outil qui vérifie la syntaxe du code HTML pour s’assurer qu’il est valide.

### --question--

#### --text--

Quel élément sert à définir une cellule dans un tableau ?

#### --distractors--

`th`

---

`tc`

---

`tcol`

#### --answer--

`td`

### --question--

#### --text--

Lequel des outils suivants permet d’inspecter et de modifier la structure HTML d’une page web ?

#### --distractors--

Suppression du DOM.

---

Testeur DOM.

---

Validation DOM.

#### --answer--

Inspecteur DOM.

### --question--

#### --text--

Quelle est la bonne façon de regrouper des champs de saisie liés ?

#### --distractors--

```html
<form>
  <fieldset>
    <caption>User information</caption>

    <label for="name">Name: </label>
    <input type="text" id="name" />

    <label for="email">Email: </label>
    <input type="email" id="email" />
  </fieldset>
</form>
```

---

```html
<form>
  <span>
    <p>User information</p>

    <label for="name">Name: </label>
    <input type="text" id="name" />

    <label for="email">Email: </label>
    <input type="email" id="email" />
  </span>
</form>
```

---

```html
<form>
  <fieldset>
    <title>User information</title>

    <label for="name">Name: </label>
    <input type="text" id="name" />

    <label for="email">Email: </label>
    <input type="email" id="email" />
  </fieldset>
</form>
```

#### --answer--

```html
<form>
  <fieldset>
    <legend>User information</legend>

    <label for="name">Name: </label>
    <input type="text" id="name" />

    <label for="email">Email: </label>
    <input type="email" id="email" />
  </fieldset>
</form>
```

### --question--

#### --text--

Lequel des éléments suivants sert à indiquer qu’un champ est en lecture seule ?

#### --distractors--

`readInputOnly`

---

`read-only`

---

`only-read`

#### --answer--

`readonly`

### --question--

#### --text--

Quel attribut indique qu’un input doit être rempli avant la soumission du formulaire ?

#### --distractors--

`necessary`

---

`obligatory`

---

`essential`

#### --answer--

`required`

### --question--

#### --text--

À quoi servent les DevTools ?

#### --distractors--

Ces outils sont intégrés directement au navigateur et servent à repérer les problèmes de lint dans ton code.

---

Ces outils sont intégrés directement au navigateur et servent à formater automatiquement ton code.

---

Ces outils sont intégrés directement au navigateur et servent à garantir une couverture de tests de 100 %.

#### --answer--

Ces outils sont intégrés directement au navigateur et servent à déboguer, profiler et analyser des pages web.

### --question--

#### --text--

Quelle est la bonne façon de désactiver un input ?

#### --distractors--

```html
<input type="checkbox" disabling />
```

---

```html
<input type="checkbox" inputDisabled />
```

---

```html
<input type="checkbox" disabledInput />
```

#### --answer--

```html
<input type="checkbox" disabled />
```

### --question--

#### --text--

Laquelle des valeurs suivantes est valide pour l’attribut `type` ?

#### --distractors--

```html
<input type="time" />
```

---

```html
<input type="action" />
```

---

```html
<input type="capture" />
```

#### --answer--

```html
<input type="email" />
```

### --question--

#### --text--

Quel exemple utilise correctement l’attribut `size` ?

#### --distractors--

```html
<input id="fullName" type="text" size=".001" />
```

---

```html
<input id="fullName" type="text" size="large" />
```

---

```html
<input id="fullName" type="text" size="10vh" />
```

#### --answer--

```html
<input id="fullName" type="text" size="10" />
```

### --question--

#### --text--

Quels attributs servent à indiquer le nombre minimal et maximal de caractères requis dans un champ de saisie ?

#### --distractors--

`minlen` and `maxlen`

---

`min` and `max`

---

`minimumLen` and `maximumLen`

#### --answer--

`minlength` and `maxlength`

### --question--

#### --text--

Lequel des exemples de boutons suivants n’utilise **PAS** une valeur correcte pour l’attribut `type` ?

#### --distractors--

```html
<button type="submit">Submit</button>
```

---

```html
<button type="button">Example Button</button>
```

---

```html
<button type="reset">Reset</button>
```

#### --answer--

```html
<button type="btn">Example Btn</button>
```

### --question--

#### --text--

Lequel des attributs suivants sert à indiquer la valeur d’un bouton ?

#### --distractors--

`buttonValue`

---

`val`

---

`btnVal`

#### --answer--

`value`

## --quiz--

### --question--

#### --text--

Quel est le rôle de l’attribut `action` dans la balise ouvrante `form` ?

#### --distractors--

Il sert à indiquer qu’un champ de saisie est en lecture seule.

---

Il sert à définir le nombre de caractères visibles pendant que l’utilisateur saisit du texte dans l’input.

---

Il sert à afficher une indication à l’utilisateur pour lui montrer quoi saisir dans le champ.

#### --answer--

Il sert à indiquer l’URL où les données du formulaire doivent être envoyées.

### --question--

#### --text--

Lequel des attributs suivants sert à indiquer la méthode HTTP à utiliser lors de l’envoi des données du formulaire ?

#### --distractors--

`set`

---

`type`

---

`http`

#### --answer--

`method`

### --question--

#### --text--

Laquelle des méthodes suivantes est courante quand on travaille avec des formulaires ?

#### --distractors--

`PUSH`

---

`SET`

---

`PULL`

#### --answer--

`GET`

### --question--

#### --text--

Laquelle des valeurs suivantes n’est PAS valide pour l’attribut `type` ?

#### --distractors--

`number`

---

`email`

---

`text`

#### --answer--

`http`

### --question--

#### --text--

Quelle est la bonne façon de créer un bouton dans un formulaire ?

#### --distractors--

```html
<form>
  <input get="button" value="Show Alert" />
</form>
```

---

```html
<form>
  <input btn="button" value="Show Alert" />
</form>
```

---

```html
<form>
  <input set="button" value="Show Alert" />
</form>
```

#### --answer--

```html
<form>
  <input type="button" value="Show Alert" />
</form>
```

### --question--

#### --text--

Qu’est-ce que l’association implicite de formulaire ?

#### --distractors--

C’est le cas où des inputs peuvent être associés à des labels en utilisant les attributs `action` et `for` sur l’élément `label`.

---

C’est le cas où des inputs peuvent être associés à des labels en utilisant les attributs `required` et `for` sur l’élément `label`.

---

C’est le cas où des inputs peuvent être associés à des labels en utilisant l’attribut `for` sur l’élément `label`.

#### --answer--

C’est le cas où des inputs peuvent être associés à des labels en plaçant le champ de saisie à l’intérieur de l’élément `label`.

### --question--

#### --text--

Quel est le rôle de l’élément `fieldset` ?

#### --distractors--

Il sert à regrouper le contenu d’en-tête dans un tableau HTML.

---

Il sert à créer un label pour un champ de saisie.

---

Il sert à ajouter une légende qui décrit le groupe d’inputs.

#### --answer--

Il sert à regrouper des inputs liés.

### --question--

#### --text--

Lequel des exemples suivants sert à créer une case à cocher dans un formulaire ?

#### --distractors--

```html
  <input check="type" id="location" name="location" value="location" />
```

---

```html
  <input type="check" id="location" name="location" value="location" />
```

---

```html
  <input checkbox="type" id="location" name="location" value="location" />
```

#### --answer--

```html
  <input type="checkbox" id="location" name="location" value="location" />
```

### --question--

#### --text--

Lequel des éléments suivants sert à ajouter un titre à un tableau HTML ?

#### --distractors--

`titles`

---

`title`

---

`captions`

#### --answer--

`caption`

### --question--

#### --text--

Quel est le rôle de l’élément `legend` ?

#### --distractors--

Il sert à regrouper le contenu du corps dans un tableau HTML.

---

Il sert à ajouter une légende qui décrit les cellules d’un tableau.

---

Il sert à regrouper des inputs liés.

#### --answer--

Il sert à ajouter une légende qui décrit le groupe d’inputs.

### --question--

#### --text--

Lequel des éléments suivants sert à regrouper le contenu d’en-tête dans un tableau HTML ?

#### --distractors--

`header`

---

`head`

---

`theader`

#### --answer--

`thead`

### --question--

#### --text--

Quelle est la bonne façon d’ajouter des lignes et des cellules de données à un tableau ?

#### --distractors--

```html
<table>
  <tr>
    <data>Davis</data>
    <data>Alex</data>
    <data>54</data>
  </tr>
</table>
```

---

```html
<table>
  <tr>
    <cell>Davis</cell>
    <cell>Alex</cell>
    <cell>54</cell>
  </tr>
</table>
```

---

```html
<table>
  <row>
    <td>Davis</td>
    <td>Alex</td>
    <td>54</td>
  </row>
</table>
```

#### --answer--

```html
<table>
  <tr>
    <td>Davis</td>
    <td>Alex</td>
    <td>54</td>
  </tr>
</table>
```

### --question--

#### --text--

Qu’est-ce que l’inspecteur DOM ?

#### --distractors--

Un outil qui sert à supprimer tous les éléments du DOM.

---

Un ensemble d’outils intégrés directement au navigateur qui aide à déboguer, profiler et analyser des pages web.

---

Un outil qui vérifie la syntaxe du code HTML pour s’assurer qu’il est valide.

#### --answer--

Un outil qui permet d’inspecter et de modifier la structure HTML d’une page web.

### --question--

#### --text--

Lequel des outils suivants vérifie la syntaxe du code HTML pour s’assurer qu’il est valide ?

#### --distractors--

HTML validate

---

HTML isValid

---

HTML valid

#### --answer--

HTML validator

### --question--

#### --text--

Quelle est la bonne façon d’indiquer le nombre de colonnes pour une cellule de données dans un tableau ?

#### --distractors--

```html
<td columns="2">Average Grade</td>
```

---

```html
<td col="2">Average Grade</td>
```

---

```html
<td column="2">Average Grade</td>
```

#### --answer--

```html
<td colspan="2">Average Grade</td>
```

### --question--

#### --text--

Qu’est-ce que l’état de focus pour un champ de saisie ?

#### --distractors--

C’est l’état d’un champ de saisie lorsque le focus est retiré par l’utilisateur.

---

C’est l’état d’un champ de saisie lorsqu’il est défini en lecture seule par l’utilisateur.

---

C’est l’état d’un champ de saisie lorsqu’il est désactivé par l’utilisateur.

#### --answer--

C’est l’état d’un champ de saisie lorsqu’il est sélectionné par l’utilisateur.

### --question--

#### --text--

Quelle est la bonne façon de créer un bouton radio ?

#### --distractors--

```html
<input id="no-option" select="radio" name="hotel-stay" />
```

---

```html
<input id="no-option" radio="type" name="hotel-stay" />
```

---

```html
<input id="no-option" type="radio-btn" name="hotel-stay" />
```

#### --answer--

```html
<input id="no-option" type="radio" name="hotel-stay" />
```

### --question--

#### --text--

Quel est le rôle de l’attribut `for` ?

#### --distractors--

Il sert à afficher une indication à l’utilisateur.

---

Il sert à indiquer la valeur de l’input

---

Il sert à créer un label pour un champ de saisie.

#### --answer--

Il sert à indiquer à quel champ de saisie le label est associé.

### --question--

#### --text--

Lequel des attributs suivants sert à définir le nombre de caractères visibles pendant que l’utilisateur saisit du texte dans l’input ?

#### --distractors--

`setsize`

---

`sizing`

---

`sizes`

#### --answer--

`size`

### --question--

#### --text--

Lequel des attributs suivants sert à afficher une indication à l’utilisateur pour lui montrer quoi saisir dans le champ ?

#### --distractors--

`showhint`

---

`hint`

---

`clue`

#### --answer--

`placeholder`
