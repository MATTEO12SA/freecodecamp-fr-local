---
id: 699c6f4d4e468a10d3ae25ff
title: Quiz sur les structures de données
challengeType: 8
dashedName: quiz-data-structures-js
---

# --description--

Pour réussir le quiz, tu dois répondre correctement à au moins 18 des 20 questions ci-dessous.

# --quizzes--

## --quiz--

### --question--

#### --text--

Que décrit la notation Big O dans l'analyse d'algorithmes ?

#### --distractors--

Le temps d'exécution exact en secondes sur un ordinateur précis.

---

Le pourcentage de lignes de code exécutées pendant une exécution.

---

À quel point le code est lisible pour les autres développeurs.

#### --answer--

Comment le temps ou l'espace croît par rapport à la taille de l'entrée (une borne supérieure).

### --question--

#### --text--

Quand tu commences un défi algorithmique, quelle est la meilleure première étape ?

#### --distractors--

Commencer à coder immédiatement pour prendre de l'élan.

---

Optimiser les performances avant de comprendre le problème.

---

Écrire des tests unitaires seulement après avoir terminé la solution.

#### --answer--

Clarifier le problème et les contraintes avec des exemples et des cas limites.

### --question--

#### --text--

Quelle est la différence clé entre les tableaux dynamiques et les tableaux statiques ?

#### --distractors--

Les tableaux dynamiques stockent des valeurs de types différents ; les tableaux statiques ne le peuvent pas.

---

Les tableaux statiques autorisent les doublons ; les tableaux dynamiques non.

---

Les tableaux dynamiques sont plus rapides que les tableaux statiques pour chaque opération.

#### --answer--

Les tableaux dynamiques peuvent grandir ou rétrécir par redimensionnement ; les tableaux statiques ont une taille fixe.

### --question--

#### --text--

Quelle est la complexité temporelle amortie d'ajouter un élément à la fin d'un tableau dynamique ?

#### --distractors--

`O(n)`

---

`O(log n)`

---

`O(n log n)`

#### --answer--

`O(1)` amorti

### --question--

#### --text--

Pourquoi l'accès au k-ième élément par indice dans une liste simplement chaînée prend-il un temps `O(n)` ?

#### --distractors--

La liste doit être redimensionnée avant tout accès.

---

L'indice est haché et recherché dans une table.

---

Les nœuds sont stockés de façon contiguë, donc un décalage est requis.

#### --answer--

Tu dois parcourir depuis le nœud de tête jusqu'au k-ième nœud un par un.

### --question--

#### --text--

Quelle fonctionnalité une liste doublement chaînée a-t-elle qu'une liste simplement chaînée n'a pas ?

#### --distractors--

Un accès aléatoire à n'importe quel indice en temps `O(1)`.

---

Un tampon de tableau intégré pour une itération plus rapide.

---

Un maintien automatique de la longueur de la liste comme constante.

#### --answer--

Des pointeurs vers les nœuds suivant et précédent permettant un parcours en arrière.

### --question--

#### --text--

Laquelle des propositions suivantes décrit le mieux une pile ?

#### --distractors--

Premier entré, premier sorti (`FIFO`) avec retraits à l'avant.

---

Une structure où n'importe quel élément peut être retiré en temps `O(1)`.

---

Un tampon circulaire avec accès aléatoire en temps constant.

#### --answer--

Dernier entré, premier sorti (`LIFO`) avec `push` et `pop` au sommet.

### --question--

#### --text--

Quelle opération retire l'élément à l'avant d'une file ?

#### --distractors--

`push`

---

`pop`

---

`peek`

#### --answer--

`dequeue`

### --question--

#### --text--

Quelle est la complexité temporelle typique en moyenne pour rechercher une valeur par clé dans une table de hachage ?

#### --distractors--

`O(n)` car toutes les clés doivent être parcourues séquentiellement.

---

`O(log n)` à cause d'une recherche binaire dans les seaux.

---

`O(n log n)` car les clés sont triées pendant l'insertion.

#### --answer--

`O(1)` en moyenne avec une bonne fonction de hachage et un faible facteur de charge.

### --question--

#### --text--

Quelle garantie une structure de données ensemble (set) fournit-elle ?

#### --distractors--

Les éléments sont stockés par défaut dans un ordre trié.

---

Les valeurs en double sont autorisées et regroupées.

---

Les éléments sont indexés par leur position d'insertion.

#### --answer--

Elle ne stocke que des éléments uniques (pas de doublons).

### --question--

#### --text--

Dans un tableau dynamique, quelle est la complexité temporelle dans le pire des cas pour insérer un élément à l'indice i (pas à la fin) ?

#### --distractors--

`O(1)`

---

`O(log n)`

---

`O(1)` amorti

#### --answer--

`O(n)`

### --question--

#### --text--

Quelle est la complexité temporelle d'insérer un nouveau nœud en tête d'une liste simplement chaînée ?

#### --distractors--

`O(n)`

---

`O(log n)`

---

`O(n log n)`

#### --answer--

`O(1)`

### --question--

#### --text--

Quelle opération renvoie l'élément au sommet d'une pile sans le retirer ?

#### --distractors--

`push`

---

`pop`

---

Insérer en bas.

#### --answer--

`peek`

### --question--

#### --text--

Laquelle des propositions suivantes décrit le mieux une file ?

#### --distractors--

Dernier entré, premier sorti (`LIFO`) avec retraits au sommet.

---

Un accès aléatoire à n'importe quel indice en temps `O(1)`.

---

Les éléments sont toujours maintenus automatiquement dans un ordre trié.

#### --answer--

Premier entré, premier sorti (`FIFO`) avec `enqueue` à l'arrière et `dequeue` à l'avant.

### --question--

#### --text--

Comment appelle-t-on une description de haut niveau de la logique d'un algorithme, indépendante du langage ?

#### --distractors--

Syntax

---

Machine code

---

Debugger

#### --answer--

Pseudocode

### --question--

#### --text--

Quelle est l'une des choses que tu devrais faire pour comprendre un problème ?

#### --distractors--

Commencer à coder immédiatement.

---

Ignorer les cas limites.

---

Passer directement à l'optimisation.

#### --answer--

Lire l'énoncé du problème plusieurs fois.

### --question--

#### --text--

Quels deux composants devrais-tu identifier en résolvant un problème ?

#### --distractors--

Le langage de programmation et l'IDE.

---

Les noms de variables et les autres identifiants.

---

Le schéma de couleurs et le formatage du code.

#### --answer--

L'entrée et la sortie attendue.

### --question--

#### --text--

Quelle complexité temporelle croît plus vite que `O(n log n)` quand n devient grand ?

#### --distractors--

`O(n)`

---

`O(log n)`

---

`O(1)`

#### --answer--

`O(n^2)`

### --question--

#### --text--

Après avoir implémenté une solution brute-force, quelle est une bonne étape suivante ?

#### --distractors--

Micro-optimiser les facteurs constants avant de mesurer.

---

Abandonner les tests et réécrire la solution depuis zéro.

---

Éviter de considérer les cas limites pour garder le code simple.

#### --answer--

Analyser sa complexité temps/espace et optimiser les goulots d'étranglement identifiés.

### --question--

#### --text--

Que mesure la complexité spatiale ?

#### --distractors--

Combien de cœurs CPU un programme utilise.

---

La longueur d'un programme en lignes de code.

---

Combien de temps un programme met à compiler.

#### --answer--

Comment l'utilisation de la mémoire croît par rapport à la taille de l'entrée.
