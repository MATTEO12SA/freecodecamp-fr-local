---
id: 66f1ae758b77cfc3e4da6151
title: Quiz TypeScript
challengeType: 8
dashedName: quiz-typescript
---

# --description--

Pour réussir le quiz, tu dois répondre correctement à au moins 18 des 20 questions ci-dessous.

# --quizzes--

## --quiz--

### --question--

#### --text--

Qu’est-ce que TypeScript ?

#### --distractors--

Un framework utilisé pour la programmation système.

---

Un outil de gestion de base de données.

---

Un langage de programmation utilisé pour styliser les sites web.

#### --answer--

Un sur-ensemble de JavaScript qui ajoute le typage statique.

### --question--

#### --text--

Pourquoi devrait-on utiliser TypeScript dans un projet ?

#### --distractors--

Il élimine le besoin de tests dans les applications.

---

Il est plus rapide que tous les autres langages de programmation.

---

Il facilite le stylage des sites web.

#### --answer--

Il améliore la maintenabilité en ajoutant des définitions de types à JavaScript.

### --question--

#### --text--

Vers quel langage TypeScript est-il compilé ?

#### --distractors--

Java

---

Python

---

C++

#### --answer--

JavaScript

### --question--

#### --text--

Quelle serait la sortie de l’extrait de code suivant ?

```ts
function sum(a: number, b: number): number {
  return a + b;
}
console.log(sum(2, 3));
```

#### --distractors--

`2 + 3`

---

`undefined`

---

`Error: Type 'number' is not assignable to type 'string'`

#### --answer--

`5`

### --question--

#### --text--

Quel est le rôle principal d’une interface en TypeScript ?

#### --distractors--

Compiler le code TypeScript en JavaScript.

---

Créer un nouveau type de données.

---

Créer un nouvel objet JavaScript.

#### --answer--

Définir la structure d’un objet.

### --question--

#### --text--

Lequel des éléments suivants N’EST PAS un type primitif de base en TypeScript ?

#### --distractors--

string

---

boolean

---

number

#### --answer--

array

### --question--

#### --text--

Lequel des éléments suivants définit correctement un type littéral en TypeScript ?

#### --distractors--

`let y: string;`

---

`let z: boolean;`

---

`let a: number[];`

#### --answer--

`let x: 5;`

### --question--

#### --text--

Quel est le rôle principal du mot-clé `type` en TypeScript ?

#### --distractors--

Déclarer des variables.

---

Créer des classes.

---

Implémenter des interfaces.

#### --answer--

Définir des types personnalisés.

### --question--

#### --text--

Lequel des éléments suivants définit correctement un type union ?

#### --distractors--

`type Result = Pass && Fail;`

---

`type Result = Pass & Fail;`

---

`type Result = Pass + Fail;`

#### --answer--

`type Result = Pass | Fail;`

### --question--

#### --text--

Laquelle des capacités suivantes est unique à `interface` et NE PEUT PAS être obtenue avec `type` en TypeScript ?

#### --distractors--

Définir la forme des objets.

---

Créer des types union.

---

Définir des types qui n’incluent que des valeurs spécifiques.

#### --answer--

Déclarer des implémentations de classes.

### --question--

#### --text--

Quel est le rôle principal de l’utilisation des generics en TypeScript ?

#### --distractors--

Implémenter l’héritage entre classes.

---

Définir la structure d’un objet.

---

Créer un nouveau type de données.

#### --answer--

Écrire du code réutilisable qui peut fonctionner avec plusieurs types.

### --question--

#### --text--

Lequel des éléments suivants est un **exemple correct** de fonction générique en TypeScript ?

#### --distractors--

`function add(a: number, b: number): number { return a + b; }`

---

`function multiply(a: number, b: number): { return a * b; }`

---

`function getValue<T>(value: any): any { return value; }`

#### --answer--

`function identity<T>(value: T): T { return value; }`

### --question--

#### --text--

Qu’est-ce que le type narrowing en TypeScript ?

#### --distractors--

Réduire la taille du JavaScript compilé.

---

Limiter le nombre de types dans une union.

---

Retirer les types inutilisés d’un projet TypeScript.

#### --answer--

Écrire du code qui restreint un type union à un type plus spécifique.

### --question--

#### --text--

Quel fichier est typiquement utilisé pour configurer les options du compilateur TypeScript ?

#### --distractors--

`package.json`

---

`package.config.json`

---

`typescript.config.js`

#### --answer--

`tsconfig.json`

### --question--

#### --text--

Que spécifie l’option `rootDir` dans le fichier `tsconfig.json` ?

#### --distractors--

La version de TypeScript à utiliser pendant la compilation.

---

L’environnement d’exécution cible (par ex. Node.js ou navigateur) pour le code TypeScript.

---

Le système de modules spécifique à utiliser pour les imports et exports.

#### --answer--

Le répertoire qui contient tes fichiers source.

### --question--

#### --text--

Quel exemple utilise `instanceof` correctement pour restreindre le type afin d’accéder à la propriété `value` en toute sécurité ?

#### --distractors--

```ts
const email = document.querySelector("#email");
if (email instanceof <input>) {
    console.log(email.value);
}
```

---

```ts
const email = document.querySelector("#email");
if (email instanceof Element) {
    console.log(email.value);
}
```

---

```ts
const email = document.querySelector("#email");
if (email instanceof <HTMLInputElement>) {
    console.log(email.value);
}
```

#### --answer--

```ts
const email = document.querySelector("#email");
if (email instanceof HTMLInputElement) {
    console.log(email.value);
}
```

### --question--

#### --text--
Que fait le réglage `"strict": true` dans `tsconfig.json` en TypeScript ?

#### --distractors--

Active la vérification de types statique pour que tu puisses attraper les erreurs avant d’exécuter ton code.

---

T’oblige à toujours utiliser la syntaxe ES6.

---

T’oblige à compiler le code avant de l’exécuter.

#### --answer--

Active un ensemble d’options de vérification de types comme exiger la gestion de `null` ou `undefined` et empêcher le `any` implicite.

### --question--

#### --text--

Quel est le rôle du type `never` en TypeScript ?

#### --distractors--

Représenter null ou undefined.

---

Représenter n’importe quelle valeur possible.

---

Représenter un type qui peut être n’importe quoi sauf null.

#### --answer--

Représenter le type des valeurs qui ne se produisent jamais.

### --question--

#### --text--

Lequel des appels de fonction `getRandomValue` suivants compilera avec succès en TypeScript ?

```ts
const getRandomValue = (array: string[]) => {
  return array[Math.floor(Math.random() * array.length)];
}
```

#### --distractors--

`getRandomValue([1, 2, 3])`

---

`getRandomValue("ABC")`

---

`getRandomValue({A: "A", B: "B", C: "C"})`

#### --answer--

`getRandomValue(["A", "B", "C"])`

### --question--

#### --text--

Comment peux-tu corriger l’erreur de compilateur TypeScript `'email' is possibly 'null'` dans le code suivant en utilisant le chaînage optionnel ?

```ts
const email = document.querySelector<HTMLInputElement>("#email");
console.log(email.value);
```

#### --distractors--

`console.log(email?:value);`

---

`console.log(email?value);`

---

`console.log(email.?value);`

#### --answer--

`console.log(email?.value);`
