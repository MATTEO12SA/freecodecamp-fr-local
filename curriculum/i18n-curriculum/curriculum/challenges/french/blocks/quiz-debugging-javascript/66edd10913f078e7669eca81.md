---
id: 66edd10913f078e7669eca81
title: Quiz sur le débogage JavaScript
challengeType: 8
dashedName: quiz-debugging-javascript
---

# --description--

Pour réussir le quiz, tu dois répondre correctement à au moins 18 des 20 questions ci-dessous.

# --quizzes--

## --quiz--

### --question--

#### --text--

Quel type d'erreur survient quand une variable est utilisée avant d'être définie ?

#### --distractors--

`SyntaxError`

---

`LogicalError`

---

`RangeError`

#### --answer--

`ReferenceError`

### --question--

#### --text--

Quelle instruction JavaScript peut être utilisée pour mettre intentionnellement en pause l'exécution de ton code à des fins de débogage ?

#### --distractors--

`try...catch`

---

`console.log()`

---

`alert()`

#### --answer--

`debugger`

### --question--

#### --text--

Quelle méthode de la console peut être utilisée pour afficher des données sous forme de tableau ?

#### --distractors--

`console.log()`

---

`console.warn()`

---

`console.error()`

#### --answer--

`console.table()`

### --question--

#### --text--

Comment peux-tu inspecter les requêtes réseau dans les outils de développement Chrome ?

#### --distractors--

En utilisant l'onglet "Network Links".

---

En utilisant l'onglet "Console".

---

En utilisant l'onglet "Sources".

#### --answer--

En utilisant l'onglet "Network".

### --question--

#### --text--

Quelle est la syntaxe correcte pour utiliser `try...catch` en JavaScript ?

#### --distractors--

`try { ... } else { ... }`

---

`try { ... } stack(catch) { ... }`

---

`try { ... } error { ... }`

#### --answer--

`try { ... } catch(error) { ... }`

### --question--

#### --text--

Lequel des éléments suivants est un exemple de `SyntaxError` ?

#### --distractors--

Utiliser une variable avant qu'elle ne soit déclarée.

---

Utiliser une variable deux fois dans la même portée.

---

Utiliser une variable qui est hors de portée.

#### --answer--

Oublier une virgule entre les éléments d'un tableau.

### --question--

#### --text--

Quel est le but des points d'arrêt dans le débogage JavaScript ?

#### --distractors--

Corriger automatiquement les erreurs du code.

---

Empêcher le code d'être exécuté.

---

Journaliser des messages dans la console.

#### --answer--

Mettre en pause l'exécution à un point précis.

### --question--

#### --text--

Que se passe-t-il quand l'instruction `throw` est exécutée ?

#### --distractors--

Le programme se termine immédiatement, en ignorant tout le code restant.

---

L'instruction throw journalise une erreur dans la console mais n'arrête pas l'exécution.

---

Elle met en pause l'exécution du code jusqu'à ce qu'elle soit reprise en désactivant les points d'arrêt.

#### --answer--

Une exception est levée et le bloc `catch` (s'il est présent) est exécuté.

### --question--

#### --text--

Quelle méthode utiliserais-tu pour inspecter des variables ou des expressions pendant le débogage dans les outils de développement ?

#### --distractors--

`inspect()`

---

`trace()`

---

`console.error()`

#### --answer--

`console.log()`

### --question--

#### --text--

Quel type d'erreur survient quand le code suivant est exécuté ?

```js
function example() {
  console.log(a);
  let a = 5;
}

example();
```

#### --distractors--

`FunctionError`

---

`SyntaxError`

---

Erreur de débordement de pile

#### --answer--

`ReferenceError`

### --question--

#### --text--

Quel outil est couramment utilisé pour avancer pas à pas dans le code ligne par ligne pendant le débogage ?

#### --distractors--

`console.log()`

---

`alert()`

---

L'instruction `throw`.

#### --answer--

Les points d'arrêt dans les outils de développement.

### --question--

#### --text--

Quel type d'erreur survient quand on tente d'accéder à une propriété de `undefined` ?

#### --distractors--

`SyntaxError`

---

`LogicalError`

---

`RangeError`

#### --answer--

`TypeError`

### --question--

#### --text--

Que fait le bloc `finally` dans une instruction `try...catch` ?

#### --distractors--

S'exécute seulement s'il y a une erreur.

---

Ignore l'exécution si `catch` est déclenché.

---

Quitte immédiatement le bloc de code.

#### --answer--

S'exécute toujours, qu'il y ait des erreurs ou non.

### --question--

#### --text--

Quel serait le résultat de l'exécution du code suivant ?

```js
const arr = [];
arr.length = -1;
```

#### --distractors--

Le code lèvera une `SyntaxError`.

---

Le code journalisera un tableau vide dans la console.

---

Le code lèvera une erreur de mémoire insuffisante.

#### --answer--

Le code lèvera une `RangeError`.

### --question--

#### --text--

Lequel des énoncés suivants est vrai concernant l'instruction `debugger` en JavaScript ?

#### --distractors--

L'instruction `debugger` est toujours ignorée par le navigateur.

---

L'instruction `debugger` envoie un rapport d'erreur au serveur.

---

L'instruction `debugger` recharge toujours la page et vide la console.

#### --answer--

L'instruction `debugger` est ignorée quand les outils de développement ne sont pas ouverts.

### --question--

#### --text--

Quel est le but principal du « profilage » en JavaScript ?

#### --distractors--

Écrire des tests unitaires pour des fonctions JavaScript et créer des profils d'exécution du code.

---

Mettre en pause l'exécution du code et libérer les ressources capturées comme la mémoire et le processeur.

---

Convertir le code JavaScript en code machine pour une exécution plus rapide.

#### --answer--

Identifier les goulots d'étranglement de performance en enregistrant l'utilisation du processeur, les appels de fonction et le temps d'exécution.

### --question--

#### --text--

Qu'est-ce qu'une « expression de surveillance » dans le contexte du débogage ?

#### --distractors--

Un outil pour mettre en pause l'exécution du code.

---

Une fonction intégrée pour journaliser les erreurs.

---

Une méthode JavaScript pour gérer les exceptions.

#### --answer--

Un outil pour surveiller la valeur des variables.

### --question--

#### --text--

Quand dois-tu utiliser `console.error()` au lieu de `console.log()` ?

#### --distractors--

Pour afficher des données tabulaires.

---

Pour journaliser des messages uniquement pendant le débogage.

---

Pour journaliser des informations sur le navigateur.

#### --answer--

Pour journaliser des messages d'erreur.

### --question--

#### --text--

Que fait le constructeur `Error()` en JavaScript ?

#### --distractors--

Journalise des messages d'erreur dans la console.

---

Exécute du code dans un bac à sable.

---

Termine le programme.

#### --answer--

Crée un nouvel objet d'erreur qui peut être levé.

### --question--

#### --text--

Quel est le but de la méthode `console.dir()` en JavaScript ?

#### --distractors--

Créer un répertoire séparé pour les fichiers de journalisation selon chaque type d'erreur.

---

Générer une représentation visuelle de la sortie de la console et l'enregistrer dans un fichier pdf.

---

Arrêter l'exécution du programme et afficher un message d'erreur lié à l'utilisation de la mémoire et du processeur.

#### --answer--

Produire une liste hiérarchique et interactive des propriétés d'un objet JavaScript spécifié.
