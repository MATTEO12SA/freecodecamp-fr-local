---
id: 66f1a2009e65c9a40a26d51e
title: Quiz sur les bases de React
challengeType: 8
dashedName: quiz-react-basics
---

# --description--

Pour réussir le quiz, tu dois répondre correctement à au moins 18 des 20 questions ci-dessous.

# --quizzes--

## --quiz--

### --question--

#### --text--

Lequel des éléments suivants n'est pas une bibliothèque ou un framework JavaScript ?

#### --distractors--

Angular

---

React

---

JQuery

#### --answer--

Laravel

### --question--

#### --text--

Lequel des éléments suivants N'EST PAS une méthode d'affichage conditionnel en React ?

#### --distractors--

Les opérateurs ternaires.

---

L'opérateur logique `&&`.

---

Les instructions `if`.

#### --answer--

`setTimeout`

### --question--

#### --text--

Quel est le but principal d'ajouter une prop `key` lors de l'affichage d'une liste en React ?

#### --distractors--

Appliquer un style CSS unique à chaque élément de la liste.

---

Servir de contenu textuel pour l'élément.

---

Ajouter un écouteur d'événements à chaque élément précis de la liste.

#### --answer--

Aider React à identifier les éléments pour un affichage efficace.

### --question--

#### --text--

Où les paquets et bibliothèques du projet sont-ils installés ?

#### --distractors--

Le répertoire utilisateur de ton ordinateur.

---

Le fichier `package.json`.

---

Le dossier `src`.

#### --answer--

Le dossier `node_modules`.

### --question--

#### --text--

Lequel des éléments suivants est VRAI à propos de JSX ?

#### --distractors--

JSX doit toujours être écrit dans des fichiers séparés de JavaScript.

---

JSX ne peut pas utiliser d'expressions JavaScript.

---

JSX n'est pas recommandé pour les applications React.

#### --answer--

JSX permet d'écrire du HTML dans du code JavaScript.

### --question--

#### --text--

Lequel des éléments suivants est un problème courant avec les SPA ?

#### --distractors--

Les ressources serveur sont plus sollicitées à cause du rendu constant des pages.

---

Le cache du navigateur devient peu fiable à cause des mises à jour fréquentes du DOM.

---

Les chargements multiples de pages créent une gestion d'état incohérente.

#### --answer--

Les lecteurs d'écran peinent avec le contenu mis à jour dynamiquement.

### --question--

#### --text--

Comment une application monopage met-elle typiquement la vue à jour ?

#### --distractors--

En faisant un rechargement serveur complet.

---

En rechargeant tout le document HTML.

---

En utilisant uniquement le rendu côté serveur.

#### --answer--

En mettant à jour seulement les parties nécessaires de la page.

### --question--

#### --text--

Laquelle des commandes suivantes démarre un projet React en mode développement ?

#### --distractors--

`npm run build`

---

`npm install`

---

`npm run develop`

#### --answer--

`npm run dev`

### --question--

#### --text--

Lequel des éléments suivants n'est pas un élément JSX valide ?

#### --distractors--

`<div className='class'></div>`

---

`<span>Hello World</span>`

---

`<button>Click Me</button>`

#### --answer--

`<img class='img'>`

### --question--

#### --text--

Quelle méthode de tableau JavaScript sert couramment à transformer un tableau de données en un tableau d'éléments JSX ?

#### --distractors--

`forEach()`

---

`filter()`

---

`reduce()`

#### --answer--

`map()`

### --question--

#### --text--

Lequel des éléments suivants est la meilleure pratique pour passer la prop `key` en React ?

#### --distractors--

Utiliser les indices de tableau dans tous les cas.

---

Générer des clés aléatoires à chaque rendu.

---

Omettre complètement la prop `key`.

#### --answer--

Utiliser des IDs uniques et stables issus des données.

### --question--

#### --text--

Quelle commande installe les dépendances listées dans `package.json` ?

#### --distractors--

`npm run`

---

`npm update`

---

`npm build`

#### --answer--

`npm install`

### --question--

#### --text--

Comment passerais-tu une prop `name` à un composant enfant ?

#### --distractors--

`<ChildComponent props="name" />`

---

`<ChildComponent {props} />`

---

`<ChildComponent {'John'} />`

#### --answer--

`<ChildComponent name="Vignesh" />`

### --question--

#### --text--

Quelle est la bonne façon de passer plusieurs props avec l'opérateur de décomposition en React ?

#### --distractors--

`<Child person={name..., age..., city...} />`.

---

`<Child spreader={...person} />`.

---

`<Child spread={person} />`.

#### --answer--

`<Child {...person} />`.

### --question--

#### --text--

Quelles sont les briques de base de React ?

#### --distractors--

Fonctions et modules.

---

Éléments DOM et événements.

---

Classes et objets.

#### --answer--

Composants réutilisables.

### --question--

#### --text--

Quelle est la bonne façon d'ajouter un style en ligne en React ?

#### --distractors--

`style={{ color: blue }}`

---

`style="color: blue"`

---

`style: { color: 'blue' }`

#### --answer--

`style={{ color: 'blue' }}`

### --question--

#### --text--

Quelle est la bonne façon d'afficher conditionnellement un composant React ?

#### --distractors--

Utiliser des instructions `if` à l'intérieur du JSX.

---

Déclarer un nouveau composant à l'intérieur d'une méthode `render()`.

---

Utiliser des boucles `while` à l'intérieur du JSX.

#### --answer--

Utiliser des opérateurs ternaires ou logiques `&&` dans le JSX.

### --question--

#### --text--

Quel mot-clé sert à amener un composant dans un autre fichier ?

#### --distractors--

`require`

---

`include`

---

`add`

#### --answer--

`import`

### --question--

#### --text--

Que signifie l'acronyme SPA en entier ?

#### --distractors--

Server Page Architectures.

---

Selective Page Applications.

---

Static Progressive Apps.

#### --answer--

Single-page Applications.

### --question--

#### --text--

Quelle extension de la syntaxe JavaScript sert à décrire l'UI en React ?

#### --distractors--

TSC

---

HTML+

---

ReactML

#### --answer--

JSX
