---
id: 66ed8ff4f45ce3ece4053eb4
title: Quiz Mises en page et effets CSS
challengeType: 8
dashedName: quiz-css-layout-and-effects
---

# --description--

Pour réussir le quiz, tu dois répondre correctement à au moins 18 des 20 questions ci-dessous.

# --quizzes--

## --quiz--

### --question--

#### --text--

Quel terme est utilisé en CSS pour décrire quand le contenu d'un élément dépasse la taille de son conteneur ?

#### --distractors--

Underflow

---

Compression

---

Padding

#### --answer--

Overflow

### --question--

#### --text--

Quel est le comportement par défaut du CSS quand le contenu déborde d'un élément ?

#### --distractors--

Cacher le contenu.

---

Couper le contenu.

---

Redimensionner le contenu.

#### --answer--

Garder le contenu visible.

### --question--

#### --text--

Pourquoi le CSS évite-t-il généralement de cacher le contenu en cas de débordement ?

#### --distractors--

Pour maintenir la mise en page et le design d'origine.

---

Pour empêcher les problèmes de performance dans le navigateur.

---

Pour permettre une meilleure optimisation pour les moteurs de recherche.

#### --answer--

Pour garantir que tout le contenu est accessible aux utilisateurs.

### --question--

#### --text--

Comment peux-tu gérer le débordement vertical en CSS en cachant le contenu ou en affichant des barres de défilement ?

#### --distractors--

Utiliser `overflow-y: visible` pour cacher le contenu et `overflow-y: auto` pour toujours afficher les barres de défilement.

---

Utiliser `overflow-y: collapse` pour cacher le contenu et `overflow-y: expand` pour afficher les barres de défilement.

---

Utiliser `overflow-y: none` pour cacher le contenu et `overflow-y: show` pour afficher les barres de défilement.

#### --answer--

Utiliser `overflow-y: hidden` pour cacher le contenu et `overflow-y: scroll` pour toujours afficher les barres de défilement.

### --question--

#### --text--

Comment peux-tu spécifier différents comportements de défilement horizontal et vertical en CSS ?

#### --distractors--

En utilisant les propriétés `scroll-x` et `scroll-y`.

---

En spécifiant la même valeur pour `overflow-x` et `overflow-y`.

---

En spécifiant deux valeurs pour la propriété `overflow`, où la première valeur s'applique à `overflow-y` et la seconde à `overflow-x`.

#### --answer--

En spécifiant deux valeurs pour la propriété `overflow`, où la première valeur s'applique à `overflow-x` et la seconde à `overflow-y`.

### --question--

#### --text--

Que te permet de faire la propriété CSS `transform` ?

#### --distractors--

Changer la couleur et le style de police d'un élément.

---

Ajuster la margin et le padding d'un élément.

---

Modifier la visibilité et l'affichage d'un élément.

#### --answer--

Faire pivoter, mettre à l'échelle, fausser et translater un élément.

### --question--

#### --text--

Comment la propriété `transform: translate(15px, 200px)` modifie-t-elle un élément ?

#### --distractors--

Elle déplace l'élément de 15 pixels vers la gauche et 200 pixels vers le bas de sa position d'origine.

---

Elle fait pivoter l'élément de 15 degrés dans le sens horaire.

---

Elle déplace l'élément de 15 pixels vers la droite et 200 pixels vers le haut de sa position d'origine.

#### --answer--

Elle déplace l'élément de 15 pixels vers la droite et 200 pixels vers le bas de sa position d'origine.

### --question--

#### --text--

Quel est le but des propriétés CSS `overflow-x` et `overflow-y` ?

#### --distractors--

Elles ajustent le padding et la margin d'un élément.

---

Elles définissent la couleur d'arrière-plan et le style de police d'un élément.

---

Elles définissent les propriétés de visibilité et d'affichage d'un élément.

#### --answer--

Elles contrôlent le débordement horizontal et vertical du contenu d'un élément.

### --question--

#### --text--

Quel est le but de la propriété `margin` en CSS ?

#### --distractors--

Elle définit l'espace entre le contenu et la bordure d'un élément.

---

Elle définit l'espace entre la bordure et le padding d'un élément.

---

Elle définit la largeur et la hauteur d'un élément.

#### --answer--

Elle définit l'espace autour d'un élément, à l'extérieur de sa bordure.

### --question--

#### --text--

Que représente la zone de contenu d'un élément en CSS ?

#### --distractors--

Elle définit les margins et le padding autour de l'élément.

---

Elle définit l'épaisseur de la bordure de l'élément.

---

Elle contrôle la visibilité de l'élément sur la page.

#### --answer--

Elle contient le contenu réel de l'élément, comme du texte, des images et des vidéos.

### --question--

#### --text--

Quelle est la différence entre `content-box` et `border-box` en CSS ?

#### --distractors--

`content-box` inclut padding et border, tandis que `border-box` inclut uniquement les dimensions du contenu.

---

`content-box` et `border-box` incluent tous deux uniquement les dimensions de la border.

---

`content-box` est utilisé pour les éléments 2D, tandis que `border-box` est utilisé pour les éléments 3D.

#### --answer--

`content-box` inclut uniquement les dimensions du contenu, tandis que `border-box` inclut le contenu, padding et border dans les dimensions.

### --question--

#### --text--

Quel est le but des CSS resets ?

#### --distractors--

Appliquer des styles par défaut à tous les éléments pour un chargement plus rapide.

---

Minimiser la taille des fichiers CSS en supprimant les styles inutilisés.

---

Optimiser les images et polices pour de meilleures performances.

#### --answer--

Supprimer le style par défaut du navigateur et garantir un style cohérent entre les navigateurs.

### --question--

#### --text--

Lequel des suivants est un exemple courant de CSS reset ?

#### --distractors--

bootstrap.css

---

undo.css

---

restore.css

#### --answer--

normalize.css

### --question--

#### --text--

Quelle propriété CSS est utilisée pour appliquer des changements comme la rotation, la mise à l'échelle et la translation aux éléments ?

#### --distractors--

`box-shadow`

---

`opacity`

---

`z-index`

#### --answer--

`transform`

### --question--

#### --text--

Comment la largeur totale d'un élément est-elle calculée en CSS quand on utilise la valeur `border-box` pour la propriété `box-sizing` ?

#### --distractors--

Largeur totale = Largeur du contenu + Border + Margin

---

Largeur totale = Largeur du contenu + Padding + Border + Margin

---

Largeur totale = Largeur du contenu

#### --answer--

Largeur totale = Largeur du contenu + Padding + Border

### --question--

#### --text--

Quel est le but de la propriété `filter` en CSS ?

#### --distractors--

Changer l'échelle et la rotation d'un élément.

---

Changer la couleur d'arrière-plan et la texture d'un élément.

---

Modifier la taille et la position d'un élément.

#### --answer--

Modifier l'apparence visuelle d'un élément en utilisant divers effets graphiques.

### --question--

#### --text--

Que fait la propriété CSS `filter: grayscale(50%);` à un élément ?

#### --distractors--

Elle augmente la luminosité de l'élément de 50%.

---

Elle diminue le contraste de l'élément de 50%.

---

Elle applique un effet sépia à l'élément.

#### --answer--

Elle applique un effet de niveaux de gris de 50% à l'élément.

### --question--

#### --text--

Quel est le but de la fonction `contrast()` en CSS ?

#### --distractors--

Elle ajuste la luminosité de l'image, où `0%` la rend noire et `100%` la rend blanche.

---

Elle ajuste le contraste de l'image, où `100%` la rend grise, `0%` n'a aucun effet, et des valeurs supérieures à `100%` augmentent le contraste.

---

Elle ajuste l'opacité de l'image, où `0%` la rend transparente et `100%` la rend opaque.

#### --answer--

Elle ajuste le contraste de l'image, où `0%` la rend grise, `100%` n'a aucun effet, et des valeurs supérieures à `100%` augmentent le contraste.

### --question--

#### --text--

Quel est le but de la fonction `hue-rotate()` en CSS ?

#### --distractors--

Elle change la luminosité d'une image.

---

Elle fait pivoter une image dans le sens horaire d'un certain nombre de degrés.

---

Elle ajuste la saturation et le contraste d'une image.

#### --answer--

Elle applique un décalage de couleur, où la valeur `angle` définit le nombre de degrés à faire pivoter autour du cercle des couleurs.

### --question--

#### --text--

Qu'est-ce que l'effondrement de margin en CSS ?

#### --distractors--

Quand la margin d'un élément est définie à un nombre négatif et cela cause des problèmes dans la mise en page.

---

Quand la margin d'un élément est diminuée par le padding et cela change les dimensions de l'élément.

---

Quand la margin d'un élément est ignorée par le navigateur, donc elle n'est pas visible.

#### --answer--

Quand les margins verticales d'éléments adjacents se chevauchent, donc elles sont combinées en une seule margin.

## --quiz--

### --question--

#### --text--

Lequel des suivants est VRAI à propos de la fonction `translate()` ?

#### --distractors--

Elle est utilisée pour mettre à l'échelle la taille d'un élément.

---

Elle est utilisée pour traduire le texte d'un élément.

---

Elle est utilisée pour changer la forme ou la taille d'un élément.

#### --answer--

Elle est utilisée pour déplacer un élément de sa position actuelle.

### --question--

#### --text--

Lequel des suivants est un composant du modèle de boîte CSS ?

#### --distractors--

Width

---

Height

---

Size

#### --answer--

Padding

### --question--

#### --text--

Quand le `box-sizing` d'un élément est défini à `content-box`, à quelle partie du modèle de boîte les `width` et `height` s'appliquent-elles ?

#### --distractors--

Contenu, padding, border et margin

---

Contenu et padding

---

Contenu, padding et border

#### --answer--

Contenu

### --question--

#### --text--

Quand le `box-sizing` d'un élément est défini à `border-box`, à quelles parties du modèle de boîte CSS les `width` et `height` ne s'appliquent PAS ?

#### --distractors--

La zone de contenu.

---

Padding

---

Border

#### --answer--

Margin

### --question--

#### --text--

Quelle valeur pour la fonction `brightness()` rend un élément complètement noir ?

#### --distractors--

`100%`

---

`0px`

---

`100px`

#### --answer--

`0%`

### --question--

#### --text--

Quel type de valeurs la fonction `hue-rotate()` utilise-t-elle ?

#### --distractors--

Pixels

---

Radians

---

Pourcentages

#### --answer--

Degrés

### --question--

#### --text--

Que fait la propriété CSS `filter: contrast(0%);` à un élément ?

#### --distractors--

Elle fera apparaître l'élément complètement blanc.

---

Elle fera apparaître l'élément complètement noir.

---

Elle n'aura aucun effet sur l'élément.

#### --answer--

Elle fera apparaître l'élément complètement gris.

### --question--

#### --text--

Quelle est la valeur de base de la fonction CSS `brightness()` à partir de laquelle l'augmentation de luminosité commence ?

#### --distractors--

`100px`

---

`50px`

---

`50%`

#### --answer--

`100%`

### --question--

#### --text--

Quel type de valeurs la fonction `grayscale()` utilise-t-elle ?

#### --distractors--

Pixels

---

Degrés

---

Radians

#### --answer--

Pourcentages

### --question--

#### --text--

Quel type de valeurs la fonction `contrast()` utilise-t-elle ?

#### --distractors--

Pixels

---

Degrés

---

Radians

#### --answer--

Pourcentages

### --question--

#### --text--

Que contrôle la propriété `overflow-x` en CSS ?

#### --distractors--

Elle détermine le débordement horizontal sur l'axe y.

---

Elle détermine le débordement vertical sur l'axe y.

---

Elle détermine le débordement vertical sur l'axe x.

#### --answer--

Elle détermine le débordement horizontal sur l'axe x.

### --question--

#### --text--

Que contrôle la propriété `overflow-y` en CSS ?

#### --distractors--

Elle détermine le débordement horizontal sur l'axe x.

---

Elle détermine le débordement vertical sur l'axe x.

---

Elle détermine le débordement horizontal sur l'axe y.

#### --answer--

Elle détermine le débordement vertical sur l'axe y.

### --question--

#### --text--

Quel est un exemple d'option tierce pour un CSS reset ?

#### --distractors--

clean.css

---

restore.css

---

reset.css

#### --answer--

sanitize.css

### --question--

#### --text--

Combien de dimensions y a-t-il dans la propriété overflow en CSS ?

#### --distractors--

Une

---

Trois

---

Quatre

#### --answer--

Deux

### --question--

#### --text--

Si la propriété raccourcie `overflow` reçoit deux valeurs, quelle propriété utilisera la première valeur ?

#### --distractors--

`overflow-y`

---

`overflow-w`

---

`overflow-z`

#### --answer--

`overflow-x`

### --question--

#### --text--

Quel type d'effet de flou la fonction `blur()` applique-t-elle à un élément en CSS ?

#### --distractors--

Giliani

---

Galilei

---

Galileo

#### --answer--

Gaussien

### --question--

#### --text--

Quel type de valeurs la fonction `sepia()` utilise-t-elle ?

#### --distractors--

Pixels

---

Radians

---

Degrés

#### --answer--

Pourcentages

### --question--

#### --text--

Si la propriété raccourcie `overflow` reçoit deux valeurs, quelle propriété utilisera la seconde valeur ?

#### --distractors--

`overflow-w`

---

`overflow-x`

---

`overflow-z`

#### --answer--

`overflow-y`

### --question--

#### --text--

Que fait un CSS reset ?

#### --distractors--

Il force les navigateurs à utiliser le mode sombre par défaut.

---

Il supprime les styles inline des éléments HTML.

---

Il désactive les extensions de navigateur qui incluent du CSS.

#### --answer--

Il supprime le style par défaut du navigateur.

### --question--

#### --text--

Que fait la propriété CSS `filter: grayscale(100%);` à un élément ?

#### --distractors--

Elle n'aura aucun effet sur l'élément.

---

Elle fera apparaître l'élément complètement noir.

---

Elle fera apparaître l'élément complètement blanc.

#### --answer--

Elle supprime la saturation des couleurs tout en maintenant la structure tonale de l'élément.
