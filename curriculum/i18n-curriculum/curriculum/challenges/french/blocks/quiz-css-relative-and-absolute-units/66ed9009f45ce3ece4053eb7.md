---
id: 66ed9009f45ce3ece4053eb7
title: Quiz sur les unités relatives et absolues CSS
challengeType: 8
dashedName: quiz-css-relative-and-absolute-units
---

# --description--

Pour réussir le quiz, tu dois répondre correctement à au moins 18 des 20 questions ci-dessous.

# --quizzes--

## --quiz--

### --question--

#### --text--

Qu'est-ce qu'une unité absolue ?

#### --distractors--

Des unités qui s'ajustent dynamiquement en fonction de la taille de l'écran.

---

Des unités qui dépendent de la taille de police de l'élément parent.

---

Des unités mesurées en pourcentages du viewport.

#### --answer--

Des unités de longueur fixes qui ne sont relatives à rien d'autre.

### --question--

#### --text--

Que couvre `100vw` en termes de largeur d'écran ?

#### --distractors--

`100%` de la largeur de l'écran.

---

`100%` de la hauteur du viewport.

---

`100%` de la largeur du parent.

#### --answer--

`100%` de la largeur du viewport.

### --question--

#### --text--

Lequel des suivants est une unité relative ?

#### --distractors--

`pt`

---

`cm`

---

`mm`

#### --answer--

`em`

### --question--

#### --text--

Si la taille de police racine est `20px`, à quoi est égal `1rem` ?

#### --distractors--

`18px`

---

`16px`

---

`10px`

#### --answer--

`20px`

### --question--

#### --text--

Que peut faire la fonction `calc()` en CSS ?

#### --distractors--

Modifier les couleurs dynamiquement.

---

Définir une largeur fixe.

---

Fonctionner uniquement avec des pourcentages.

#### --answer--

Additionner, soustraire, multiplier ou diviser différentes unités.

### --question--

#### --text--

Quelle unité est l'unité absolue la plus couramment utilisée en CSS ?

#### --distractors--

`inch`

---

`rem`

---

`%`

#### --answer--

`px`

### --question--

#### --text--

Si la hauteur du viewport est `800px`, à combien équivaut `50vh` ?

#### --distractors--

`50px`

---

`200px`

---

`100px`

#### --answer--

`400px`

### --question--

#### --text--

Que signifie définir la largeur d'un élément à `100%` ?

#### --distractors--

Il prend toute la largeur du document.

---

Il prend toute la largeur de l'écran.

---

Il prend `100px`.

#### --answer--

Il prend toute la largeur de son parent.

### --question--

#### --text--

Que se passe-t-il avec `calc(10px + 20%)` en CSS ?

#### --distractors--

Ça ajoute `10px` à `10%` de la largeur du document.

---

Ça vaut toujours `30px`.

---

Ça ajoute `10px` à `20%` de la largeur de l'écran.

#### --answer--

Ça ajoute `10px` à `20%` de la largeur du parent.

### --question--

#### --text--

Quelle unité est relative à la taille de police de l'élément racine ?

#### --distractors--

`px`

---

`pt`

---

`%`

#### --answer--

`rem`

### --question--

#### --text--

Avec une taille de police racine de `16px`, quelle est la taille de `10rem` ?

#### --distractors--

`140px`

---

`10px`

---

`100px`

#### --answer--

`160px`

### --question--

#### --text--

Pourquoi pourrais-tu utiliser des `points` au lieu de `pixels` dans le design ?

#### --distractors--

Les points sont évolutifs.

---

Les points sont meilleurs pour les mises en page responsives.

---

Les points sont plus faciles à gérer sur les écrans.

#### --answer--

Les points sont pour le design d'impression.

### --question--

#### --text--

Pourquoi les unités `rem` sont-elles préférées aux `pixels` en typographie ?

#### --distractors--

Parce qu'elles fournissent un dimensionnement fixe indépendamment de la résolution de l'écran.

---

Parce qu'elles garantissent la compatibilité avec les navigateurs plus anciens.

---

Parce qu'elles sont plus faciles à implémenter dans les designs responsives.

#### --answer--

Parce qu'elles s'adaptent proportionnellement avec les paramètres du navigateur de l'utilisateur.

### --question--

#### --text--

Combien d'espace écran prend `80vw` ?

#### --distractors--

`80%` de la largeur du parent.

---

`100%` de la hauteur du viewport.

---

`80px`

#### --answer--

`80%` de la largeur du viewport.

### --question--

#### --text--

Quelles unités devrais-tu utiliser si tu veux qu'un élément occupe une certaine largeur ou hauteur par pourcentage de l'écran ?

#### --distractors--

`pt` et `px`

---

`rem` et `em`

---

`pt` et `pc`

#### --answer--

`vw` et `vh`

### --question--

#### --text--

Que signifie `vw` ?

#### --distractors--

Vertical Width.

---

Virtual Width.

---

Variable Width.

#### --answer--

Viewport Width.

### --question--

#### --text--

Quelle est la différence clé entre `rem` et `em` en CSS ?

#### --distractors--

`em` est relatif à l'élément racine, tandis que `rem` est relatif à l'élément parent.

---

`rem` est relatif à la largeur du bureau, tandis que `em` est relatif à la largeur mobile.

---

`rem` est relatif à la largeur définie par l'utilisateur, tandis que `em` est relatif à la largeur du navigateur.

#### --answer--

`em` est relatif à la taille de police de l'élément parent, tandis que `rem` est relatif à l'élément racine.

### --question--

#### --text--

Qu'est-ce qu'une unité `vh` ?

#### --distractors--

Une unité basée sur la largeur du viewport.

---

Une unité basée sur la hauteur de l'élément.

---

Une unité qui s'ajuste en fonction de la taille de police.

#### --answer--

Une unité égale à 1% de la hauteur du viewport.

### --question--

#### --text--

Que fait définir la largeur d'un élément à `auto` en CSS ?

#### --distractors--

Ça définit la largeur à `100%` de l'élément parent par défaut.

---

Ça définit la largeur à `50%` du viewport sauf si d'autres styles le remplacent.

---

Ça définit la largeur à `0px` si aucun contenu ou padding n'est présent.

#### --answer--

Ça permet au navigateur de déterminer la largeur en fonction du contenu et du conteneur.

### --question--

#### --text--

Quel est un avantage clé de la fonction `calc()` ?

#### --distractors--

Elle minifie automatiquement tes fichiers CSS pour de meilleures performances.

---

Elle permet l'utilisation de variables dans le CSS sans aucun préprocesseur.

---

Elle permet d'intégrer des expressions JavaScript directement dans les règles CSS.

#### --answer--

Elle te permet de déterminer la valeur d'une propriété CSS dynamiquement en fonction de différents aspects de l'application ou du viewport.
