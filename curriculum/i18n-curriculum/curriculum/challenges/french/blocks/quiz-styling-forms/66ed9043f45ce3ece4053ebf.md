---
id: 66ed9043f45ce3ece4053ebf
title: Quiz sur la mise en forme des formulaires
challengeType: 8
dashedName: quiz-styling-forms
---

# --description--

Pour réussir le quiz, tu dois répondre correctement à au moins 9 des 10 questions ci-dessous.

# --quizzes--

## --quiz--

### --question--

#### --text--

Quel est un problème courant pour styliser le type d'entrée `datetime-local` ?

#### --distractors--

Il ne fonctionne pas avec la propriété `float`.

---

Il est impossible de le rendre responsive.

---

Il ne fonctionne pas avec les unités `rem`.

#### --answer--

Le format d'entrée est différent selon les navigateurs.

### --question--

#### --text--

Lequel des suivants est considéré comme une bonne pratique pour styliser les entrées de texte ?

#### --distractors--

Les tailles de police des entrées de texte doivent être définies avec des unités `em` et les couleurs doivent être d'un gris clair.

---

Les tailles d'entrée de texte ne devraient utiliser que des bordures vert foncé au focus et la couleur du texte devrait être gris foncé.

---

Les couleurs de police des entrées de texte doivent être des couleurs claires et la taille ne devrait être définie qu'en utilisant l'unité `px`.

#### --answer--

Les polices des entrées de texte doivent être de taille adéquate, et la couleur doit avoir un contraste suffisant avec l'arrière-plan.

### --question--

#### --text--

Comment le style de l'état d'erreur devrait-il se rapporter au style de l'état de focus ?

#### --distractors--

Ils devraient être identiques pour la cohérence.

---

L'état d'erreur devrait être moins visible pour éviter d'alarmer les utilisateurs.

---

L'état d'erreur devrait être animé tandis que l'état de focus devrait être statique.

#### --answer--

L'état d'erreur devrait être distinct de l'état de focus.

### --question--

#### --text--

Quelle est une bonne pratique pour styliser les entrées dans un état focalisé ?

#### --distractors--

Il est important d'utiliser uniquement des bordures gris clair pour les entrées focalisées.

---

Il est important d'utiliser uniquement des bordures rouge foncé pour les entrées focalisées.

---

Il est important de supprimer tout indicateur visible.

#### --answer--

Il est important de préserver un indicateur visible.

### --question--

#### --text--

Quelle est une bonne pratique pour styliser les entrées dans un état d'erreur ?

#### --distractors--

Augmenter la taille de police du texte d'entrée à `3rem`.

---

Définir l'entrée à `display: none;` pour qu'elle soit supprimée de la page.

---

Définir l'entrée à désactivée pour que les utilisateurs ne puissent plus interagir avec.

#### --answer--

Fournir un indicateur visuel avec un message pour que les utilisateurs sachent que quelque chose est incorrect et doit être corrigé.

### --question--

#### --text--

Pourquoi est-il important d'utiliser un texte placeholder descriptif dans les entrées de formulaire ?

#### --distractors--

Cela aide à pré-remplir le formulaire pour l'utilisateur.

---

Cela valide automatiquement l'entrée pendant que l'utilisateur tape.

---

Cela cache le label de l'entrée, réduisant l'encombrement visuel.

#### --answer--

Cela fournit aux utilisateurs des conseils sur le format d'entrée attendu.

### --question--

#### --text--

Qu'est-ce que WebKit ?

#### --distractors--

C'est un moteur de navigateur qui s'assure qu'il n'y a pas d'erreurs de validation dans ton CSS.

---

C'est une propriété CSS spéciale utilisée dans CSS Grid.

---

C'est une propriété CSS spéciale utilisée dans CSS flexbox.

#### --answer--

C'est un moteur de navigateur qui affiche les pages web.

### --question--

#### --text--

Quand est-il approprié d'utiliser `appearance: none;` pour les éléments de formulaire ?

#### --distractors--

Pour supprimer complètement les éléments de formulaire du DOM et de la page.

---

Pour cacher les éléments de formulaire à ceux qui utilisent des technologies d'assistance comme les lecteurs d'écran.

---

Pour supprimer uniquement les couleurs par défaut appliquées aux éléments de formulaire et utiliser les tiennes.

#### --answer--

Pour supprimer certains des styles par défaut appliqués aux éléments de formulaire.

### --question--

#### --text--

Quelles sont quelques considérations quand on travaille avec `appearance: none;` sur les éléments de formulaire ?

#### --distractors--

Il est important de préserver les indicateurs survolés et de soumission sur les éléments interactifs.

---

Il est important d'utiliser `appearance: none;` uniquement quand tu veux appliquer différents styles pour le navigateur Safari.

---

Il est important d'utiliser `appearance: none;` quand tu dois supprimer complètement un élément de formulaire de la page.

#### --answer--

Il est important de préserver les indicateurs de focus et d'erreur sur les éléments interactifs.

### --question--

#### --text--

Quel type d'éléments de formulaire est couramment stylisé en utilisant `appearance: none;` pour supprimer leur apparence par défaut ?

#### --distractors--

Les éléments `label`.

---

Les éléments `div`.

---

Les éléments `span`.

#### --answer--

Les éléments `input`.
