---
id: 66edd3b3096349f06cf688bb
title: Quiz sur l'audio et la vidéo en JavaScript
challengeType: 8
dashedName: quiz-javascript-audio-and-video
---

# --description--

Pour réussir le quiz, tu dois répondre correctement à au moins 18 des 20 questions ci-dessous.

# --quizzes--

## --quiz--

### --question--

#### --text--

Quel est le but du constructeur `Audio()` en JavaScript ?

#### --distractors--

Créer un nouveau fichier audio.

---

Lire un fichier audio existant.

---

Modifier des fichiers audio.

#### --answer--

Créer un nouvel élément `audio`.

### --question--

#### --text--

Quelle méthode est utilisée pour démarrer la lecture d'un élément `audio` ou `video` ?

#### --distractors--

`start()`

---

`begin()`

---

`resume()`

#### --answer--

`play()`

### --question--

#### --text--

Quelle est la différence entre les méthodes `pause()` et `play()` pour les éléments média ?

#### --distractors--

`pause()` renvoie une Promise tandis que `play()` n'en renvoie pas.

---

`pause()` ne peut pas être reprise tandis que `play()` peut démarrer depuis n'importe quelle position.

---

`pause()` revient automatiquement au début tandis que `play()` démarre là où l'audio a été mis en pause.

#### --answer--

`pause()` arrête la lecture mais conserve la position actuelle tandis que `play()` démarre la lecture.

### --question--

#### --text--

Quel format audio a le support navigateur le plus large ?

#### --distractors--

`FLAC`

---

`OGG`

---

`WAV`

#### --answer--

`MP3`

### --question--

#### --text--

Quel est le but de l'élément `source` à l'intérieur d'un élément `audio` ou `video` ?

#### --distractors--

Spécifier le volume du média.

---

Ajouter des sous-titres au média.

---

Lier un processeur audio externe.

#### --answer--

Spécifier plusieurs sources média pour différents formats.

### --question--

#### --text--

Quels sont les deux types MIME possibles qu'un format MP4 peut avoir ?

#### --distractors--

`mp4/audio` and `mp4/video`

---

`audio/mpeg4` and `video/mpeg4`

---

`mp4/mpeg` and `mp4/stream`

#### --answer--

`audio/mp4` and `video/mp4`

### --question--

#### --text--

Que fait un codec dans le contexte de l'audio et de la vidéo ?

#### --distractors--

Augmente le volume du média.

---

Change le format de fichier du média.

---

Chiffre le média pour une transmission sécurisée.

#### --answer--

Convertit le média entre formats analogiques et numériques.

### --question--

#### --text--

Quelle API fournit un moyen de créer, traiter et analyser de l'audio dans les applications web ?

#### --distractors--

Audio Processing API.

---

Sound Manipulation API.

---

Media Stream API.

#### --answer--

Web Audio API.

### --question--

#### --text--

Quel est le but de la méthode `getUserMedia()` ?

#### --distractors--

Authentifier les utilisateurs auprès d'un service média.

---

Récupérer les préférences de l'utilisateur pour la lecture média.

---

Télécharger des fichiers média sur l'appareil de l'utilisateur.

#### --answer--

Accéder à la caméra et/ou au microphone de l'utilisateur.

### --question--

#### --text--

Quelle propriété de l'interface `HTMLMediaElement` représente le temps de lecture actuel ?

#### --distractors--

`time`

---

`playbackPosition`

---

`currentPosition`

#### --answer--

`currentTime`

### --question--

#### --text--

Que fait l'extrait de code suivant ?

```javascript
const audio = new Audio('song.mp3');
audio.loop = true;
```

#### --distractors--

Lit le fichier audio une seule fois.

---

Augmente le volume de l'audio.

---

Met l'audio en pause après une boucle.

#### --answer--

Configure l'audio pour qu'il se répète indéfiniment.

### --question--

#### --text--

Que signifie MIME en toutes lettres ?

#### --distractors--

Multiple Internet Message Extensions

---

Mail Internet Multimedia Extensions

---

Multimedia Internet Mail Extensions

#### --answer--

Multipurpose Internet Mail Extensions

### --question--

#### --text--

À quoi t'aide la méthode `canPlayType()` ?

#### --distractors--

Vérifier si de l'audio est en cours de lecture dans le navigateur dans un format spécifique.

---

Vérifier si un fichier média existe dans le cache du navigateur et est accessible pour la lecture.

---

Tester si un élément audio a les contrôles activés et si l'utilisateur a les permissions appropriées pour interagir avec lui.

#### --answer--

Déterminer si un navigateur est susceptible de pouvoir lire ton format audio spécifique.

### --question--

#### --text--

Lequel des éléments suivants N'EST PAS un événement valide de l'API `HTMLMediaElement` ?

#### --distractors--

`waiting`

---

`canplay`

---

`ended`

#### --answer--

`stopped`

### --question--

#### --text--

Quel événement de `HTMLMediaElement` se déclenche quand la lecture est mise en pause à cause de la mise en mémoire tampon des données ?

#### --distractors--

`stopped`

---

`pause`

---

`buffering`

#### --answer--

`waiting`

### --question--

#### --text--

Quelle interface de la Web Audio API est utilisée pour représenter un graphe de traitement audio ?

#### --distractors--

`AudioNode`

---

`AudioBuffer`

---

`AudioListener`

#### --answer--

`AudioContext`

### --question--

#### --text--

Quel est le but de l'API Media Source Extensions ?

#### --distractors--

Ajouter des effets visuels à la vidéo.

---

Convertir entre différents formats média.

---

Permettre à JavaScript de synchroniser plusieurs pistes audio.

#### --answer--

Elle te permet de passer directement le flux de la webcam d'un utilisateur à un élément vidéo

### --question--

#### --text--

Quand l'événement `ended` est-il déclenché ?

#### --distractors--

Quand la source du média est changée.

---

Quand la méthode `pause()` est appelée.

---

Quand le média est arrêté manuellement.

#### --answer--

Quand la fin du média a été atteinte.

### --question--

#### --text--

Comment l'API Screen Capture est-elle exposée ?

#### --distractors--

Via l'interface `window.screenCapture`

---

En utilisant la propriété `navigator.capture`

---

En utilisant la méthode `captureScreen()`

#### --answer--

En appelant la méthode `getDisplayMedia()`.

### --question--

#### --text--

À quoi est associé le format WMV ?

#### --distractors--

Wide Media Video

---

World Media Video

---

Web Media Player

#### --answer--

Windows Media Player
