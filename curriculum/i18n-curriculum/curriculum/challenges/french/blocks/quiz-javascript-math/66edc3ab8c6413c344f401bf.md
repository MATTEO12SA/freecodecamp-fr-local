---
id: 66edc3ab8c6413c344f401bf
title: Quiz sur les mathématiques JavaScript
challengeType: 8
dashedName: quiz-javascript-math
---

# --description--

Pour réussir le quiz, tu dois répondre correctement à au moins 18 des 20 questions ci-dessous.

# --quizzes--

## --quiz--

### --question--

#### --text--

Que signifie `NaN` ?

#### --distractors--

`Numeric and Number`

---

`Not a Null`

---

`Null and Number`

#### --answer--

`Not a Number`

### --question--

#### --text--

Lequel des suivants n'est PAS un opérateur arithmétique en JavaScript ?

#### --distractors--

`*`

---

`+`

---

`-`

#### --answer--

`??`

### --question--

#### --text--

Que se passe-t-il si tu essaies de diviser par zéro en JavaScript ?

#### --distractors--

JavaScript affichera 0.

---

Rien ne se passera.

---

JavaScript déclenchera une erreur.

#### --answer--

JavaScript affichera `Infinity`

### --question--

#### --text--

Laquelle des propositions suivantes est l'utilisation correcte de l'opérateur d'exponentiation ?

#### --distractors--

`2*3`

---

`2#3`

---

`2%3`

#### --answer--

`2**3`

### --question--

#### --text--

Quel opérateur devrais-tu utiliser pour décrémenter une variable en JavaScript ?

#### --distractors--

`++`

---

`//`

---

`**`

#### --answer--

`--`

### --question--

#### --text--

Lequel des suivants n'est PAS un opérateur bit à bit ?

#### --distractors--

`&`

---

`|`

---

`^`

#### --answer--

`>`

### --question--

#### --text--

Lequel des suivants est le bon opérateur composé pour obtenir le reste ?

#### --distractors--

`//=`

---

`\=`

---

`%%=`

#### --answer--

`%=`

### --question--

#### --text--

Quelle est la différence entre les opérateurs `==` et `===` ?

#### --distractors--

Il n'y a pas de différence. La comparaison aura toujours le même résultat.

---

L'opérateur `==` est utilisé pour vérifier si le type de données est le même, tandis que `===` est utilisé pour vérifier si la valeur est égale.

---

L'opérateur `==` ne peut être utilisé qu'avec les types primitifs, tandis que l'opérateur `===` n'est utilisé que pour les objets.

#### --answer--

L'opérateur `==` ne compare que les valeurs, tandis que l'opérateur `===` compare les valeurs et les types.

### --question--

#### --text--

À quoi sert l'opérateur plus unaire (`+`) ?

#### --distractors--

Il est utilisé pour décrémenter une valeur de un.

---

Il est utilisé pour obtenir le reste d'une opération.

---

Il est utilisé pour inverser la représentation binaire d'un nombre.

#### --answer--

Il est utilisé pour convertir son opérande en nombre.

### --question--

#### --text--

Laquelle des méthodes `Math` suivantes arrondit une valeur à l'entier le plus proche ?

#### --distractors--

`Math.trunc()`

---

`Math.max()`

---

`Math.min()`

#### --answer--

`Math.round()`

### --question--

#### --text--

Que fait la méthode `Math.sqrt()` ?

#### --distractors--

Cette méthode renvoie un nombre à virgule flottante aléatoire.

---

Cette méthode renvoie la valeur absolue d'un nombre.

---

Cette méthode renvoie le cube d'un nombre.

#### --answer--

Cette méthode renvoie la racine carrée d'un nombre.

### --question--

#### --text--

Laquelle des propositions suivantes n'est PAS une méthode valide de l'objet `Math` ?

#### --distractors--

`Math.ceil()`

---

`Math.pow()`

---

`Math.abs()`

#### --answer--

`Math.extend()`

### --question--

#### --text--

Laquelle des propositions suivantes renverra `false` ?

#### --distractors--

`isNaN("Matt")`

---

`isNaN()`

---

`isNaN(undefined)`

#### --answer--

`isNaN(7)`

### --question--

#### --text--

Que fait la méthode `parseFloat()` ?

#### --distractors--

Elle analyse un argument de type chaîne et effectue une opération bit à bit dessus.

---

Elle analyse un argument de type chaîne et renvoie `false`.

---

Elle analyse un argument de type chaîne et renvoie un entier.

#### --answer--

Elle analyse un argument de type chaîne et renvoie un nombre à virgule flottante.

### --question--

#### --text--

Qu'est-ce qui sera affiché dans la console ?

```js
let num = 5;
console.log(++num);
```

#### --distractors--

`error`

---

`null`

---

`5`

#### --answer--

`6`

### --question--

#### --text--

Quelle serait la sortie du code suivant ?

```js
console.log(5 == '5');
```

#### --distractors--

`undefined`

---

`false`

---

`null`

#### --answer--

`true`

### --question--

#### --text--

Qu'est-ce que la précédence des opérateurs ?

#### --distractors--

La précédence des opérateurs est utilisée pour vérifier si la valeur de gauche est inférieure à celle de droite.

---

La précédence des opérateurs est utilisée pour élever une variable à la puissance du nombre spécifié et réaffecter le résultat à la variable.

---

La précédence des opérateurs est utilisée pour augmenter la valeur de un.

#### --answer--

La précédence des opérateurs détermine l'ordre dans lequel les opérations sont évaluées dans une expression.

### --question--

#### --text--

Lequel des suivants n'est PAS un opérateur de comparaison ?

#### --distractors--

`>`

---

`>=`

---

`<`

#### --answer--

`!>`

### --question--

#### --text--

Que fait l'opérateur de négation unaire (`-`) ?

#### --distractors--

Il inverse la valeur booléenne de son opérande.

---

Il diminue la valeur de un.

---

Il augmente la valeur de un.

#### --answer--

Il inverse le signe de l'opérande.

### --question--

#### --text--

Laquelle des propositions suivantes est un exemple d'utilisation de l'opérateur de coalescence des nuls ?

#### --distractors--

```js
const userSettings = {
 theme: null,
 volume: 0,
 notifications: false,
};

userSettings.theme () 'light';
```

---

```js
const userSettings = {
 theme: null,
 volume: 0,
 notifications: false,
};

userSettings.theme ** 'light';
```

---

```js
const userSettings = {
 theme: null,
 volume: 0,
 notifications: false,
};

userSettings.theme \\ 'light';
```

#### --answer--

```js
const userSettings = {
 theme: null,
 volume: 0,
 notifications: false,
};

userSettings.theme ?? 'light';
```
