---
id: 67f41268a129e63f8e071736
title: Quiz sur les boucles et les séquences
challengeType: 8
dashedName: quiz-loops-and-sequences
---

# --description--

Pour réussir le quiz, tu dois répondre correctement à au moins 18 des 20 questions ci-dessous.

# --quizzes--

## --quiz--

### --question--

#### --text--

Laquelle de ces options est la syntaxe correcte pour une liste ?

#### --distractors--

```py
cities = //'Los Angeles', 'London', 'Tokyo'\\
```

---

```py
cities = {'Los Angeles', 'London', 'Tokyo'}
```

---

```py
cities = <'Los Angeles', 'London', 'Tokyo'>
```

#### --answer--

```py
cities = ['Los Angeles', 'London', 'Tokyo']
```

### --question--

#### --text--

Qu'est-ce qui s'affichera dans la console ?

```py
cities = ['Los Angeles', 'London', 'Tokyo']
print(cities[-1])
```

#### --distractors--

`'None'`

---

`'Los Angeles'`

---

`'London'`

#### --answer--

`'Tokyo'`

### --question--

#### --text--

Qu'est-ce qu'un itérable ?

#### --distractors--

C'est un type spécial de chaîne qui peut contenir un grand nombre de caractères.

---

C'est un type spécial de nombre utilisé pour représenter des valeurs vraiment très grandes.

---

C'est un type de données spécial surtout utilisé dans les classes et les fonctions spéciales.

#### --answer--

C'est un type spécial d'objet que tu peux parcourir en boucle, un élément à la fois.

### --question--

#### --text--

Qu'affichera ce qui suit dans la console ?

```py
developer = 'Jessica'

print(list(developer))
```

#### --distractors--

`'J', 'e', 's', 's', 'i', 'c', 'a'`

---

`{'J', 'e', 's', 's', 'i', 'c', 'a'}`

---

`<'J', 'e', 's', 's', 'i', 'c', 'a'>`

#### --answer--

`['J', 'e', 's', 's', 'i', 'c', 'a']`

### --question--

#### --text--

Laquelle de ces options sert à obtenir le nombre d'éléments d'une liste ?

#### --distractors--

`list()`

---

`long()`

---

`length()`

#### --answer--

`len()`

### --question--

#### --text--

Que se passe-t-il si tu essaies de passer un indice (positif ou négatif) hors des limites d'une liste ?

#### --distractors--

Tu obtiendras une `SyntaxError`.

---

Tu obtiendras une `RangeError`.

---

Tu obtiendras une `TypeError`.

#### --answer--

Tu obtiendras une `IndexError`.

### --question--

#### --text--

Qu'est-ce qui s'affichera dans la console ?

```py
desserts = ['Cake', 'Cookies', 'Ice Cream', 'Pie']
print(desserts[1:3])
```

#### --distractors--

`['Cake', 'Cookies', 'Ice Cream', 'Pie']`

---

`['Cake', 'Cookies']`

---

`['Cake', 'Cookies', 'Cookies', 'Ice Cream']`

#### --answer--

`['Cookies', 'Ice Cream']`

### --question--

#### --text--

Laquelle de ces méthodes sert à ajouter un élément à la fin d'une liste ?

#### --distractors--

`add()`

---

`pop()`

---

`push()`

#### --answer--

`append()`

### --question--

#### --text--

Qu'est-ce qui s'affichera dans la console ?

```py
numbers = [1, 2, 3, 4, 5]
even_numbers = [6, 8, 10]

numbers.extend(even_numbers)
print(numbers)
```

#### --distractors--

`[2, 3, 4, 5, 6, 8, 10]`

---

`[5, 6, 8, 10]`

---

`[1, 2, 3, 4]`

#### --answer--

`[1, 2, 3, 4, 5, 6, 8, 10]`

### --question--

#### --text--

Laquelle de ces méthodes sert à insérer un élément à un indice précis dans une liste ?

#### --distractors--

`push()`

---

`add()`

---

`place()`

#### --answer--

`insert()`

### --question--

#### --text--

Qu'est-ce qui s'affichera dans la console ?

```py
numbers = [1, 2, 3, 4, 5, 5, 5]
numbers.remove(5)

print(numbers)
```

#### --distractors--

`[1, 2, 3, 4]`

---

`[1, 2, 3, 4, 5, 5, 5]`

---

`[1, 2, 3, 4, 5]`

#### --answer--

`[1, 2, 3, 4, 5, 5]`

### --question--

#### --text--

Laquelle de ces méthodes sert à vider une liste ?

#### --distractors--

`pop()`

---

`empty()`

---

`remove()`

#### --answer--

`clear()`

### --question--

#### --text--

Qu'est-ce qu'un tuple ?

#### --distractors--

C'est un type de données mutable constitué d'une séquence aléatoire de valeurs.

---

C'est un type de données spécial utilisé pour représenter de grands nombres.

---

C'est une méthode qui sert à ajouter des éléments à une liste.

#### --answer--

C'est une séquence ordonnée immuable de valeurs.

### --question--

#### --text--

Laquelle de ces options est la bonne façon de déballer les éléments d'un tuple ?

#### --distractors--

```py
developer = ('Alice', 34, 'Rust Developer')
(name, age, job) <= developer
```

---

```py
developer = ('Alice', 34, 'Rust Developer')
<<name, age, job>> = developer
```

---

```py
developer = ('Alice', 34, 'Rust Developer')
name, age, job == developer
```

#### --answer--

```py
developer = ('Alice', 34, 'Rust Developer')
name, age, job = developer
```

### --question--

#### --text--

Quel sera le résultat du code suivant ?

```py
developer = ('Jane Doe', 23, 'Python Developer')
del developer[1]
```

#### --distractors--

Cela produira une `RangeError`.

---

Rien ne se passera.

---

Cela créera une boucle infinie.

#### --answer--

Cela produira une `TypeError`.

### --question--

#### --text--

Quel sera le résultat du code suivant ?

```py
programming_languages = ('Rust', 'Java', 'Python', 'C++', 'Rust', 'Python')
programming_languages.index('Python', 3)
```

#### --distractors--

`2`

---

`6`

---

`0`

#### --answer--

`5`

### --question--

#### --text--

Laquelle de ces fonctions sert à créer une nouvelle liste des valeurs triées ?

#### --distractors--

`has_sort()`

---

`sort()`

---

`is_sorted()`

#### --answer--

`sorted()`

### --question--

#### --text--

Laquelle de ces options est la bonne façon d'utiliser une boucle `for` ?

#### --distractors--

```py
programming_languages = ['Rust', 'Java', 'Python', 'C++']

for language in programming_languages [
    print(language)
]
```

---

```py
programming_languages = ['Rust', 'Java', 'Python', 'C++']

for language of programming_languages:
    print(language)
```

---

```py
programming_languages = ['Rust', 'Java', 'Python', 'C++']

for language in programming_languages {
    print(language)
}
```

#### --answer--

```py
programming_languages = ['Rust', 'Java', 'Python', 'C++']

for language in programming_languages:
    print(language)
```

### --question--

#### --text--

Laquelle de ces fonctions sert à générer une séquence d'entiers ?

#### --distractors--

`for()`

---

`int()`

---

`sequence()`

#### --answer--

`range()`

### --question--

#### --text--

Laquelle de ces fonctions sert à parcourir plusieurs itérables en parallèle ?

#### --distractors--

`map()`

---

`filter()`

---

`reduce()`

#### --answer--

`zip()`
