---
id: 67358be1c7903489c0a7db78
title: Quiz JavaScript Maps et Sets
challengeType: 8
dashedName: quiz-javascript-maps-and-sets
---

# --description--

Pour réussir le quiz, tu dois répondre correctement à au moins 18 des 20 questions ci-dessous.

# --quizzes--

## --quiz--

### --question--

#### --text--

Qu'est-ce qu'un Set en JavaScript ?

#### --distractors--

Une structure de données qui autorise les valeurs en double.

---

Une structure de données qui ne stocke que des chaînes et des nombres.

---

Une collection qui n'autorise pas la suppression de ses éléments.

#### --answer--

Une collection dans laquelle une valeur ne peut apparaître qu'une seule fois.

### --question--

#### --text--

Laquelle des méthodes suivantes est disponible dans les Maps mais pas dans les Sets ?

#### --distractors--

`has()`

---

`clear()`

---

`values()`

#### --answer--

`set()`

### --question--

#### --text--

Quelle méthode est utilisée pour ajouter des éléments à un Set ?

#### --distractors--

`.push()`

---

`.append()`

---

`.insert()`

#### --answer--

`.add()`

### --question--

#### --text--

Comment vérifies-tu si un Set contient une certaine valeur ?

#### --distractors--

`.contains()`

---

`.hasValue()`

---

`.find()`

#### --answer--

`.has()`

### --question--

#### --text--

Lequel de ces cas d'usage convient particulièrement bien aux Sets ?

#### --distractors--

Gérer des paires clé-valeur dans une base de données.

---

Créer des listes ordonnées d'éléments.

---

Stocker des structures de données hiérarchiques dans le local storage du navigateur.

#### --answer--

Éliminer les doublons d'un tableau.

### --question--

#### --text--

Qu'est-ce qu'un WeakSet en JavaScript ?

#### --distractors--

Une collection de type Set qui n'autorise que les types de données primitifs.

---

Une collection de type Set qui autorise les valeurs en double.

---

Une collection de type Set qui empêche de retirer des valeurs.

#### --answer--

Une collection de type Set qui ne stocke que des objets.

### --question--

#### --text--

Quelle est une différence entre un Set et un WeakSet ?

#### --distractors--

Un WeakSet peut stocker n'importe quel type de valeur, y compris des nombres.

---

Un Set n'autorise pas les valeurs en double, mais un WeakSet si.

---

Un Set autorise le ramasse-miettes, alors qu'un WeakSet ne l'autorise pas.

#### --answer--

Un Set peut contenir n'importe quel type de valeur, tandis qu'un WeakSet ne contient que des objets.

### --question--

#### --text--

Qu'est-ce qu'un Map en JavaScript ?

#### --distractors--

Une collection qui n'autorise que des valeurs uniques.

---

Une collection qui ne peut utiliser que des chaînes comme clés.

---

Une collection qui n'autorise pas la suppression de ses éléments.

#### --answer--

Une collection de paires clé-valeur de n'importe quel type.

### --question--

#### --text--

Quelle méthode est utilisée pour insérer des paires clé-valeur dans un Map ?

#### --distractors--

`.append()`

---

`.add()`

---

`.insert()`

#### --answer--

`.set()`

### --question--

#### --text--

Quelle sera la sortie du code suivant ?

```js
const myMap = new Map();
myMap.set('a', 1);
myMap.set('a', 2);
console.log(myMap.get('a'));
```

#### --distractors--

`1`

---

`undefined`

---

`null`

#### --answer--

`2`

### --question--

#### --text--

En quoi un Map diffère-t-il d'un Object en JavaScript ?

#### --distractors--

Un Map n'autorise que des nombres comme clés.

---

Les Objects autorisent n'importe quel type comme clés, tandis que les Maps n'autorisent que des chaînes.

---

Les Maps ne prennent pas en charge l'itération, tandis que les Objects si.

#### --answer--

Les Maps autorisent n'importe quel type de clé, mais les Objects n'autorisent que les chaînes et les symboles.

### --question--

#### --text--

Comment peux-tu retirer une paire clé-valeur d'un Map ?

#### --distractors--

`.remove()`

---

`.deleteKey()`

---

`.erase()`

#### --answer--

`.delete()`

### --question--

#### --text--

Que renvoie la propriété `size` d'un Map ?

#### --distractors--

Le nombre d'indices dans le Map.

---

Le nombre de valeurs uniques dans le Map.

---

La mémoire totale utilisée par le Map.

#### --answer--

Le nombre de paires clé-valeur dans le Map.

### --question--

#### --text--

Qu'est-ce qu'un WeakMap en JavaScript ?

#### --distractors--

Une collection de type Map qui autorise des valeurs primitives comme clés.

---

Une collection de type Map qui empêche la suppression des éléments.

---

Une collection de type Map qui trie automatiquement ses clés.

#### --answer--

Une collection de type Map avec des références faibles aux clés.

### --question--

#### --text--

Quelle est une différence entre un Map et un WeakMap ?

#### --distractors--

Un Map n'autorise que des types primitifs comme clés, alors qu'un WeakMap n'autorise que des objets.

---

Un Map autorise le ramasse-miettes, alors qu'un WeakMap ne l'autorise pas.

---

Un WeakMap autorise les clés en double, alors qu'un Map ne les autorise pas.

#### --answer--

Un Map est itérable, alors qu'un WeakMap ne l'est pas.

### --question--

#### --text--

Laquelle des méthodes suivantes n'existe PAS sur un WeakMap ?

#### --distractors--

`.has()`

---

`.set()`

---

`.delete()`

#### --answer--

`.size()`

### --question--

#### --text--

Quelle sera la sortie du code suivant ?

```js
const set = new Set([1, 2, 3]);
set.delete(2);
console.log(set.has(2));
```

#### --distractors--

`true`

---

`undefined`

---

`2`

#### --answer--

`false`

### --question--

#### --text--

Comment peux-tu parcourir les valeurs d'un Set ?

#### --distractors--

En utilisant la méthode `.map()`.

---

En utilisant la méthode `.reduce()`.

---

En utilisant la méthode `.filter()`.

#### --answer--

En utilisant la méthode `.forEach()`.

### --question--

#### --text--

Quelles deux méthodes des Sets renvoient un `SetIterator` qui contient les valeurs d'un certain Set ?

#### --distractors--

`entries()` et `values()`

---

`forEach()` et `values()`

---

`has()` et `add()`

#### --answer--

`keys()` et `values()`

### --question--

#### --text--

Quelle sera la sortie du code suivant ?

```js
const map = new Map([["a", 1], ["b", 2]]);
map.clear();
console.log(map.size);
```

#### --distractors--

`2`

---

`1`

---

`undefined`

#### --answer--

`0`
