---
id: 66edd3f9bef926f129990425
title: Quiz sur le local storage et le CRUD
challengeType: 8
dashedName: quiz-local-storage-and-crud
---

# --description--

Pour réussir le quiz, tu dois répondre correctement à au moins 18 des 20 questions ci-dessous.

# --quizzes--

## --quiz--

### --question--

#### --text--

Que signifie CRUD dans le contexte du développement web ?

#### --distractors--

Create, Replace, Undo, Delete.

---

Connect, Remove, Update, Display.

---

Copy, Retrieve, Update, Delete.

#### --answer--

Create, Read, Update, Delete.

### --question--

#### --text--

Quelle méthode HTTP est couramment associée à l'opération Create dans une API RESTful ?

#### --distractors--

`GET`

---

`PUT`

---

`DELETE`

#### --answer--

`POST`

### --question--

#### --text--

Laquelle des opérations CRUD suivantes est typiquement effectuée lors de la modification d'un enregistrement existant dans une base de données ?

#### --distractors--

Read

---

Delete

---

Create

#### --answer--

Update

### --question--

#### --text--

Quelle est la principale différence entre localStorage et sessionStorage dans la Web Storage API ?

#### --distractors--

localStorage stocke les données en mémoire, tandis que sessionStorage stocke les données sur le disque.

---

sessionStorage est plus rapide que localStorage pour récupérer les données.

---

Les données de localStorage ne sont disponibles que pendant une session, tandis que sessionStorage persiste d'une session à l'autre.

#### --answer--

Les données de localStorage persistent jusqu'à suppression explicite, tandis que les données de sessionStorage sont effacées à la fin de la session de page.

### --question--

#### --text--

Quelle méthode est utilisée pour stocker des données dans localStorage ?

#### --distractors--

`localStorage.setData()`

---

`localStorage.saveItem()`

---

`localStorage.storeData()`

#### --answer--

`localStorage.setItem()`

### --question--

#### --text--

Comment retires-tu un seul élément de sessionStorage ?

#### --distractors--

`sessionStorage.clearItem('key')`

---

`sessionStorage.remove('key')`

---

`sessionStorage.deleteItem('key')`

#### --answer--

`sessionStorage.removeItem('key')`

### --question--

#### --text--

Quelle est la différence entre `PUT` et `PATCH` ?

#### --distractors--

`PUT` modifie des champs spécifiques, tandis que `PATCH` remplace toute la ressource.

---

`PUT` n'ajoute que de nouvelles ressources, tandis que `PATCH` met à jour celles qui existent.

---

`PUT` sert à récupérer des données, tandis que `PATCH` sert à envoyer des données.

#### --answer--

`PUT` remplace toute la ressource, tandis que `PATCH` met à jour des champs spécifiques.

### --question--

#### --text--

À quoi sert le cache storage ?

#### --distractors--

Le cache storage sert à stocker de façon permanente et sécurisée les identifiants utilisateur dans le navigateur à des fins d'authentification.

---

Le cache storage sert à stocker de grands volumes de données structurées dans le navigateur à l'aide d'un système de base de données.

---

Le cache storage sert à suivre les sessions et préférences utilisateur en stockant des paires clé-valeur accessibles sur plusieurs sites web.

#### --answer--

Le cache storage sert à stocker des ressources réseau fréquemment demandées, réduisant les temps de chargement et améliorant les performances.

### --question--

#### --text--

Qu'est-ce qu'un cookie dans le contexte du développement web ?

#### --distractors--

Un type de requête HTTP.

---

Un morceau de code qui s'exécute dans le navigateur.

---

Une méthode pour stocker de gros fichiers sur un serveur.

#### --answer--

Un petit morceau de données stocké par le navigateur web de l'utilisateur.

### --question--

#### --text--

Laquelle des méthodes suivantes est utilisée pour définir manuellement un cookie en JavaScript ?

#### --distractors--

`document.setCookie`

---

`document.cookie.set`

---

`document.createCookie`

#### --answer--

`document.cookie`

### --question--

#### --text--

Lequel des extraits de code suivants supprime correctement un cookie nommé `username` ?

#### --distractors--

`document.cookie.delete("username");`

---

`document.removeCookie("username");`

---

`cookie.delete("username");`

#### --answer--

`document.cookie = "username=; expires=Thu, 01 Jan 1970 00:00:00 UTC; path=/;";`

### --question--

#### --text--

Lequel des énoncés suivants concernant les cookies est faux ?

#### --distractors--

Les cookies de session expirent à la fermeture du navigateur.

---

Les cookies HttpOnly ne peuvent pas être consultés par JavaScript.

---

Les cookies Secure ne sont transmis que via HTTPS.

#### --answer--

Les cookies ne peuvent être définis que par le serveur.

### --question--

#### --text--

Qu'est-ce que le browser fingerprinting ?

#### --distractors--

Une technique utilisée pour identifier les utilisateurs à partir de leur adresse IP.

---

Une forme de chiffrement des données.

---

Une méthode de stockage de données dans le cache du navigateur.

#### --answer--

Une façon de suivre les utilisateurs via les attributs du navigateur et de l'appareil.

### --question--

#### --text--

Quel est un risque principal associé au stockage de mots de passe dans le local storage ?

#### --distractors--

Les données du local storage sont supprimées lorsque tu fermes l'onglet du navigateur.

---

Les données du local storage sont perdues lorsque tu supprimes les cookies.

---

Le local storage ne fonctionne pas hors ligne.

#### --answer--

Le local storage n'est pas chiffré et peut être consulté facilement.

### --question--

#### --text--

Lequel des énoncés suivants est vrai concernant le stockage côté client ?

#### --distractors--

Le stockage côté client utilise des objets plutôt que des chaînes pour stocker les données.

---

Le stockage côté client utilise des services cloud pour synchroniser les données utilisateur sur plusieurs appareils.

---

Le stockage côté client est plus sûr que le stockage côté serveur.

#### --answer--

Le stockage côté client permet aux sites web de stocker des données sur l'appareil d'un utilisateur.

### --question--

#### --text--

Quel est le principal avantage d'utiliser IndexedDB plutôt que localStorage ?

#### --distractors--

IndexedDB ne nécessite pas le consentement de l'utilisateur pour le stockage des données.

---

IndexedDB a une API plus simple.

---

IndexedDB est pris en charge dans tous les navigateurs web.

#### --answer--

IndexedDB peut gérer de plus grandes quantités de données et des requêtes plus complexes.

### --question--

#### --text--

Laquelle des approches suivantes est un moyen valide de récupérer des données depuis un object store dans IndexedDB ?

#### --distractors--

Appeler `retrieve()` sur l'object store.

---

Utiliser `query()` pour accéder à des enregistrements spécifiques.

---

Utiliser la méthode `fetch()`.

#### --answer--

Utiliser `get()` sur la référence du store.

### --question--

#### --text--

Quels types de transactions IndexedDB prend-il en charge ?

#### --distractors--

Transactions de lecture uniquement.

---

Transactions asynchrones uniquement.

---

Transactions synchrones uniquement.

#### --answer--

Transactions de lecture et d'écriture.

### --question--

#### --text--

Lequel des énoncés suivants est vrai concernant le cycle de vie d'un Service Worker ?

#### --distractors--

Les Service Workers s'exécutent dans le thread principal de l'application web.

---

Les Service Workers peuvent être activés avant d'être installés.

---

Les Service Workers ne peuvent s'exécuter que lorsque l'application web est ouverte dans le navigateur.

#### --answer--

Un Service Worker peut intercepter les requêtes réseau après avoir été activé.

### --question--

#### --text--

Quel énoncé décrit le mieux une caractéristique clé des Progressive Web Apps (PWAs) ?

#### --distractors--

Les PWAs ne peuvent être installées que sur des appareils mobiles.

---

Les PWAs doivent obligatoirement disposer d'un serveur back-end.

---

Les PWAs ne sont accessibles que via des navigateurs spécifiques.

#### --answer--

Les PWAs fournissent une fonctionnalité hors ligne grâce aux Service Workers.
