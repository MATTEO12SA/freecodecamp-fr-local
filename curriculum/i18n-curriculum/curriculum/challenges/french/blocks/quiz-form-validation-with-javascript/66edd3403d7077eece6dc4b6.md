---
id: 66edd3403d7077eece6dc4b6
title: Quiz sur la validation de formulaires avec JavaScript
challengeType: 8
dashedName: quiz-form-validation-with-javascript
---

# --description--

Pour réussir le quiz, tu dois répondre correctement à au moins 18 des 20 questions ci-dessous.

# --quizzes--

## --quiz--

### --question--

#### --text--

Quelle méthode est utilisée pour vérifier si la valeur d'un élément `input` satisfait les règles de validation ?

#### --distractors--

`validateInput()`

---

`isValid()`

---

`checkValue()`

#### --answer--

`checkValidity()`

### --question--

#### --text--

Que se passe-t-il si la méthode `checkValidity()` trouve un `input` invalide ?

#### --distractors--

Le formulaire est automatiquement soumis.

---

Rien ne se passe.

---

La valeur de l'`input` est effacée.

#### --answer--

Le navigateur affiche un message de validation.

### --question--

#### --text--

Quelle méthode est utilisée pour personnaliser le message affiché en cas d'échec de validation ?

#### --distractors--

`setErrorMessage()`

---

`setValidationMessage()`

---

`customMessage()`

#### --answer--

`setCustomValidity()`

### --question--

#### --text--

Quel est le comportement de validation par défaut pour les champs obligatoires lorsque le formulaire est soumis ?

#### --distractors--

Le formulaire est soumis même si les champs obligatoires sont vides.

---

Le formulaire se soumet mais affiche un avertissement.

---

Le formulaire réinitialise les valeurs des `input`.

#### --answer--

Le navigateur interrompt la soumission du formulaire si les champs obligatoires sont vides.

### --question--

#### --text--

Quelle action ne provoque pas la vérification des erreurs d'un formulaire HTML ?

#### --distractors--

Modifier la valeur de l'`input`.

---

Soumettre le formulaire.

---

Appeler `reportValidity()`.

#### --answer--

Cliquer sur un bouton reset.

### --question--

#### --text--

Que renvoie `checkValidity()` lorsque toutes les saisies du formulaire sont valides ?

#### --distractors--

`false`

---

`undefined`

---

`null`

#### --answer--

`true`

### --question--

#### --text--

Quand la méthode `checkValidity()` interrompt-elle la soumission du formulaire ?

#### --distractors--

Lorsque le formulaire n'a pas de bouton submit.

---

Lorsque l'`action` du formulaire est vide.

---

Lorsque la méthode du formulaire est POST.

#### --answer--

Lorsque le formulaire a au moins un `input` invalide (et que le comportement par défaut de soumission du formulaire est empêché).

### --question--

#### --text--

Que se passe-t-il si `checkValidity()` est appelée, mais qu'un champ échoue à la validation ?

#### --distractors--

Le formulaire se soumet sans erreur.

---

Les valeurs des `input` sont effacées.

---

Le formulaire se ferme immédiatement.

#### --answer--

Le champ invalide est mis en évidence et un message apparaît.

### --question--

#### --text--

Quelle action fera exécuter `checkValidity()` ?

#### --distractors--

Réinitialiser le formulaire.

---

Modifier la valeur d'un `input`.

---

Cliquer sur une balise d'ancre.

#### --answer--

Soumettre le formulaire manuellement.

### --question--

#### --text--

Quel est le but d'appeler `checkValidity()` avant de soumettre un formulaire ?

#### --distractors--

Soumettre automatiquement le formulaire.

---

Recharger la page.

---

Effacer tous les champs `input`.

#### --answer--

Vérifier que tous les éléments `input` respectent leurs contraintes spécifiées.

### --question--

#### --text--

Quel est le but principal de `e.preventDefault()` ?

#### --distractors--

Recharger le formulaire.

---

Effacer les données du formulaire.

---

Réinitialiser les valeurs du formulaire.

#### --answer--

Arrêter l'action par défaut d'un événement.

### --question--

#### --text--

Lorsqu'elle est utilisée sur l'événement submit d'un formulaire, que fait `e.preventDefault()` ?

#### --distractors--

Elle soumet le formulaire.

---

Elle efface tous les champs du formulaire.

---

Elle désactive temporairement le formulaire.

#### --answer--

Elle empêche le formulaire d'être soumis.

### --question--

#### --text--

Dans quel scénario `e.preventDefault()` est-elle le plus utile ?

#### --distractors--

Lorsqu'on gère un clic sur un bouton qui soumet un formulaire.

---

Lorsqu'on ferme une boîte de dialogue modale.

---

Lorsqu'on ouvre une nouvelle page.

#### --answer--

Lorsqu'on soumet un formulaire et qu'on empêche le rechargement de la page.

### --question--

#### --text--

Que se passe-t-il si tu oublies d'appeler `e.preventDefault()` sur un événement submit de formulaire ?

#### --distractors--

Tous les champs de saisie sont remis à leurs valeurs par défaut.

---

Le formulaire ne se soumet pas.

---

Les données du formulaire sont perdues.

#### --answer--

Le formulaire se soumet et actualise la page.

### --question--

#### --text--

Quel comportement par défaut peut être empêché avec `e.preventDefault()` ?

#### --distractors--

Modifier un formulaire.

---

Afficher le formulaire.

---

Recharger le formulaire.

#### --answer--

Le comportement par défaut provoqué par la soumission d'un formulaire.

### --question--

#### --text--

Qu'est-ce qui déclenche l'événement submit du formulaire ?

#### --distractors--

Lorsqu'un champ de saisie change.

---

Lorsque le formulaire est réinitialisé.

---

Lorsque l'action du formulaire est effacée.

#### --answer--

Lorsque le bouton submit du formulaire est cliqué ou que Enter est pressée.

### --question--

#### --text--

Où gères-tu habituellement l'événement submit en JavaScript ?

#### --distractors--

Sur un élément de saisie texte.

---

Sur n'importe quel champ du formulaire.

---

Sur un élément button en dehors du formulaire.

#### --answer--

Sur l'élément form lui-même.

### --question--

#### --text--

Que fait l'événement submit par défaut ?

#### --distractors--

Il valide le formulaire mais ne le soumet pas.

---

Il efface les champs du formulaire.

---

Il annule la soumission du formulaire.

#### --answer--

Il envoie les données du formulaire au serveur spécifié dans l'action du formulaire.

### --question--

#### --text--

Quel événement dois-tu écouter pour gérer les données du formulaire lorsque celui-ci est soumis en JavaScript ?

#### --distractors--

`"input"`

---

`"change"`

---

`"focus"`

#### --answer--

`"submit"`

### --question--

#### --text--

Quelle est la valeur par défaut de l'attribut `method` dans un formulaire HTML ?

#### --distractors--

`"POST"`

---

`"UPDATE"`

---

`"PUT"`

#### --answer--

`"GET"`
