---
id: 6708143cab2b583ecd3324f5
title: Que sont les attributs et comment fonctionnent-ils ?
challengeType: 19
dashedName: what-are-attributes
---

# --interactive--

Un attribut est une valeur placée dans la balise ouvrante d'un élément HTML. Les attributs fournissent des informations supplémentaires sur l'élément ou indiquent comment il doit se comporter. Voici la syntaxe de base d'un attribut :

```html
<element attribute="value"></element>
```

Le nom de l'attribut est suivi d'un signe égal (`=`) et d'une valeur entre guillemets. La valeur peut être une chaîne de caractères ou un nombre, selon l'attribut.

Ce premier exemple utilise les attributs `href` et `target`. L'attribut `href` indique l'URL d'un lien, et l'attribut `target` indique où ouvrir ce lien.

**Remarque :** l'élément `a`, aussi appelé élément d'ancre, sert à créer des hyperliens. Le texte entre les balises ouvrante et fermante `a` est la partie cliquable que les utilisateurs sélectionnent pour naviguer.

Active l'éditeur interactif et change `href="https://www.freecodecamp.org/news/"` en `href="https://www.freecodecamp.org"`. Ensuite, quand tu cliqueras sur le lien dans l'éditeur interactif, tu verras la page d'accueil de freeCodeCamp s'ouvrir dans un nouvel onglet du navigateur.

:::interactive_editor

```html
<a href="https://www.freecodecamp.org/news/" target="_blank">Visit freeCodeCamp</a>
```

:::

Sans l'attribut `href`, le lien ne fonctionnerait pas, car il n'y aurait pas d'URL de destination. Tu dois donc inclure cet attribut `href` pour rendre le lien fonctionnel. `target="_blank"` permet au lien de s'ouvrir dans un nouvel onglet du navigateur. Tu en apprendras davantage sur l'attribut `target` dans de futures leçons.

D'autres attributs courants sont `src` et `alt`, ou attribut alternatif, qui servent respectivement à indiquer la source d'une image et à fournir un texte descriptif alternatif pour cette image.

Active l'éditeur interactif et change `src="https://cdn.freecodecamp.org/curriculum/cat-photo-app/cats.jpg"` en `src="https://cdn.freecodecamp.org/curriculum/cat-photo-app/running-cats.jpg"`. Puis change `alt="Two tabby kittens sleeping together on a couch."` en `alt="Two cats running in the dirt."`.

:::interactive_editor

```html
<img src="https://cdn.freecodecamp.org/curriculum/cat-photo-app/cats.jpg" alt="Two tabby kittens sleeping together on a couch." />
```

:::

Comme l'attribut `href`, l'attribut `src` est nécessaire, car il indique le fichier image à afficher. L'attribut `alt` n'est pas obligatoire, mais il est recommandé pour l'accessibilité. L'accessibilité consiste à s'assurer que tout le monde, y compris les personnes en situation de handicap, peut utiliser et comprendre des choses comme les sites web, les applications et les espaces physiques. Tu en apprendras davantage sur l'accessibilité dans les prochaines leçons.

Certains attributs ont une syntaxe un peu particulière, comme l'attribut `checked`.

Active l'éditeur interactif et essaie de cliquer sur la case à cocher dans la fenêtre d'aperçu pour la voir passer de l'état coché à l'état décoché.

:::interactive_editor

```html
<input type="checkbox" checked />
```

:::

Dans l'exemple suivant, nous avons un élément `input` avec l'attribut `type` défini sur `checkbox`. Les inputs servent à collecter des données auprès des utilisateurs, et l'attribut `type` indique le type d'input. Dans ce cas, cet input est une case à cocher. Tu en apprendras davantage sur le fonctionnement des inputs dans les prochaines leçons.

L'attribut `checked` sert à indiquer que la case à cocher doit être cochée par défaut. L'attribut `checked` ne nécessite pas de valeur. S'il est présent, la case sera cochée par défaut. Si l'attribut n'est pas présent, la case sera décochée. C'est ce qu'on appelle un attribut booléen. Tu en apprendras davantage sur les booléens en général quand tu arriveras à la section JavaScript.

Active l'éditeur interactif et essaie de supprimer l'attribut `checked` de l'élément `input`. Tu verras que la case à cocher n'est plus cochée par défaut.

:::interactive_editor

```html
<input type="checkbox" checked />
```

:::

Il existe plusieurs attributs booléens courants que tu rencontreras en HTML, comme `disabled`, `readonly` et `required`. Ces attributs servent à indiquer l'état d'un élément, par exemple s'il est désactivé, en lecture seule ou obligatoire.

Voici un exemple d'élément `input` de type texte qui est désactivé par défaut. Active l'éditeur interactif et essaie de cliquer sur l'élément `input` dans la fenêtre d'aperçu. Ensuite, supprime l'attribut `disabled` de l'élément `input` et tu verras que l'input n'est plus désactivé par défaut. Tu devrais maintenant pouvoir cliquer dessus et écrire dans le champ.

:::interactive_editor

```html
<input type="text" disabled>
```

:::

HTML possède de nombreux attributs qui peuvent servir à personnaliser le comportement et l'apparence des éléments sur une page web. Comprendre comment utiliser les attributs est essentiel pour créer du contenu web interactif et accessible. Dans les prochaines leçons, tu découvriras d'autres attributs HTML et tu apprendras à les utiliser efficacement dans tes projets de développement web.

# --questions--

## --text--

Lequel des éléments suivants est un exemple d'attribut booléen ?

## --answers--

`src`

### --feedback--

Relis la section où les attributs booléens courants ont été abordés.

---

`href`

### --feedback--

Relis la section où les attributs booléens courants ont été abordés.

---

`disabled`

---

`alt`

### --feedback--

Relis la section où les attributs booléens courants ont été abordés.

## --video-solution--

3

## --text--

Quel est le rôle d'un attribut en HTML ?

## --answers--

Les attributs fournissent des informations supplémentaires et aident à définir le comportement des éléments HTML.

---

Les attributs changent la couleur de fond d'un élément.

### --feedback--

Relis la section où la définition des attributs a été abordée.

---

Les attributs changent la taille de police d'un élément.

### --feedback--

Relis la section où la définition des attributs a été abordée.

---

Les attributs ajoutent une fonctionnalité JavaScript à un élément.

### --feedback--

Relis la section où la définition des attributs a été abordée.

## --video-solution--

1

## --text--

Laquelle des syntaxes suivantes est correcte pour un attribut booléen ?

## --answers--

`<input type="checkbox" checked>`

---

`<input type="checkbox" checked="on">`

### --feedback--

Relis la section où l'attribut `checked` a été abordé.

---

`<input type="checkbox" checked="off">`

### --feedback--

Relis la section où l'attribut `checked` a été abordé.

---

`<input type="checkbox" checked="isChecked">`

### --feedback--

Relis la section où l'attribut `checked` a été abordé.

## --video-solution--

1
