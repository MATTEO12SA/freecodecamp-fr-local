---
id: 66f1a417757b6ca4eecd89d6
title: Quiz sur l'état et les Hooks React
challengeType: 8
dashedName: quiz-react-state-and-hooks
---

# --description--

Pour réussir le quiz, tu dois répondre correctement à au moins 18 des 20 questions ci-dessous.

# --quizzes--

## --quiz--

### --question--

#### --text--

Lequel des éléments suivants est la bonne façon d'ajouter un événement clic à un bouton en React ?

#### --distractors--

```jsx
<button onClick={{ handleLoginClick }} />
```

---

```jsx
<button onclick={handleLoginClick} />
```

---

```jsx
<button onClick={handleLoginClick()} />
```

#### --answer--

```jsx
<button onClick={handleLoginClick} />
```

### --question--

#### --text--

Qu'est-ce qu'un Synthetic Event React ?

#### --distractors--

Un type d'élément JSX pour les événements `inputs` et `submit` uniquement.

---

Un type d'élément JSX pour les événements `submit` uniquement.

---

Une façon de modifier la phase de bubbling de la propagation d'événements pour la rendre plus conviviale.

#### --answer--

Une enveloppe autour des événements natifs comme `click` et `submit`.

### --question--

#### --text--

Lequel des éléments suivants est un préfixe courant pour nommer les fonctions gestionnaires d'événements ?

#### --distractors--

isHandling

---

hasHandled

---

handling

#### --answer--

handle

### --question--

#### --text--

Quelles sont les trois étapes majeures du rendu React ?

#### --distractors--

Handshake, chargement et hydratation.

---

Récupération, décodage et exécution.

---

Montage, mise à jour et démontage.

#### --answer--

Déclenchement, rendu et commit.

### --question--

#### --text--

Lequel des éléments suivants est la bonne façon d'utiliser le hook `useState` ?

#### --distractors--

```js
const [count, setCount] = useState<0>;
```

---

```js
const <count, setCount> = useState(0);
```

---

```js
const count, setCount = useState(0);
```

#### --answer--

```js
const [count, setCount] = useState(0);
```

### --question--

#### --text--

Lequel des éléments suivants N'EST PAS un attribut valide pour gérer les événements React ?

#### --distractors--

`onChange`

---

`onClick`

---

`onSubmit`

#### --answer--

`onHandle`

### --question--

#### --text--

Lequel des éléments suivants est la bonne façon de mettre à jour l'état d'un tableau ?

```js
const [certificates, setCertificates] = useState([]);
```

#### --distractors--

```js
setCertificates(previousItems => previousItems, "Front-End");
```

---

```js
setCertificates.push("Front-End");
```

---

```js
setCertificates().append("Front-End");
```

#### --answer--

```js
setCertificates(previousItems => [...previousItems, "Front-End"]);
```

### --question--

#### --text--

Qu'est-ce qui ne va pas avec cette fonction ?

```js
function updateSpaceship() {
  setSpaceship(previousState => ({
    name: "Discovery"
  }));
}
```

#### --distractors--

Mettre à jour une variable `useState` n'est pas autorisé dans une fonction imbriquée.

---

`previousState` doit être entouré de parenthèses.

---

Les fonctions fléchées ne doivent pas être utilisées dans une fonction setter d'état.

#### --answer--

`previousState` doit être étalé (spread) avant `name` pour que les autres propriétés ne soient pas écrasées.

### --question--

#### --text--

Lequel des éléments suivants est la bonne façon de mettre à jour l'état pour retirer des éléments d'un tableau ?

#### --distractors--

```js
const removeItem = (id) => {
  setItems((prevItems) => prevItems.reduce((item) => item.id));
};
```

---

```js
const removeItem = (id) => {
  setItems((prevItems) => prevItems.map((item) => item.id !== id));
};
```

---

```js
const removeItem = (id) => {
  setItems(() => delete item.id !== id);
};
```

#### --answer--

```js
const removeItem = (id) => {
  setItems((prevItems) => prevItems.filter((item) => item.id !== id));
};
```

### --question--

#### --text--

À quoi sert typiquement l'attribut `ref` en React ?

#### --distractors--

Il sert à auditer les problèmes de performance dans les composants React.

---

Il sert à auditer les problèmes d'accessibilité dans les composants React.

---

Il sert à retirer des nœuds DOM.

#### --answer--

Il sert à accéder à un nœud DOM.

### --question--

#### --text--

Lequel des éléments suivants est la bonne façon d'accéder à la valeur actuelle d'un `ref` ?

#### --distractors--

```js
const inputRef = useRef(null);
inputRef.initial
```

---

```js
const inputRef = useRef(null);
inputRef.val
```

---

```js
const inputRef = useRef(null);
inputRef.curr
```

#### --answer--

```js
const inputRef = useRef(null);
inputRef.current
```

### --question--

#### --text--

Combien de fois ce message sera-t-il journalisé dans la console ?

```js
useEffect(() => {
  console.log("Nice work!!");
}, []);
```

#### --distractors--

Trois fois.

---

Deux fois.

---

Aucune, car le tableau de dépendances est vide.

#### --answer--

Une seule fois au rendu initial.

### --question--

#### --text--

À quoi servent typiquement les hooks personnalisés React ?

#### --distractors--

Ils servent à exécuter la fonctionnalité la plus complexe d'une app.

---

Ils ne sont en général pas utilisés ; on préfère les hooks intégrés.

---

Ils remplacent les méthodes de cycle de vie des versions précédentes de React.

#### --answer--

Ils aident à réutiliser de la logique avec état entre composants.

### --question--

#### --text--

Combien de fois ce message sera-t-il journalisé dans la console ?

```js
useEffect(() => {
  console.log("I love freeCodeCamp!");
});
```

#### --distractors--

Il sera journalisé à chaque soumission de formulaire ou événement de clic.

---

Il sera journalisé deux fois.

---

Il ne sera journalisé qu'une seule fois.

#### --answer--

Il sera journalisé à chaque rendu du composant.

### --question--

#### --text--

Lequel des éléments suivants est la bonne façon d'empêcher un rechargement du navigateur pour un événement `onSubmit` ?

#### --distractors--

```jsx
function handleSubmit(event) {
  event.defaultPrevent();
  console.log("Form submitted!");
}

<form onSubmit={handleSubmit}>
  <input type="text" />
  <button>Submit</button>
</form>;
```

---

```jsx
function handleSubmit(event) {
  event.Default();
  console.log("Form submitted!");
}

<form onSubmit={handleSubmit}>
  <input type="text" />
  <button>Submit</button>
</form>;
```

---

```jsx
function handleSubmit(event) {
  event.prevent();
  console.log("Form submitted!");
}

<form onSubmit={handleSubmit}>
  <input type="text" />
  <button>Submit</button>
</form>;
```

#### --answer--


```jsx
function handleSubmit(event) {
  event.preventDefault();
  console.log("Form submitted!");
}

<form onSubmit={handleSubmit}>
  <input type="text" />
  <button>Submit</button>
</form>;
```

### --question--

#### --text--

Lequel des hooks suivants sert typiquement à récupérer des données ?

#### --distractors--

`useReducer`

---

`useState`

---

`useDebounce`

#### --answer--

`useEffect`

### --question--

#### --text--

Lequel des éléments suivants N'EST PAS une propriété valide de l'objet Event natif ?

#### --distractors--

`currentTarget`

---

`type`

---

`target`

#### --answer--

`prop`

### --question--

#### --text--

Laquelle des étapes de rendu suivantes survient quand React détecte qu'un changement s'est produit et que l'interface utilisateur (UI) pourrait devoir être mise à jour ?

#### --distractors--

setter

---

commit

---

loop

#### --answer--

trigger

### --question--

#### --text--

Laquelle des étapes suivantes désigne le moment où React prend les changements préparés depuis le virtual DOM et les applique au vrai DOM ?

#### --distractors--

trigger

---

render

---

setter

#### --answer--

commit

### --question--

#### --text--

Quel est le rôle des `dependencies` dans un `useEffect` ?

```js
useEffect(() => {
  // Your side effect logic (usually a function) goes here
}, [dependencies]);
```

#### --distractors--

L'argument optionnel `dependencies` n'a aucun effet sur le `useEffect`.

---

L'argument optionnel `dependencies` fait tourner l'effet en boucle infinie.

---

L'argument optionnel `dependencies` sert à empêcher l'effet de s'exécuter.

#### --answer--

L'argument optionnel `dependencies` contrôle quand l'effet s'exécute.
