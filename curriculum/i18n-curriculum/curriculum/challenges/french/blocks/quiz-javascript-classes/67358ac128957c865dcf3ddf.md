---
id: 67358ac128957c865dcf3ddf
title: Quiz JavaScript Classes
challengeType: 8
dashedName: quiz-javascript-classes
---

# --description--

Pour réussir le quiz, tu dois répondre correctement à au moins 18 des 20 questions ci-dessous.

# --quizzes--

## --quiz--

### --question--

#### --text--

Quel est le rôle d'une classe en JavaScript ?

#### --distractors--

Définir uniquement des propriétés, pas de méthodes.

---

Définir uniquement des variables statiques.

---

Organiser uniquement des données primitives.

#### --answer--

Créer des modèles réutilisables pour des objets.

### --question--

#### --text--

Comment définis-tu une classe en JavaScript ?

#### --distractors--

Avec le mot-clé `className`.

---

Avec le mot-clé `object`.

---

Avec le mot-clé `constructor` uniquement.

#### --answer--

Avec le mot-clé `class`.

### --question--

#### --text--

Quelle méthode est utilisée dans une classe pour initialiser les propriétés ?

#### --distractors--

`static`

---

`init`

---

`define`

#### --answer--

`constructor`

### --question--

#### --text--

Que représente `this` dans le constructeur d'une classe ?

#### --distractors--

L'objet classe global.

---

La fonction elle-même.

---

Une référence undefined.

#### --answer--

Une instance de la classe.

### --question--

#### --text--

Laquelle des affirmations suivantes décrit le comportement de `this` dans une fonction fléchée à l'intérieur d'une méthode de classe ?

#### --distractors--

Il hérite de la valeur de `this` de la fonction la plus proche dans une portée différente.

---

Il fait référence à une nouvelle instance de la classe.

---

Il hérite de la valeur de `this` de l'objet global en mode strict.

#### --answer--

Il hérite de la valeur de `this` de la portée englobante où il est défini.

### --question--

#### --text--

Quel est le rôle principal du mot-clé `extends` ?

#### --distractors--

Définir des méthodes statiques dans le constructeur d'une classe parente.

---

Créer une méthode dans une classe.

---

Initialiser des propriétés par défaut dans la classe parente.

#### --answer--

Créer une sous-classe qui hérite d'une classe parente.

### --question--

#### --text--

Lequel des exemples suivants montre comment créer une classe `Car` qui hérite d'une classe `Vehicle` ?

#### --distractors--

`class Car uses Vehicle {}`

---

`class Car = Vehicle {}`

---

`class Car inherits Vehicle {}`

#### --answer--

`class Car extends Vehicle {}`

### --question--

#### --text--

Quelle est la sortie du code suivant ?

```javascript
class Animal {
  speak() {
    return "Animal speaks";
  }
}

class Dog extends Animal {
  speak() {
    return "Dog barks";
  }
}

const myDog = new Dog();
console.log(myDog.speak());
```

#### --distractors--

`Animal speaks`

---

Une erreur est levée.

---

`undefined`

#### --answer--

`Dog barks`

### --question--

#### --text--

Quelle est la fonction du mot-clé `super` dans une sous-classe ?

#### --distractors--

Définir une nouvelle instance de la classe parente.

---

Supprimer des méthodes et propriétés de la classe parente.

---

Accéder uniquement aux méthodes statiques de la classe parente.

#### --answer--

Appeler le constructeur ou les méthodes de la classe parente.

### --question--

#### --text--

Laquelle des affirmations suivantes décrit une méthode statique ?

#### --distractors--

Une méthode inaccessible à l'intérieur de la classe elle-même.

---

Une méthode utilisée uniquement dans les fonctions fléchées.

---

Une méthode unique à chaque instance d'une classe.

#### --answer--

Une méthode qui appartient à la classe elle-même, pas à une instance.

### --question--

#### --text--

Que va afficher le code suivant ?

```js
class Calculator {
  static add(a, b) {
    return a + b;
  }
}

console.log(Calculator.add(3, 4));
```

#### --distractors--

Une erreur est levée.

---

`undefined`

---

`NaN`

#### --answer--

`7`

### --question--

#### --text--

Comment accèdes-tu à une propriété statique dans une classe JavaScript ?

#### --distractors--

En utilisant `this.propertyName` dans une méthode.

---

En utilisant `instance.propertyName` après avoir créé une instance.

---

En utilisant `super.propertyName` dans une sous-classe.

#### --answer--

En utilisant `ClassName.propertyName`.

### --question--

#### --text--

Dans quel scénario utiliserais-tu une méthode statique plutôt qu'une méthode d'instance ?

#### --distractors--

Quand tu dois accéder et modifier les propriétés de l'instance.

---

Quand la méthode doit être appelée sur une instance d'objet.

---

Quand la méthode ne dépend pas de données spécifiques à l'instance et opère sur des données au niveau de la classe.

#### --answer--

Quand la méthode opère sur des données partagées par toutes les instances de la classe.

### --question--

#### --text--

Que va afficher le code suivant ?

```js
class MyClass {
  sayHello() {
    return "Hello!";
  }
}

console.log(MyClass.sayHello());
```

#### --distractors--

`Hello!`

---

`null`

---

`undefined`

#### --answer--

Cela lève une erreur.

### --question--

#### --text--

Quel mot-clé est utilisé pour hériter des propriétés et méthodes d'une autre classe ?

#### --distractors--

`import`

---

`inherit`

---

`static`

#### --answer--

`extends`

### --question--

#### --text--

Qu'affiche le code suivant ?

```js
class Person {
  static species = "Human";
}

console.log(Person.species);
```

#### --distractors--

`undefined`

---

`null`

---

Une erreur est levée.

#### --answer--

`Human`

### --question--

#### --text--

Laquelle des affirmations suivantes décrit le mieux l'héritage ?

#### --distractors--

Il permet à une méthode de renvoyer un nouvel objet.

---

Il permet à une instance de définir ses propres propriétés.

---

Il permet de convertir une propriété statique en propriété non statique.

#### --answer--

Il permet à une classe d'acquérir des propriétés et méthodes d'une autre classe.

### --question--

#### --text--

Que se passe-t-il si un constructeur n'est pas défini dans une sous-classe ?

#### --distractors--

Une erreur sera levée.

---

La sous-classe héritera du constructeur de n'importe quelle classe dans le code.

---

La sous-classe ne pourra pas créer de nouvelles instances.

#### --answer--

Le constructeur de la superclasse sera utilisé par défaut.

### --question--

#### --text--

Que représente `this` dans le code suivant ?

```js
class Animal {
  constructor(name) {
    this.name = name;
  }
}
```

#### --distractors--

L'objet global.

---

La classe `Animal` elle-même.

---

Une variable undefined.

#### --answer--

L'instance de `Animal`.

### --question--

#### --text--

Quel est l'usage principal des propriétés statiques ?

#### --distractors--

Définir des propriétés par défaut pour chaque instance de classe.

---

Surcharger des méthodes dans une sous-classe.

---

Définir des données spécifiques à l'instance.

#### --answer--

Stocker des données au niveau de la classe partagées entre les instances.
