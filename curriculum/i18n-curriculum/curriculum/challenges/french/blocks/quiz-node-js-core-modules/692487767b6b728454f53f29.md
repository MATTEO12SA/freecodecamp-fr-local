---
id: 692487767b6b728454f53f29
title: Quiz sur les modules de base de NodeJS
challengeType: 8
dashedName: quiz-node-js-core-modules
---

# --description--

Pour réussir le quiz, tu dois répondre correctement à au moins 18 des 20 questions ci-dessous.

# --quizzes--

## --quiz--

### --question--

#### --text--

Quelle méthode `fs` ajoute du contenu à un fichier existant sans l'écraser ?

#### --distractors--

`writeFile()`

---

`readFile()`

---

`unlink()`

#### --answer--

`appendFile()`

### --question--

#### --text--

Que se passe-t-il quand tu utilises des méthodes synchrones du module `fs` ?

#### --distractors--

Elles suppriment automatiquement les fichiers existants.

---

Elles s'exécutent plus vite que les asynchrones dans tous les cas.

---

Elles renvoient des Promises par défaut.

#### --answer--

Elles bloquent le programme jusqu'à la fin de l'opération.

### --question--

#### --text--

Que fait le paramètre `utf8` dans `fs.readFile()` ?

#### --distractors--

Il empêche les erreurs d'être levées.

---

Il change l'extension du fichier.

---

Il compresse les gros fichiers.

#### --answer--

Il indique à Node de renvoyer une chaîne lisible au lieu d'un buffer.

### --question--

#### --text--

Quelle méthode `Buffer` crée un nouveau buffer rempli de zéros ?

#### --distractors--

`Buffer.from()`

---

`Buffer.concat()`

---

`Buffer.byteLength()`

#### --answer--

`Buffer.alloc()`

### --question--

#### --text--

Que fait `Buffer.toString()` ?

#### --distractors--

Convertit un buffer en code binaire.

---

Efface le contenu d'un buffer.

---

Affiche la longueur en octets du buffer.

#### --answer--

Convertit le contenu du buffer en une chaîne lisible.

### --question--

#### --text--

Quelle fonctionnalité `crypto` nécessite une clé privée pour générer une signature ?

#### --distractors--

`createHash()`

---

`createCipheriv()`

---

`randomBytes()`

#### --answer--

`sign()`

### --question--

#### --text--

Pourquoi devrais-tu éviter d'écrire tes propres algorithmes crypto directement ?

#### --distractors--

C'est illégal dans les applications Node.js.

---

Node.js ne prend pas en charge le chiffrement.

---

JavaScript ne peut pas calculer de hachages.

#### --answer--

Cela peut être dangereux sans bibliothèques bien testées.

### --question--

#### --text--

Quelle méthode `crypto` est recommandée pour générer des tokens aléatoires sécurisés à la place de `Math.random()` ?

#### --distractors--

`randomInt()`

---

`createHmac()`

---

`createCipheriv()`

#### --answer--

`randomBytes()`

### --question--

#### --text--

Quelle méthode du module OS renvoie l'architecture CPU ?

#### --distractors--

`os.cpus()`

---

`os.platform()`

---

`os.version()`

#### --answer--

`os.arch()`

### --question--

#### --text--

Que renvoie `os.userInfo()` ?

#### --distractors--

Uniquement le nom d'utilisateur.

---

Uniquement l'adresse IP de l'appareil.

---

Uniquement l'ID de l'utilisateur connecté.

#### --answer--

Des informations sur l'utilisateur système actuel.

### --question--

#### --text--

Quelle méthode `path` joint des segments et normalise les slashs en trop ?

#### --distractors--

`path.resolve()`

---

`path.extname()`

---

`path.dirname()`

#### --answer--

`path.join()`

### --question--

#### --text--

Quelle méthode `path` produit un chemin absolu basé sur le répertoire de travail actuel ?

#### --distractors--

`path.extname()`

---

`path.format()`

---

`path.parse()`

#### --answer--

`path.resolve()`

### --question--

#### --text--

Que représente la variable globale `__dirname` ?

#### --distractors--

Le nom de l'utilisateur qui exécute Node.

---

L'extension du fichier actuel.

---

Un chemin de dossier relatif commençant par `./`.

#### --answer--

Le chemin absolu du répertoire contenant le fichier actuel.

### --question--

#### --text--

Quelle propriété de process contient des variables d'environnement comme `NODE_ENV` ?

#### --distractors--

`process.cwd`

---

`process.version`

---

`process.platform`

#### --answer--

`process.env`

### --question--

#### --text--

Que fait l'événement `process.on("exit")` ?

#### --distractors--

Il redémarre l'application Node.

---

Il envoie un signal `exit` aux processus enfants.

---

Il vide la file de la boucle d'événements.

#### --answer--

Exécute du code juste avant que le processus Node se termine.

### --question--

#### --text--

Quel type de stream peut à la fois lire et écrire des données sans les transformer ?

#### --distractors--

Stream Readable

---

Stream Transform

---

Stream Writable

#### --answer--

Stream Duplex

### --question--

#### --text--

Que faut-il pour implémenter efficacement la lecture et l'écriture de gros fichiers ?

#### --distractors--

Utiliser uniquement `Buffer.alloc()`

---

Importer le module `crypto`

---

Enregistrer d'abord tout le fichier en mémoire

#### --answer--

Utiliser des streams pour traiter les données par morceaux

### --question--

#### --text--

Quelle méthode relie un stream readable à un stream writable ?

#### --distractors--

`pipeTo()`

---

`connect()`

---

`streamWrite()`

#### --answer--

`pipe()`

### --question--

#### --text--

Quel événement de stream indique qu'il n'y a plus de données à écrire ?

#### --distractors--

`close`

---

`drain`

---

`end`

#### --answer--

`finish`

### --question--

#### --text--

Qu'est-ce qui est journalisé si tu affiches un morceau d'un stream readable sans `toString()` ?

#### --distractors--

Du contenu en texte brut

---

Le PID du processus

---

Le nom du système d'exploitation

#### --answer--

Un objet buffer avec des données binaires
