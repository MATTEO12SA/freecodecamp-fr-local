---
id: 66ed9018f45ce3ece4053eb9
title: Quiz sur les variables CSS
challengeType: 8
dashedName: quiz-css-variables
---

# --description--

Pour réussir le quiz, tu dois répondre correctement à au moins 18 des 20 questions ci-dessous.

# --quizzes--

## --quiz--

### --question--

#### --text--

Lequel des éléments suivants n'est PAS un descripteur valide dans la règle `@property` ?

#### --distractors--

`syntax`

---

`inherits`

---

`initial-value`

#### --answer--

`animation`

### --question--

#### --text--

Dans le code suivant, quelle valeur aura `color` si `--main-color` n'est pas définie ?

```css
p {
  color: var(--main-color, pink);
}
```

#### --distractors--

`Undefined`

---

Aucune couleur ne sera appliquée.

---

`--main-color`

#### --answer--

`pink`

### --question--

#### --text--

Laquelle des options suivantes est une façon valide de déclarer une propriété personnalisée en CSS ?

#### --distractors--

`$background-color: #333;`

---

`@property: #333`

---

`var(--background-color: #333);`

#### --answer--

`--background-color: #333;`

### --question--

#### --text--

Quel est le but principal des propriétés personnalisées en CSS ?

#### --distractors--

Modifier la structure du DOM.

---

Créer des sélecteurs dynamiques.

---

Améliorer l'accessibilité.

#### --answer--

Définir des styles réutilisables.

### --question--

#### --text--

Que représente le sélecteur `:root` ?

#### --distractors--

Le premier élément enfant de `body`.

---

L'élément parent de tous les éléments `header`.

---

Le premier enfant de la balise `html`.

#### --answer--

L'élément de plus haut niveau dans l'arbre DOM.

### --question--

#### --text--

Comment appliquer une propriété personnalisée `--foreground` comme valeur de `color` en CSS ?

#### --distractors--

`color: --foreground;`

---

`color: css(--foreground);`

---

`color: $foreground;`

#### --answer--

`color: var(--foreground);`

### --question--

#### --text--

À quoi sert la règle `@property` en CSS ?

#### --distractors--

À définir une media query.

---

À contrôler les animations CSS et leur timing.

---

À regrouper des propriétés CSS dans une seule règle.

#### --answer--

À définir le comportement des propriétés personnalisées.

### --question--

#### --text--

Quand tu déclares une `@property` personnalisée, à quoi sert `syntax` dans sa définition ?

#### --distractors--

Elle précise la valeur par défaut de la propriété personnalisée.

---

Elle détermine si la propriété peut être héritée par les éléments enfants.

---

Elle définit si la propriété est appliquée à tous les éléments.

#### --answer--

Elle impose un type de donnée ou un motif de valeur précis pour la propriété personnalisée.

### --question--

#### --text--

À quoi dois-tu faire attention quand tu utilises des propriétés personnalisées ?

#### --distractors--

Les propriétés personnalisées sont automatiquement converties en unités `px`.

---

Les propriétés personnalisées remplacent toujours les styles inline.

---

Les propriétés personnalisées ne peuvent pas être utilisées pour définir des valeurs de police.

#### --answer--

Les propriétés personnalisées peuvent ne pas être prises en charge par les anciens navigateurs.

### --question--

#### --text--

À quoi sert une valeur de fallback dans la fonction `var()` pour les propriétés personnalisées CSS ?

#### --distractors--

À réduire la quantité de code CSS.

---

À améliorer les performances dans les navigateurs modernes.

---

À optimiser le temps de rendu sur les réseaux lents.

#### --answer--

À garantir qu'une valeur valide est appliquée si la propriété personnalisée est indéfinie.

### --question--

#### --text--

Quand tu définis une variable `--foreground` dans une media query, que se passe-t-il quand la media query ne correspond plus au viewport actuel ?

#### --distractors--

La propriété personnalisée est conservée et continue de s'appliquer.

---

La propriété personnalisée revient à sa valeur initiale.

---

La propriété personnalisée est recalculée selon le viewport.

#### --answer--

La propriété personnalisée n'est plus disponible, car elle est limitée à la portée de la media query.

### --question--

#### --text--

Comment se comporterait la déclaration suivante ?

```css
color: var(--main-color, var(--fallback-color, #000));
```

#### --distractors--

Elle appliquera `--main-color` même si elle n'est pas définie, en utilisant cette valeur par défaut.

---

Elle appliquera `--main-color` et `--fallback-color` simultanément, ce qui donnera un mélange des deux couleurs.

---

Elle utilisera toujours `#000` par défaut, que `--main-color` ou `--fallback-color` soient définies ou non.

#### --answer--

Elle applique `--main-color` si elle est définie ; sinon, elle vérifie `--fallback-color;` et si les deux sont indéfinies, elle utilise `#000`.

### --question--

#### --text--

Quel est un avantage de `@property` en CSS ?

#### --distractors--

Elle améliore les performances en précalculant les valeurs des propriétés personnalisées.

---

Elle préfixe automatiquement les propriétés personnalisées pour une meilleure compatibilité navigateur.

---

Elle limite l'utilisation des propriétés personnalisées à des éléments précis.

#### --answer--

Elle permet d'animer les propriétés personnalisées.

### --question--

#### --text--

Que contrôle la propriété `inherits` dans une déclaration `@property` personnalisée ?

#### --distractors--

Si la propriété personnalisée aura une valeur initiale.

---

Si la propriété peut avoir une version raccourcie.

---

Si la propriété accepte des valeurs de fallback.

#### --answer--

Si la valeur de la propriété personnalisée est transmise aux éléments enfants.

### --question--

#### --text--

Dans la déclaration d'une `@property` personnalisée, que précise `initial-value` ?

#### --distractors--

Les valeurs acceptables que la propriété peut recevoir.

---

La priorité de la propriété dans la cascade.

---

Le type de valeur que la propriété doit avoir.

#### --answer--

La valeur de fallback de la propriété.

### --question--

#### --text--

Avec le code HTML et CSS suivant, quelle sera la valeur de la propriété `color` pour l'élément `.box` ?

```html
<div class="container">
  <div class="box">Text</div>
</div>
```

```css
:root {
  --main-color: red;
}

.container {
  --main-color: blue;
}

.box {
  color: var(--main-color, black);
}
```

#### --distractors--

`black`

---

`red`

---

`green`

#### --answer--

`blue`

### --question--

#### --text--

À quelle propriété CSS faut-il appliquer un dégradé ?

#### --distractors--

`color`

---

`border-radius`

---

`box-shadow`

#### --answer--

`background`

### --question--

#### --text--

À quoi servent les arrêts de couleur dans les dégradés CSS ?

#### --distractors--

À définir le niveau de transparence du dégradé.

---

À préciser la direction du dégradé.

---

À répéter le dégradé avec un motif fixe.

#### --answer--

À définir les points précis où les couleurs changent dans le dégradé.

### --question--

#### --text--

Que se passe-t-il si aucun angle ou aucune direction n'est précisé dans un dégradé linéaire CSS ?

#### --distractors--

Le dégradé utilise par défaut une direction diagonale.

---

Le dégradé va par défaut du bas vers le haut.

---

Le dégradé va par défaut de gauche à droite.

#### --answer--

Le dégradé va par défaut du haut vers le bas.

### --question--

#### --text--

Quelle fonction de dégradé CSS permet de créer un dégradé qui rayonne vers l'extérieur depuis un point central ?

#### --distractors--

`linear-gradient()`

---

`conic-gradient()`

---

`repeating-linear-gradient()`

#### --answer--

`radial-gradient()`

## --quiz--

### --question--

#### --text--

Laquelle des options suivantes est une façon correcte de déclarer une propriété personnalisée CSS ?

#### --distractors--

`background-color: var(--blue);`

---

`custom-property: blue;`

---

`define --my-color: blue;`

#### --answer--

`--my-color: blue;`

### --question--

#### --text--

Quel sélecteur est généralement utilisé pour définir des propriétés personnalisées CSS globales ?

#### --distractors--

`.global {}`

---

`* {}`

---

`body {}`

#### --answer--

`:root {}`

### --question--

#### --text--

Quand tu utilises `var()`, pourquoi est-il recommandé d'inclure une valeur de fallback ?

#### --distractors--

Cela garantit que la variable s'animera correctement.

---

Cela évite de charger des feuilles de style externes.

---

Cela évite un reflow du navigateur.

#### --answer--

Cela garantit une valeur valide si la propriété personnalisée est indéfinie.

### --question--

#### --text--

Quelle est la syntaxe correcte pour appliquer une propriété personnalisée comme couleur d'arrière-plan ?

#### --distractors--

`background: get(--main-bg);`

---

`background: css(--main-bg);`

---

`background: --main-bg;`

#### --answer--

`background: var(--main-bg);`

### --question--

#### --text--

Quelle règle CSS permet aux développeurs de définir des propriétés personnalisées avec un plus grand contrôle sur leur comportement ?

#### --distractors--

`@media`

---

`@keyframes`

---

`@supports`

#### --answer--

`@property`

### --question--

#### --text--

Que contrôle le champ `inherits` dans une définition `@property` ?

#### --distractors--

Si la propriété est utilisée en JavaScript.

---

Si la propriété déclenchera des repaint.

---

Si la propriété peut contenir des fonctions.

#### --answer--

Si la valeur de la propriété est transmise aux éléments enfants.

### --question--

#### --text--

Quand tu définis un angle de dégradé animé avec `@property`, quelle syntaxe dois-tu utiliser ?

#### --distractors--

`"<number>"`

---

`"<color>"`

---

`"<string>"`

#### --answer--

`"<angle>"`

### --question--

#### --text--

Qu'est-ce qui distingue une propriété personnalisée CSS d'une propriété CSS standard dans sa façon d'être définie ?

#### --distractors--

Les propriétés CSS standard sont définies avec un seul tiret, tandis que les propriétés personnalisées utilisent deux tirets.

---

Les propriétés CSS standard utilisent `var()` pour leur définition, contrairement aux propriétés personnalisées.

---

Les propriétés CSS standard sont définies avec la règle `@property`, contrairement aux propriétés personnalisées.

#### --answer--

Les propriétés personnalisées doivent commencer par deux tirets (`--`), contrairement aux propriétés CSS standard.

### --question--

#### --text--

Lequel des éléments suivants est un avantage des propriétés personnalisées CSS ?

#### --distractors--

Elles réduisent le besoin de commentaires CSS.

---

Elles optimisent automatiquement les images.

---

Elles rendent les variables JavaScript obsolètes.

#### --answer--

Elles permettent de réutiliser et maintenir les styles de façon centralisée.

### --question--

#### --text--

Quelle propriété CSS peut changer dynamiquement selon les media queries avec des propriétés personnalisées ?

#### --distractors--

Uniquement `background-color`

---

Uniquement `font-family`

---

Uniquement `z-index`

#### --answer--

Toute propriété qui accepte une valeur.

### --question--

#### --text--

Dans le contexte des variables CSS, quel est le rôle de `initial-value` dans une règle `@property` ?

#### --distractors--

Elle définit la valeur minimale de la propriété.

---

Elle définit la valeur maximale pour les animations.

---

Elle change la priorité du sélecteur.

#### --answer--

Elle assigne une valeur par défaut à la propriété si aucune valeur n'est définie.

### --question--

#### --text--

Avec le code HTML et CSS suivant, quelle couleur d'arrière-plan sera appliquée à l'élément `.card` ?

```html
<div class="dark-theme">
  <div class="card">Content</div>
</div>
```

```css
:root {
  --bg-color: white;
}

.dark-theme {
  --bg-color: #333;
}

.card {
  background: var(--bg-color);
}
```

#### --distractors--

`white`

---

`transparent`

---

`inherit`

#### --answer--

`#333`

### --question--

#### --text--

Dans le CSS suivant, que se passe-t-il si un utilisateur essaie d'assigner une valeur invalide à `--padding` (par exemple, une couleur au lieu d'une longueur) ?

```css
@property --padding {
  syntax: "<length>";
  initial-value: 0px;
  inherits: false;
}
```

#### --distractors--

Le navigateur accepte la valeur mais l'ignore pendant la mise en page.

---

Le navigateur lance une erreur d'exécution.

---

Le navigateur convertit automatiquement la valeur en longueur valide.

#### --answer--

Le navigateur se rabat sur la valeur initiale de la propriété.

### --question--

#### --text--

À quoi sert la propriété personnalisée `--gradient-angle` dans cet exemple ?

```css
@property --gradient-angle {
  syntax: "<angle>";
  inherits: false;
  initial-value: 0deg;
}

.gradient {
  width: 100px;
  height: 100px;
  background: linear-gradient(var(--gradient-angle), red, blue);
  transition: --gradient-angle 0.5s;
}

.gradient:hover {
  --gradient-angle: 90deg;
}
```

#### --distractors--

Elle définit la vitesse de transition du dégradé.

---

Elle précise la taille du dégradé.

---

Elle définit le mode de fusion des couleurs.

#### --answer--

Elle contrôle la direction du dégradé.

### --question--

#### --text--

Quel scénario montre le mieux l'avantage d'utiliser des propriétés personnalisées pour les thèmes ?

#### --distractors--

Les utiliser seulement pour les tailles de police.

---

Les appliquer uniquement sur `:hover`.

---

Les limiter à une seule classe CSS.

#### --answer--

Changer les valeurs via une classe.

### --question--

#### --text--

Pourquoi les développeurs préfèrent-ils souvent utiliser `:root` pour définir les variables CSS ?

#### --distractors--

Parce que `:root` améliore la vitesse de chargement des pages.

---

Parce que `:root` désactive la spécificité.

---

Parce que `:root` est requis pour JavaScript.

#### --answer--

Parce que `:root` permet de donner une portée globale aux propriétés.

### --question--

#### --text--

Que ferait le CSS suivant si `--secondary-color` est indéfinie ?

```css
h1 {
  color: var(--secondary-color, orange);
}
```

#### --distractors--

Il appliquera `black`.

---

Il ignorera la propriété `color`.

---

Il appliquera `--secondary-color`.

#### --answer--

Il appliquera `orange`.

### --question--

#### --text--

Quel type de valeurs `<color>` représente-t-il dans un champ `syntax` de `@property` ?

#### --distractors--

Des angles comme `90deg`.

---

Des longueurs comme `10px`.

---

Des pourcentages comme `50%`.

#### --answer--

Des valeurs de couleur comme `#ff0000` ou `red`.

### --question--

#### --text--

Quel est le principal avantage d'utiliser des propriétés personnalisées avec `@property` ?

#### --distractors--

Elles évitent d'avoir besoin de valeurs de fallback.

---

Elles forcent une mise en page statique.

---

Elles réduisent le besoin de classes.

#### --answer--

Elles permettent l'animation des propriétés.

### --question--

#### --text--

Que fait `var(--undefined-property, fallback)` quand la propriété personnalisée n'est pas définie ?

#### --distractors--

Lance une erreur et arrête d'appliquer les styles.

---

Ignore toute la règle CSS.

---

Applique le nom de la variable tel quel.

#### --answer--

Applique la valeur de fallback à la place.
