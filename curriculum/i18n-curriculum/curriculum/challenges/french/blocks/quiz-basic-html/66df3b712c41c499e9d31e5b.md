---
id: 66df3b712c41c499e9d31e5b
title: Quiz HTML de base
challengeType: 8
dashedName: quiz-basic-html
---

# --description--

Pour réussir le quiz, tu dois répondre correctement à au moins 18 des 20 questions ci-dessous.

# --quizzes--

## --quiz--

### --question--

#### --text--

Comment fonctionne l'attribut `loop` à l'intérieur de l'élément `audio` ?

#### --distractors--

Il permet à l'élément `audio` de synchroniser la lecture avec d'autres éléments multimédias de la page web.

---

Il ajuste la hauteur et la tonalité de l'audio.

---

Il déclenche l'élément `audio` pour arrêter et redémarrer la lecture dans un ordre aléatoire.

#### --answer--

Il relance l'audio depuis le début une fois qu'il est terminé.

### --question--

#### --text--

Qu'est-ce qu'un élément vide en HTML ?

#### --distractors--

Un élément utilisé pour intégrer des vidéos à l'écran.

---

Un élément pour afficher des listes.

---

Un élément utilisé pour intégrer du son dans le document.

#### --answer--

Un élément sans contenu ni balise de fermeture.

### --question--

#### --text--

Laquelle des valeurs suivantes est correcte pour l'attribut `id` ?

#### --distractors--

`id="main@content"`

---

`id="main content"`

---

`id="main~content"`

#### --answer--

`id="main-content"`

### --question--

#### --text--

Comment fonctionne `target="_parent"` ?

#### --distractors--

Il ouvre un lien dans un nouveau contexte de navigation.

---

Il indique la position dans le document lié vers laquelle le navigateur doit défiler après avoir cliqué sur le lien.

---

Il redirige l'utilisateur vers un autre site web précisé dans l'attribut `href`.

#### --answer--

Il ouvre un lien dans le parent du contexte courant.

### --question--

#### --text--

Quelle est la différence entre un attribut booléen et un attribut normal ?

#### --distractors--

Les attributs booléens ne peuvent être utilisés qu'avec les cases à cocher, alors que les attributs normaux sont utilisés avec les boutons radio.

---

Les attributs normaux sont toujours obligatoires pour les images, alors que les attributs booléens sont facultatifs.

---

Les attributs booléens doivent toujours avoir une valeur assignée, alors que les attributs normaux n'en ont pas besoin.

#### --answer--

Un attribut booléen peut être présent ou absent, alors qu'un attribut normal a toujours une valeur précisée.

### --question--

#### --text--

Quel élément est utilisé pour lier des scripts à ton fichier HTML ?

#### --distractors--

`img`

---

`ul`

---

`h2`

#### --answer--

`script`

### --question--

#### --text--

Qu'est-ce qu'un boilerplate HTML ?

#### --distractors--

Un outil spécial utilisé par les développeurs web pour ajouter des filigranes aux documents.

---

Un type de langage de balisage.

---

Une extension qui génère automatiquement du texte « lorem ipsum » sur la page.

#### --answer--

Un modèle qui contient toutes les informations essentielles nécessaires à un document HTML.

### --question--

#### --text--

Quel attribut applique un identifiant unique à un élément HTML ?

#### --distractors--

`class`

---

`href`

---

`action`

#### --answer--

`id`

### --question--

#### --text--

Qu'est-ce qu'une entité HTML (référence de caractère) ?

#### --distractors--

Une représentation graphique d'un caractère en HTML.

---

Une balise de formatage spéciale utilisée en HTML.

---

Une image utilisée pour représenter un caractère en design web.

#### --answer--

Un ensemble de caractères utilisé pour représenter un caractère réservé en HTML.

### --question--

#### --text--

Quel est le rôle de l'élément `audio` ?

#### --distractors--

Il permet aux utilisateurs d'enregistrer leur voix directement sur une page web.

---

Il est utilisé pour styliser les éléments `audio` et `video` sur la page.

---

Il est utilisé pour rendre le texte d'une page web audible quand on clique dessus.

#### --answer--

Il est utilisé pour ajouter de l'audio au document HTML.

### --question--

#### --text--

Lequel des suivants n'est PAS un exemple d'élément HTML ?

#### --distractors--

`img`

---

`h1`

---

`link`

#### --answer--

`byte`

### --question--

#### --text--

Quel est le rôle de l'attribut `target` à l'intérieur des éléments d'ancrage ?

#### --distractors--

Il définit la couleur du lien quand l'utilisateur le survole.

---

Il indique la position dans le document lié vers laquelle le navigateur doit défiler après avoir cliqué sur le lien.

---

Il détermine la taille de la police du texte d'ancrage à l'intérieur de l'élément `link`.

#### --answer--

Il est utilisé pour indiquer où le document lié sera ouvert quand l'utilisateur clique sur le lien.

### --question--

#### --text--

Qu'est-ce qu'un chemin absolu ?

#### --distractors--

Un chemin qui est relatif au fichier courant.

---

Un chemin qui contient des variables.

---

Un chemin qui contient des caractères jokers.

#### --answer--

Un chemin complet vers une ressource, à partir du répertoire racine.

### --question--

#### --text--

Quel est le rôle de l'élément `link` en HTML ?

#### --distractors--

Il est utilisé pour créer des hyperliens à l'intérieur de la même page web.

---

Il est utilisé pour définir la disposition et la structure d'une page web.

---

Il est utilisé pour intégrer des fichiers audio ou vidéo dans une page web.

#### --answer--

Il est utilisé pour lier à des ressources externes comme des feuilles de style et des icônes de site.

### --question--

#### --text--

Laquelle des valeurs suivantes est une valeur valide pour les propriétés Open Graph ?

#### --distractors--

`"og:socialMedia"`

---

`"og:mediaOG"`

---

`"og:openGraph"`

#### --answer--

`"og:title"`

### --question--

#### --text--

Lequel des suivants est un exemple d'attribut booléen ?

#### --distractors--

`href`

---

`src`

---

`width`

#### --answer--

`checked`

### --question--

#### --text--

Laquelle des syntaxes suivantes est correcte pour l'élément `img` ?

#### --distractors--

`<img src="image.jpg" alt="Description of the image"></img>`

---

`<img source="image.jpg" desc="Description of the image"></img>`

---

`<img src="image.jpg" desc="Description of the image">`

#### --answer--

`<img src="image.jpg" alt="Description of the image">`

### --question--

#### --text--

À quoi sert l'élément `iframe` ?

#### --distractors--

Il est utilisé pour ajouter des légendes aux images.

---

Il est utilisé pour ajouter des informations de droits d'auteur pour les médias.

---

Il est utilisé pour créer des hyperliens à l'intérieur de la même page web.

#### --answer--

Il est utilisé pour intégrer un autre document dans le document HTML courant.

### --question--

#### --text--

Lequel des suivants n'est PAS un état de lien valide pour les éléments d'ancrage ?

#### --distractors--

`hover`

---

`visited`

---

`active`

#### --answer--

`href`

### --question--

#### --text--

Quel est le rôle de l'élément `title` ?

#### --distractors--

Un élément qui permet aux utilisateurs d'éditer le code source d'une page web.

---

Il définit la disposition et le positionnement des éléments dans une page web.

---

Il est utilisé pour intégrer des vidéos et du contenu multimédia directement dans un document HTML.

#### --answer--

Il indique le titre d'un document et apparaît dans l'onglet ou la fenêtre du navigateur.

## --quiz--

### --question--

#### --text--

Lequel des attributs `target` suivants ouvre un lien dans le parent du contexte courant ?

#### --distractors--

`_blank`

---

`_self`

---

`_unfencedTop`

#### --answer--

`_parent`

### --question--

#### --text--

Quel attribut est utilisé pour définir l'encodage de caractères `UTF-8` pour un document HTML ?

#### --distractors--

`accept`

---

`capture`

---

`enctype`

#### --answer--

`charset`

### --question--

#### --text--

Quel est le rôle du HTML sur le web ?

#### --distractors--

Le HTML est utilisé pour le style et la mise en page.

---

Le HTML est utilisé pour ajouter de l'interactivité sur la page.

---

Le HTML est un registre de paquets logiciels.

#### --answer--

Le HTML représente le contenu et la structure d'une page web.

### --question--

#### --text--

Quelle licence d'image fonctionne pour le domaine public ?

#### --distractors--

JPG

---

Creative Commons.

---

BSD

#### --answer--

Creative Commons 0.

### --question--

#### --text--

Comment définis-tu un attribut booléen à `false` ?

#### --distractors--

En attribuant la valeur `0` à l'attribut.

---

En utilisant l'opérateur `not` devant le nom de l'attribut.

---

En attribuant à l'attribut une chaîne vide (`""`).

#### --answer--

En omettant l'attribut sur l'élément.

### --question--

#### --text--

À quoi sert l'élément `script` ?

#### --distractors--

Il est utilisé pour intégrer du CSS dans le document HTML ou pour lier à un fichier CSS externe.

---

Il est utilisé pour intégrer du code Pascal dans le document HTML.

---

Il est utilisé pour intégrer du code C# dans le document HTML.

#### --answer--

Il est utilisé pour intégrer du JavaScript dans le document HTML ou pour lier à un fichier JavaScript externe.

### --question--

#### --text--

Lequel des éléments suivants est utilisé pour définir le titre d'un document HTML ?

#### --distractors--

`footer`

---

`section`

---

`figcaption`

#### --answer--

`title`

### --question--

#### --text--

À quoi sert généralement l'attribut `class` ?

#### --distractors--

Il est utilisé pour intégrer des métadonnées sur l'élément.

---

Il est utilisé pour définir des styles en ligne directement dans la balise HTML.

---

Il est utilisé pour préciser des identifiants uniques pour les fonctions JavaScript.

#### --answer--

Il est utilisé pour appliquer un ensemble de styles à plusieurs éléments.

### --question--

#### --text--

Lequel des suivants n'est PAS un exemple d'entité HTML couramment utilisée ?

#### --distractors--

`&quot;` (Guillemet double).

---

`&copy;` (Symbole de droit d'auteur).

---

`&gt;` (Symbole supérieur à).

#### --answer--

`&div;` (élément div).

### --question--

#### --text--

Quel élément est utilisé pour ajouter de l'audio à ton document HTML ?

#### --distractors--

`media`

---

`video`

---

`hr`

#### --answer--

`audio`

### --question--

#### --text--

Où le contenu affiché sur la page est-il placé dans un document HTML ?

#### --distractors--

Entre les éléments `head` et `body`.

---

À l'intérieur de l'élément `repr`.

---

À l'intérieur de l'élément `script`.

#### --answer--

À l'intérieur de l'élément `body`.

### --question--

#### --text--

Quel attribut est utilisé pour relancer l'audio une fois qu'il est terminé ?

#### --distractors--

`multiple`

---

`inputmode`

---

`enctype`

#### --answer--

`loop`

### --question--

#### --text--

Laquelle des syntaxes suivantes est correcte pour un élément `div` ?

#### --distractors--

`<<div>>block container<</div>>`

---

`>>div>>block container>>div>>`

---

`[div]block container[/div]`

#### --answer--

`<div>block container</div>`

### --question--

#### --text--

Comment fonctionne `target="_self"` ?

#### --distractors--

Il indique la position dans le document lié vers laquelle le navigateur doit défiler après avoir cliqué sur le lien.

---

Il télécharge automatiquement le document lié sur l'ordinateur de l'utilisateur.

---

Il redirige l'utilisateur vers un autre site web précisé dans l'attribut `href`.

#### --answer--

Il ouvre un lien dans le contexte de navigation courant.

### --question--

#### --text--

Qu'est-ce qu'un chemin relatif ?

#### --distractors--

Il indique l'emplacement exact d'un fichier ou d'un répertoire depuis le répertoire racine du système de fichiers.

---

Il inclut l'URL complète depuis le nom de domaine jusqu'au fichier ou répertoire précis.

---

Il commence toujours par une barre oblique (`/`) qui indique le répertoire racine du site web ou du système de fichiers.

#### --answer--

Il indique l'emplacement d'un fichier ou d'un répertoire relativement au répertoire de travail courant.

### --question--

#### --text--

Laquelle des syntaxes suivantes est correcte pour un élément `link` ?

#### --distractors--

`<link rel="stylesheet" href="./styles.css"></link>`

---

`</link rel="stylesheet" href="./styles.css"></link>`

---

`<<link rel="stylesheet" href="./styles.css"></link>>`

#### --answer--

`<link rel="stylesheet" href="./styles.css" />`

### --question--

#### --text--

Lequel des suivants n'est PAS une valeur valide pour les propriétés Open Graph ?

#### --distractors--

`"og:title"`

---

`"og:type"`

---

`"og:image"`

#### --answer--

`"og:socialMedia"`

### --question--

#### --text--

Laquelle des syntaxes suivantes est correcte pour un élément paragraphe ?

#### --distractors--

`>p>paragraph element>/p>`

---

`<<p>>paragraph element<</p>>`

---

`<p>`

#### --answer--

`<p>paragraph element</p>`

### --question--

#### --text--

Que signifie le nom de l'élément `iframe` ?

#### --distractors--

Inline Framing

---

Inner Frame

---

Inline video

#### --answer--

Inline Frame

### --question--

#### --text--

Que signifie l'état actif pour les éléments d'ancrage ?

#### --distractors--

Le lien est désactivé et ne peut pas être cliqué.

---

Le curseur de la souris est au-dessus du lien.

---

Le lien a été visité par l'utilisateur.

#### --answer--

Un utilisateur est en train de cliquer dessus.

## --quiz--

### --question--

#### --text--

Lequel des suivants ouvrira le lien dans un nouvel onglet du navigateur ?

#### --distractors--

```html
<a target="_parent" href="https://freecatphotoapp.com">cat photos</a>
```

---

```html
<a target="_top" href="https://freecatphotoapp.com">cat photos</a>
```

---

```html
<a target="_self" href="https://freecatphotoapp.com">cat photos</a>
```

#### --answer--

```html
<a target="_blank" href="https://freecatphotoapp.com">cat photos</a>
```

### --question--

#### --text--

Lequel des suivants est un élément vide ?

#### --distractors--

`div`

---

`section`

---

`p`

#### --answer--

`img`

### --question--

#### --text--

Laquelle des syntaxes suivantes est la bonne façon d'utiliser les entités HTML ?

#### --distractors--

`<p>This is an &lt&img /&gt& element</p>`

---

`<p>This is an ;lt&img /;gt& element</p>`

---

`<p>This is an ;lt;img /;gt; element</p>`

#### --answer--

`<p>This is an &lt;img /&gt; element</p>`

### --question--

#### --text--

À quoi sert l'élément `ul` en HTML ?

#### --distractors--

Il est utilisé pour regrouper une liste d'images uniquement.

---

Il est utilisé pour regrouper une liste de liens uniquement.

---

Il est utilisé pour regrouper une liste ordonnée d'éléments.

#### --answer--

Il est utilisé pour regrouper une liste à puces d'éléments.

### --question--

#### --text--

Laquelle des syntaxes suivantes est la bonne façon d'appliquer plusieurs classes à un élément HTML ?

#### --distractors--

```html
<div class="box>>red>>box"></div>
``` 

---

```html
<div class="box<<red<<box"></div>
``` 

---

```html
<div class="box-red-box"></div>
``` 

#### --answer--

```html
<div class="box red-box"></div>
``` 

### --question--

#### --text--

Lequel des éléments suivants est utilisé pour représenter une liste ordonnée d'éléments ?

#### --distractors--

`span`

---

`div`

---

`ul`

#### --answer--

`ol` 

### --question--

#### --text--

À quoi servent les propriétés Open Graph ?

#### --distractors--

Ces propriétés sont utilisées pour intégrer du contenu multimédia interactif directement dans les pages web.

---

Ces propriétés sont utilisées pour générer des publicités pop-up dynamiques sur les sites web.

---

Ces propriétés sont utilisées pour chiffrer les données sensibles transmises entre les serveurs web et les navigateurs des utilisateurs.

#### --answer--

Ces propriétés sont utilisées pour définir comment le contenu d'un site web sera vu sur les différentes plateformes de réseaux sociaux. 

### --question--

#### --text--

Lequel des suivants est utilisé pour définir une courte description pour une page web ?

#### --distractors--

```html
<meta
  add="description"
  content="Discover expert tips and techniques for gardening in small spaces, choosing the right plants, and maintaining a thriving garden."
/>
``` 

---

```html
<meta
  set="description"
  content="Discover expert tips and techniques for gardening in small spaces, choosing the right plants, and maintaining a thriving garden."
/>
``` 

---

```html
<meta
  description="description"
  content="Discover expert tips and techniques for gardening in small spaces, choosing the right plants, and maintaining a thriving garden."
/>
``` 

#### --answer--

```html
<meta
  name="description"
  content="Discover expert tips and techniques for gardening in small spaces, choosing the right plants, and maintaining a thriving garden."
/>
``` 

### --question--

#### --text--

Que signifie SVG ?

#### --distractors--

Scalable Vector Graph

---

Scalable Vistor Graphics

---

Site Vector Graphics

#### --answer--

Scalable Vector Graphics 

### --question--

#### --text--

Quel est le rôle de l'élément `footer` ?

#### --distractors--

Cet élément est utilisé pour regrouper des légendes et des images sur une page web. 

---

Cet élément est utilisé pour créer des lignes horizontales entre les paragraphes sur la page.

---

Cet élément est utilisé pour créer une liste d'éléments ordonnés en bas de la page. 

#### --answer--

Cet élément est utilisé pour regrouper du contenu de droits d'auteur ou des documents associés en bas de la page. 

### --question--

#### --text--

Qu'est-ce que l'encodage de caractères `UTF-8` ?

#### --distractors--

C'est un encodage de caractères standardisé utilisé pour définir le titre d'une page web.

---

C'est un ensemble de caractères utilisé pour définir la description d'une page web.

---

C'est un ensemble spécial de caractères utilisé dans les éléments pour améliorer les performances des médias. 

#### --answer--

C'est un encodage de caractères standardisé largement utilisé sur le web.  

### --question--

#### --text--

Lequel des suivants est un exemple d'élément remplacé ?

#### --distractors--

`slot`

---

`div`

---

`p`

#### --answer--

`iframe` 

### --question--

#### --text--

Lequel des suivants est considéré comme le titre le plus important d'une page web ?

#### --distractors--

`h6`

---

`h3`

---

`h5`

#### --answer--

`h1` 

### --question--

#### --text--

Lequel des éléments suivants est utilisé pour lier à des ressources externes comme des feuilles de style et des icônes de site ?

#### --distractors--

`a`

---

`p`

---

`div`

#### --answer--

`link` 

### --question--

#### --text--

Lequel des éléments suivants est utilisé pour transmettre un sentiment d'urgence, de sérieux ou d'importance forte ?

#### --distractors--

`important`

---

`heavy`

---

`strength`

#### --answer--

`strong`

### --question--

#### --text--

Quel est le rôle de l'élément `figcaption` ?

#### --distractors--

Il fournit des légendes pour le contenu à l'intérieur d'un élément `span`. 

---

Il fournit des légendes pour le contenu à l'intérieur d'un élément `slot`. 

---

Il fournit des légendes pour le contenu à l'intérieur d'un élément `div`. 

#### --answer--

Il fournit des légendes pour le contenu à l'intérieur d'un élément `figure`. 

### --question--

#### --text--

Laquelle des syntaxes suivantes est la bonne façon de définir la déclaration `DOCTYPE` pour un document HTML ?

#### --distractors--

`<DOCTYPE>`

---

`<*DOCTYPE html>`

---

`<<DOCTYPE html>>`

#### --answer--

`<!DOCTYPE html>`

### --question--

#### --text--

Quel est le rôle de l'élément `head` dans un document HTML ?

#### --distractors--

C'est un conteneur d'éléments d'ancrage pour le document HTML. 

---

C'est un conteneur d'images pour le document HTML. 

---

C'est un conteneur d'éléments de titre pour le document HTML. 

#### --answer--

C'est un conteneur de métadonnées pour le document HTML. 

### --question--

#### --text--

Laquelle des syntaxes suivantes est la bonne façon de définir la langue du document HTML en anglais ?

#### --distractors--

`<html lang-set="en">` 

---

`<html set-lang="en">` 

---

`<html language="en">` 

#### --answer--

`<html lang="en">` 

### --question--

#### --text--

Lequel des éléments suivants ne devrait PAS être placé à l'intérieur du `head` ?

#### --distractors--

`link`

---

`meta`

---

`title`

#### --answer--

`img` 

## --quiz--

### --question--

#### --text--

Laquelle des affirmations suivantes concernant les chemins absolus et relatifs n'est PAS correcte ?

#### --distractors--

Les chemins absolus partent du répertoire racine, alors que les chemins relatifs dépendent de l'emplacement du fichier courant.

---

Les chemins relatifs sont plus courts et plus souples pour lier des fichiers dans un site web, alors que les chemins absolus contiennent l'URL complète.

---

Les chemins absolus et relatifs peuvent tous deux être utilisés pour lier des fichiers du même site web.

#### --answer--

Les chemins relatifs ne peuvent pas utiliser `..` pour remonter dans les répertoires, mais les chemins absolus le peuvent.

### --question--

#### --text--

Laquelle des valeurs suivantes n'est PAS une valeur valide pour l'attribut target dans une balise `a` ?

#### --distractors--

`_self`

---

`_parent`

---

`_top`

#### --answer--

`_main`

### --question--

#### --text--

Quel est le rôle de l'attribut `controls` dans l'élément `audio` ?

#### --distractors--

Il définit le format du fichier audio que le navigateur doit utiliser.

---

Il démarre automatiquement l'audio au chargement de la page.

---

Il indique l'URL de la source du fichier audio.

#### --answer--

Il ajoute des contrôles de lecture intégrés comme lecture, pause et réglage du volume.

### --question--

#### --text--

Lequel des éléments suivants améliore le SEO en fournissant une brève description de la page web ?

#### --distractors--

```html
<meta
 description="describe"
 content="Discover expert tips and techniques for gardening in small spaces, choosing the right plants, and maintaining a thriving garden."
/>
```

---

```html
<meta
 name="desc"
 content="Discover expert tips and techniques for gardening in small spaces, choosing the right plants, and maintaining a thriving garden."
/>
```

---

```html
<meta
 description="description"
 content="Discover expert tips and techniques for gardening in small spaces, choosing the right plants, and maintaining a thriving garden."
/>
```

#### --answer--

```html
<meta
 name="description"
 content="Discover expert tips and techniques for gardening in small spaces, choosing the right plants, and maintaining a thriving garden."
/>
```

### --question--

#### --text--

Que fait `<meta charset="utf-8" />` ?

#### --distractors--

Il définit la langue de la page web en anglais.

---

Il lie une feuille de style CSS externe.

---

Il indique le titre de la page web.

#### --answer--

Il définit l'encodage de caractères pour garantir un affichage correct du texte.

### --question--

#### --text--

Quelle est la différence entre les éléments `link` et `script` en HTML ?

#### --distractors--

`link` est pour les fichiers JavaScript, et `script` est pour les fichiers CSS.

---

`link` et `script` sont tous deux utilisés pour styliser les pages web.

---

`script` sert à ajouter des styles, et `link` sert à exécuter du JavaScript.

#### --answer--

`link` est utilisé pour ajouter des styles depuis un fichier CSS externe, alors que `script` est utilisé pour exécuter du code JavaScript.

### --question--

#### --text--

Laquelle des affirmations suivantes sur les classes et les ID HTML est incorrecte ?

#### --distractors--

Un nom de classe peut être utilisé plusieurs fois dans un document HTML.

---

L'attribut `rel` définit la relation entre la ressource liée et le document HTML.

---

La propriété `og:image` est utilisée pour préciser l'image affichée dans les publications sur les réseaux sociaux.

#### --answer--

Les noms d'ID ne peuvent pas contenir de tiret bas et doivent être uniques.

### --question--

#### --text--

Laquelle des affirmations suivantes sur les éléments de titre HTML est vraie ?

#### --distractors--

La taille du texte augmente de `h1` à `h6`.

---

La taille du texte reste la même pour tous les éléments de titre.

---

`h3` est plus grand que `h2`.

#### --answer--

La taille du texte diminue de `h1` à `h6`.

### --question--

#### --text--

Quelle affirmation est incorrecte concernant les éléments HTML ?

#### --distractors--

Les éléments HTML sont les blocs de construction de base d'une page web.

---

Certaines bases de code incluent une barre oblique (/) à l'intérieur des éléments vides.

---

Un attribut booléen est un attribut qui fonctionne simplement en étant présent ou absent dans une balise.

#### --answer--

Les éléments vides contiennent du contenu et n'ont qu'une balise d'ouverture.

### --question--

#### --text--

Laquelle des affirmations suivantes concernant les éléments vides en HTML est vraie ?

#### --distractors--

Un élément vide doit toujours inclure une barre oblique / avant de se fermer.

---

Un élément vide ne devrait jamais inclure une barre oblique /.

---

La barre oblique / est obligatoire pour les éléments vides auto-fermants dans toutes les versions de HTML.

#### --answer--

`<img>` et `<img/>` sont tous deux acceptables en HTML moderne.

### --question--

#### --text--

Quel est le but principal des commentaires en programmation ?

#### --distractors--

Faire que le code s'exécute plus vite.

---

Changer la façon dont le navigateur affiche le contenu.

---

Stocker des données temporaires dans le code.

#### --answer--

Laisser des notes pour toi et pour les autres développeurs dans le code.

### --question--

#### --text--

Quel attribut target ouvre un lien dans le même contexte de navigation que le courant ?

#### --distractors--

`_blank`

---

`_top`

---

`_parent`

#### --answer--

`_self`

### --question--

#### --text--

Quel est le rôle de l'élément `em` en HTML ?

#### --distractors--

Agrandir le texte.

---

Changer la couleur du texte.

---

Souligner les mots pour les mettre en valeur.

#### --answer--

Mettre en avant un texte important en le rendant italique.

### --question--

#### --text--

Que fait l'élément `strong` en HTML ?

#### --distractors--

Augmenter la taille de la police.

---

Changer la couleur du texte.

---

Souligner le texte.

#### --answer--

Rendre le texte en gras pour montrer son importance.

### --question--

#### --text--

Quelle est la principale différence entre les éléments `figure` et `figcaption` en HTML ?

#### --distractors--

`figure` est utilisé pour les légendes, alors que `figcaption` est utilisé pour regrouper du contenu lié comme des images ou des diagrammes.

---

`figcaption` doit toujours être placé à l'extérieur de l'élément `figure`.

---

Il n'y a pas de différence ; les deux servent au même usage.

#### --answer--

`figure` regroupe du contenu lié comme des images ou des diagrammes, alors que `figcaption` fournit une légende pour ce contenu

### --question--

#### --text--

Pourquoi les entités HTML sont-elles utilisées ?

#### --distractors--

Pour ajouter du style au texte.

---

Pour créer des hyperliens dans une page web.

---

Pour augmenter la taille de police des caractères spéciaux.

#### --answer--

Pour représenter des caractères réservés comme `<` et `&` en HTML.

### --question--

#### --text--

Laquelle des affirmations suivantes concernant les balises Open Graph est correcte ?

#### --distractors--

Les balises Open Graph sont utilisées pour styliser une page web avec du CSS.

---

Les balises Open Graph sont utilisées uniquement pour le SEO et n'ont aucun effet sur les réseaux sociaux.

---

Les balises Open Graph doivent être placées à l'intérieur de la section `body` du HTML.

#### --answer--

La propriété `og:title` définit le titre qui apparaît quand le contenu est partagé sur les réseaux sociaux.

### --question--

#### --text--

Laquelle des affirmations suivantes concernant les formats d'image est incorrecte ?

#### --distractors--

WEBP et AVIF sont meilleurs pour l'optimisation moderne des images.

---

Les images SVG peuvent être mises à l'échelle sans perdre en qualité.

---

La compression aide à réduire la taille des fichiers image.

#### --answer--

PNG et JPG sont les meilleurs formats pour les images optimisées aujourd'hui.

### --question--

#### --text--

Que fait l'attribut `loop` dans l'élément `audio` ?

#### --distractors--

Joue l'audio une seule fois.

---

Arrête l'audio après une lecture.

---

Ralentit la lecture de l'audio.

#### --answer--

Répète l'audio en continu.

### --question--

#### --text--

Laquelle des suivantes décrit le mieux un boilerplate HTML ?

#### --distractors--

Un outil spécial utilisé par les développeurs web pour ajouter des filigranes aux documents.

---

Une extension de navigateur qui accélère le chargement des pages web.

---

Un framework de programmation exclusivement pour les animations en HTML.

#### --answer--

Un modèle prédéfini contenant la structure HTML essentielle pour le développement web.
