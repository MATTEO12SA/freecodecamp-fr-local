---
id: 66edcd0ecb4b25dc64a34804
title: Quiz sur les objets JavaScript
challengeType: 8
dashedName: quiz-javascript-objects
---

# --description--

Pour réussir le quiz, tu dois répondre correctement à au moins 18 des 20 questions ci-dessous.

# --quizzes--

## --quiz--

### --question--

#### --text--

Laquelle des propositions suivantes ne décrit PAS un objet en JavaScript ?

#### --distractors--

Une collection de paires clé-valeur.

---

Un conteneur de propriétés et de méthodes.

---

Un type de données non primitif.

#### --answer--

Un type de données primitif.

### --question--

#### --text--

Laquelle des définitions d'objet suivantes est invalide ?

#### --distractors--

`const book = { "pages": 255 };`

---

`const car = { year: 2025 };`

---

`const person = { "name": "nora" };`

#### --answer--

`const product = { color: blue };`

### --question--

#### --text--

Comment peux-tu accéder à la propriété `job` de cet objet `person` ?

```js
let person = { job: "Software Developer" };
```

#### --distractors--

`person -> job`

---

`person#job`

---

`person[job]`

#### --answer--

`person.job`

### --question--

#### --text--

Comment peux-tu accéder à la propriété `street` de l'objet `person` ci-dessous ?

```js
const person = {
  address: {
    street: "sample-street"
  }
};
```

#### --distractors--

`person -> address -> street`

---

`person#address#street`

---

`person[address.street]`

#### --answer--

`person["address"]["street"]`

### --question--

#### --text--

Laquelle de ces clés de propriété ne peut être accédée qu'avec la notation par crochets ?

#### --distractors--

Une clé de propriété connue et statique.

---

Une clé de propriété imbriquée sur plusieurs niveaux d'un objet.

---

Une clé de propriété qui pourrait ne pas exister dans l'objet.

#### --answer--

Une clé de propriété qui est dynamique.

### --question--

#### --text--

Laquelle des propositions suivantes utilise correctement la déstructuration pour accéder à la propriété `name` de l'objet `person` ci-dessous ?

```js
const person = { name: "John" }
```

#### --distractors--

`const name = person.name;`

---

`const name = person["name"];`

---

`const { ...name } = person;`

#### --answer--

`const { name } = person;`

### --question--

#### --text--

Laquelle des propositions suivantes utilise correctement la déstructuration pour accéder à la propriété `flour` de l'objet `recipe` ci-dessous ?

```js
const recipe = {
  ingredients: {
    flour: "2 cups",
    sugar: "1 cup"
  }
};
```

#### --distractors--

`const flour = recipe.ingredients.flour;`

---

`const flour = recipe["ingredients"]["flour"];`

---

`const { ingredients: flour } = recipe;`

#### --answer--

`const { ingredients: { flour } } = recipe;`

### --question--

#### --text--

Comment peux-tu affecter une valeur par défaut à une clé de propriété lorsque tu utilises la déstructuration en JavaScript ?

#### --distractors--

`const key = object.key && "defaultValue";`

---

`const key = object.key || "defaultValue";`

---

`const { key: "defaultValue" } = object;`

#### --answer--

`const { key = "defaultValue" } = object;`

### --question--

#### --text--

Comment peux-tu vérifier si un objet `car` possède une propriété `year` ?

#### --distractors--

`year.hasProperty("car");`

---

`car.hasProperty("year");`

---

`car.hasOwnProperty(year);`

#### --answer--

`car.hasOwnProperty("year");`

### --question--

#### --text--

Qu'est-ce qu'une méthode d'objet ?

#### --distractors--

Une chaîne associée à un objet.

---

Un tableau associé à un objet.

---

Un nombre associé à un objet.

#### --answer--

Une fonction associée à un objet.

### --question--

#### --text--

Comment peux-tu appeler la méthode `add` de cet objet `calculator` pour additionner les nombres `10` et `7` ?

```js
const calculator = {
  add: function(a, b) {
    return a + b;
  }
};
```

#### --distractors--

`calculator#add(10, 7);`

---

`calculator.call(add(10, 7));`

---

`add.calculator(10, 7);`

#### --answer--

`calculator.add(10, 7);`

### --question--

#### --text--

Quel opérateur supprime une propriété d'un objet ?

#### --distractors--

`remove`

---

`erase`

---

`discard`

#### --answer--

`delete`

### --question--

#### --text--

Comment peux-tu vérifier si une propriété `score` existe dans un objet `player` ?

#### --distractors--

`player in "score"`

---

`player in score`

---

`score in player`

#### --answer--

`"score" in player`

### --question--

#### --text--

Comment peux-tu créer un nouvel objet vide en JavaScript ?

#### --distractors--

`object() new`

---

`new object()`

---

`Object() new`

#### --answer--

`new Object()`

### --question--

#### --text--

Quelle est la syntaxe correcte pour utiliser le chaînage optionnel en JavaScript ?

#### --distractors--

`object?key`

---

`object.["key"]`

---

`object.?key`

#### --answer--

`object?.key`

### --question--

#### --text--

À quoi sert le chaînage optionnel en JavaScript ?

#### --distractors--

Utilisé pour définir des variables avec des valeurs par défaut.

---

Utilisé pour définir des méthodes optionnelles au sein d'un objet qui peuvent ou non être appelées.

---

Utilisé pour écrire un code plus concis en enchaînant plusieurs appels de méthode dans une seule instruction.

#### --answer--

Utilisé pour accéder en toute sécurité aux propriétés d'un objet qui pourrait être `null` ou `undefined`.

### --question--

#### --text--

Quelle méthode est utilisée pour convertir un objet JavaScript en une chaîne JSON ?

#### --distractors--

`Stringify.toJSON()`

---

`JSON.toObject()`

---

`Object.toJSON()`

#### --answer--

`JSON.stringify()`

### --question--

#### --text--

Quelle méthode reconvertit une chaîne JSON en un objet JavaScript ?

#### --distractors--

`JSON.toObject()`

---

`JSON.convert()`

---

`JSON.object()`

#### --answer--

`JSON.parse()`

### --question--

#### --text--

Laquelle de ces options accède à la propriété `color` d'un objet `toy` en utilisant la notation par crochets ?

#### --distractors--

`toy.color`

---

`toy[color]`

---

`color[toy]`

#### --answer--

`toy["color"]`

### --question--

#### --text--

Comment peux-tu accéder au téléphone `home` de cet objet `learner` en JavaScript en utilisant la notation par point ?

```js
const learner = {
  name: "Nora",
  age: 45,
  contact: {
    email: "nora@email.com",
    phone: {
      home: "123-456-7890",
      work: "098-765-4321"
    }
  }
};
```

#### --distractors--

`learner.name.phone.home`

---

`learner.email.work`

---

`learner.contact.home`

#### --answer--

`learner.contact.phone.home`
