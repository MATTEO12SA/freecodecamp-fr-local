---
id: 68376949899fcbad3c9cac23
title: 'Quiz : JavaScript et accessibilité'
challengeType: 8
dashedName: quiz-js-a11y
---

# --description--

Pour réussir le quiz, tu dois répondre correctement à au moins 9 des 10 questions ci-dessous.

# --quizzes--

## --quiz--

### --question--

#### --text--

Quel attribut est utilisé à des fins d'accessibilité pour indiquer si un contrôle est développé ou réduit ?

#### --distractors--

`aria-controlled`

---

`aria-controls`

---

`aria-expands`

#### --answer--

`aria-expanded`

### --question--

#### --text--

Quels deux attributs peuvent être utilisés avec `aria-expanded` pour établir une connexion programmatique entre l'élément de contrôle et l'élément qu'il contrôle ?

#### --distractors--

`aria-labelledBy` et `aria-owns`

---

`aria-controls` et `aria-label`

---

`aria-live` et `aria-owns`

#### --answer--

`aria-controls` et `aria-owns`

### --question--

#### --text--

Lequel des attributs suivants crée une région live sur ta page, qui peut servir à notifier les utilisateurs de lecteurs d'écran des changements dynamiques de contenu dans cette région dès qu'ils se produisent ?

#### --distractors--

`aria-live-regions`

---

`aria-lives`

---

`aria-live-region`

#### --answer--

`aria-live`

### --question--

#### --text--

Lequel des éléments `div` suivants sera désactivé uniquement pour les personnes qui utilisent des technologies d'assistance, comme les lecteurs d'écran ?

#### --distractors--

```html
<div role="button" tabindex="-1" aria-disables="true">Edit</div>
```

---

```html
<div role="button" tabindex="-1" aria-disabling="true">Edit</div>
```

---

```html
<div role="button" tabindex="-1" aria-disable="true">Edit</div>
```

#### --answer--

```html
<div role="button" tabindex="-1" aria-disabled="true">Edit</div>
```

### --question--

#### --text--

Lequel des exemples suivants est une utilisation valide de l'attribut `aria-expanded` ?

#### --distractors--

```html
<button aria-expanded="expand">Menu</button>
```

---

```html
<button aria-expanded="allow">Menu</button>
```

---

```html
<button aria-expanded="set">Menu</button>
```

#### --answer--

```html
<button aria-expanded="true">Menu</button>
```

### --question--

#### --text--

Quel attribut sert à créer une relation programmatique entre un élément de contrôle et l'élément qu'il affecte sur la page ?

#### --distractors--

`aria-set`

---

`aria-region`

---

`aria-expanded`

#### --answer--

`aria-controls`

### --question--

#### --text--

Lequel des éléments suivants N'EST PAS un cas d'usage courant des régions live ?

#### --distractors--

Les messages affichés après qu'une action a été effectuée (comme un message d'erreur ou une confirmation).

---

Le contenu qui se met à jour périodiquement (comme un ticker, un marquee ou un compte à rebours).

---

Les messages d'état généraux (comme les mises à jour sur un processus).

#### --answer--

Les messages affichés avant qu'une action ait été effectuée (comme une boîte de dialogue de confirmation).

### --question--

#### --text--

Lequel des exemples suivants est la bonne façon d'indiquer quel onglet est actuellement sélectionné ?

#### --distractors--

```html
<div role="tablist">
  <button role="tab" aria-selecting="true">Tab 1</button>
  <button role="tab" aria-selecting="false">Tab 2</button>
  <button role="tab" aria-selecting="false">Tab 3</button>
</div>
```

---

```html
<div role="tablist">
  <button role="tab" aria-select="true">Tab 1</button>
  <button role="tab" aria-select="false">Tab 2</button>
  <button role="tab" aria-select="false">Tab 3</button>
</div>
```

---

```html
<div role="tablist">
  <button role="tab" aria-selects="true">Tab 1</button>
  <button role="tab" aria-selects="false">Tab 2</button>
  <button role="tab" aria-selects="false">Tab 3</button>
</div>
```

#### --answer--

```html
<div role="tablist">
  <button role="tab" aria-selected="true">Tab 1</button>
  <button role="tab" aria-selected="false">Tab 2</button>
  <button role="tab" aria-selected="false">Tab 3</button>
</div>
```

### --question--

#### --text--

Laquelle des valeurs suivantes N'EST PAS une valeur valide pour l'attribut `aria-live` ?

#### --distractors--

`off`

---

`polite`

---

`assertive`

#### --answer--

`on`

### --question--

#### --text--

Laquelle des valeurs suivantes de l'attribut `aria-live` signifie que la mise à jour est très importante et a la priorité la plus haute ?

#### --distractors--

`now`

---

`immediate`

---

`urgent`

#### --answer--

`assertive`
