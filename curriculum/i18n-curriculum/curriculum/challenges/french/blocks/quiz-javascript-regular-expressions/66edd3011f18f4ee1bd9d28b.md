---
id: 66edd3011f18f4ee1bd9d28b
title: Quiz sur les expressions régulières JavaScript
challengeType: 8
dashedName: quiz-javascript-regular-expressions
---

# --description--

Pour réussir le quiz, tu dois répondre correctement à au moins 18 des 20 questions ci-dessous.

# --quizzes--

## --quiz--

### --question--

#### --text--

Laquelle des expressions suivantes N'EST PAS une expression régulière valide ?

#### --distractors--

```js
/hello, world!/i
```

---

```js
new RegExp("hello, world!")
```

---

```js
/(?:^|\s)hello[,]?\sworld[!]?(?:$|\s)/i
```

#### --answer--

```js
"hello, world!"
```

### --question--

#### --text--

Quel `RegExp` contient le même motif que l'expression régulière `/f[o0]{2} b[a4@]r/i` ?

#### --distractors--

```js
new RegExp("/f[o0]{2} b[a4@]r/", "i")
```

---

```js
new RegExp("f[o0]{2} b[a4@]r", "g")
```

---

```js
new RegExp("f[o0]{2} b[a4@]r")
```

#### --answer--

```js
new RegExp("f[o0]{2} b[a4@]r", "i")
```

### --question--

#### --text--

Quel est le type de retour de la méthode d'expression régulière `test` ?

#### --distractors--

Un tableau de chaînes qui ont correspondu à l'expression régulière.

---

Un objet contenant des informations sur la (sous-)chaîne à laquelle l'expression régulière a correspondu.

---

`null`, la méthode `test` valide seulement si l'expression régulière donnée est valide.

#### --answer--

Un booléen indiquant si la chaîne correspond à l'expression régulière.

### --question--

#### --text--

Que fait la méthode `match` lorsqu'on lui passe une expression régulière sans aucun drapeau ?

#### --distractors--

Elle cherche toutes les chaînes qui correspondent à l'expression régulière donnée, et renvoie TOUTES les correspondances sous forme de tableau.

---

Elle cherche la première correspondance complète, et renvoie l'indice de début de cette correspondance.

---

Elle cherche la première correspondance complète, et renvoie un booléen indiquant si une correspondance a été trouvée ou non.

#### --answer--

Elle cherche la première correspondance complète, et renvoie un tableau contenant cette première correspondance.

### --question--

#### --text--

Quel est le meilleur cas d'usage pour `test` ?

#### --distractors--

Pour vérifier une expression régulière donnée.

---

Pour obtenir des informations détaillées sur une correspondance de chaîne, comme la longueur de la chaîne correspondante.

---

Pour extraire toutes les correspondances d'une chaîne donnée.

#### --answer--

Pour vérifier si une chaîne donnée correspond à l'expression régulière.

### --question--

#### --text--

Quel est le but d'utiliser `replace` ?

#### --distractors--

Remplacer l'expression régulière actuelle par une nouvelle expression régulière.

---

Lorsqu'on donne un indice et une chaîne, remplacer le caractère à l'indice spécifié par la chaîne fournie.

---

Supprimer toutes les occurrences d'une sous-chaîne correspondante d'une chaîne.

#### --answer--

Remplacer une chaîne correspondante par une chaîne de remplacement donnée.

### --question--

#### --text--

Quelle est la différence entre `match` et `matchAll` ?

#### --distractors--

`match` renvoie toujours une chaîne, tandis que `matchAll` renvoie toujours un tableau de correspondances.

---

`match` renvoie un booléen indiquant si une correspondance est trouvée, tandis que `matchAll` renvoie un nombre représentant le nombre de correspondances trouvées.

---

`match` renvoie un nombre indiquant l'indice de début de la première correspondance trouvée, tandis que `matchAll` renvoie un tableau de nombres contenant les indices où une correspondance a été trouvée.

#### --answer--

`match` renvoie un tableau de chaînes qui correspondent, tandis que `matchAll` renvoie un objet itérateur de toutes les correspondances trouvées.

### --question--

#### --text--

Quel est le bon usage de `replaceAll` ?

#### --distractors--

```js
foo.replaceAll(/foobar/, "fizzbuzz")
```

---

```js
foo.replaceAll(/foobar/i, "fizzbuzz")
```

---

```js
foo.replaceAll(/foobar/gi, /fizzbuzz/)
```

#### --answer--

```js
foo.replaceAll(/foobar/g, "fizzbuzz")
```

### --question--

#### --text--

Laquelle des classes de caractères suivantes est équivalente à l'expression régulière `/[a-zA-Z0-9_]/` ?

#### --distractors--

`\d`

---

`\s`

---

`\n`

#### --answer--

`\w`

### --question--

#### --text--

Laquelle des classes de caractères suivantes est la plus appropriée pour extraire les chiffres d'un numéro de téléphone ?

#### --distractors--

`\w`

---

`\s`

---

`\D`

#### --answer--

`\d`

### --question--

#### --text--

Que se passe-t-il lorsqu'un lookahead `(?=)` est utilisé dans une expression régulière ?

#### --distractors--

Le lookahead affirmera à gauche de la chaîne, en vérifiant que le sous-motif EST présent.

---

Un lookahead est un motif d'expression régulière alternatif utilisé lorsque le motif principal ne correspond pas.

---

Le lookahead affirmera à droite de la chaîne, en vérifiant que le sous-motif N'EST PAS présent.

#### --answer--

Un lookahead affirme à droite de la chaîne, en vérifiant que le sous-motif EST présent.

### --question--

#### --text--

Lors de la création d'une expression régulière, où doit être placé un lookbehind `(?<=)` ?

#### --distractors--

Le lookbehind peut être placé n'importe où dans l'expression régulière.

---

Le lookbehind doit être placé à droite du motif principal de l'expression régulière.

---

Le lookbehind doit inclure le motif principal de l'expression régulière.

#### --answer--

Le lookbehind doit être placé à gauche du motif principal de l'expression régulière.

### --question--

#### --text--

Quel quantificateur correspond à l'élément précédent zéro ou une fois ?

#### --distractors--

`+`

---

`*`

---

`a{0,}`

#### --answer--

`?`

### --question--

#### --text--

Laquelle des expressions régulières suivantes n'autorise que les nombres entre 0 et 999 999 ?

#### --distractors--

`/^\d{6}$/`

---

`/^\d{0, 999999}$/`

---

`/^\d+$/`

#### --answer--

`/^\d{1,6}$/`

### --question--

#### --text--

Laquelle des affirmations suivantes est vraie à propos de la classe de caractères personnalisée (`[]`) ?

#### --distractors--

C'est un ensemble de caractères à retirer de la correspondance.

---

Elle peut définir un ensemble de caractères à faire correspondre sans avoir besoin d'échapper aucun caractère spécial.

---

Elle représente un ensemble de caractères sous forme Unicode.

#### --answer--

Elle peut définir un ensemble personnalisé de caractères à faire correspondre.

### --question--

#### --text--

Laquelle des classes de caractères suivantes correspond correctement à l'alphabet en majuscules ?

#### --distractors--

`\w`

---

`[a-z]`

---

`[AZ]`

#### --answer--

`[A-Z]`

### --question--

#### --text--

Que se passe-t-il lorsqu'un groupe de capture `(...)` est utilisé dans une expression régulière ?

#### --distractors--

Le groupe de capture est immédiatement évalué, peu importe où il se trouve dans l'expression régulière.

---

Le groupe de capture tente de correspondre avec le sous-motif donné, et continue sans mémoriser le résultat.

---

Le groupe de capture est un sous-motif prioritaire, qui renverra immédiatement lorsqu'une correspondance est trouvée pour le sous-motif.

#### --answer--

Le groupe de capture tente de correspondre avec le sous-motif donné, et continue avec le résultat en mémoire.

### --question--

#### --text--

Que se passe-t-il lorsqu'un groupe non capturant `(?:...)` est utilisé dans une expression régulière ?

#### --distractors--

Le groupe non capturant tente de correspondre avec le sous-motif donné, et continue avec le résultat en mémoire.

---

Le groupe non capturant se termine immédiatement lorsqu'il trouve une correspondance pour le sous-motif donné.

---

Le groupe non capturant est considéré comme une correspondance optionnelle, donc seule une correspondance réussie est ajoutée au résultat.

#### --answer--

Le groupe non capturant tente de correspondre avec le sous-motif donné, et continue sans garder le résultat en mémoire.

### --question--

#### --text--

Étant donné l'expression régulière suivante, lequel de ces tests renverra `true` ?

```js
const regex = /(cat)\s+\1/i;
```

#### --distractors--

```js
regex.test("cat dog cat");
```

---

```js
regex.test("catcat");
```

---

```js
regex.test("cat\s+cat");
```

#### --answer--

```js
regex.test("cat   cat");
```

### --question--

#### --text--

Que fait la classe de caractères joker (`.`) dans une expression régulière ?

#### --distractors--

Elle ne correspond qu'aux caractères d'espace blanc.

---

Elle correspond au début d'une chaîne.

---

Elle correspond à un seul chiffre.

#### --answer--

Elle correspond à n'importe quel caractère unique sauf un saut de ligne.
