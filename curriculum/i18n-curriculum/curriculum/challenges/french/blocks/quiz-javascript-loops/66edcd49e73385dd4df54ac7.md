---
id: 66edcd49e73385dd4df54ac7
title: Quiz sur les boucles JavaScript
challengeType: 8
dashedName: quiz-javascript-loops
---

# --description--

Pour réussir le quiz, tu dois répondre correctement à au moins 18 des 20 questions ci-dessous.

# --quizzes--

## --quiz--

### --question--

#### --text--

Laquelle des propositions suivantes décrit le mieux l'itération ?

#### --distractors--

C'est le processus qui consiste à écrire du code de façon répétée jusqu'à ce qu'il fonctionne correctement.

---

C'est une technique pour stocker des données dans des emplacements mémoire consécutifs.

---

C'est le processus qui consiste à envoyer des signaux entre des composants pour établir une connexion.

#### --answer--

C'est le processus qui consiste à répéter un ensemble d'instructions plusieurs fois.

### --question--

#### --text--

Quelle technique est utilisée pour les itérations en programmation ?

#### --distractors--

La récursivité

---

Les conditions

---

La compilation

#### --answer--

Les boucles

### --question--

#### --text--

Lequel des éléments suivants est un objet itérable ?

#### --distractors--

undefined

---

Nombre

---

Booléen

#### --answer--

Chaîne

### --question--

#### --text--

Lequel des éléments suivants n'est pas un type de boucle pris en charge en JavaScript ?

#### --distractors--

Boucle `for`.

---

Boucle `while`.

---

Boucle `for...in`.

#### --answer--

Boucle `if...else`.

### --question--

#### --text--

Quel est l'ordre correct de la déclaration d'une boucle `for` ?

#### --distractors--

```javascript
for (condition; increment/decrement; initialization) {
  statement;
}
```

---

```javascript
for (increment/decrement; condition; initialization) {
  statement;
}
```

---

```javascript
for (initialization; increment/decrement; condition) {
  statement;
}
```

#### --answer--

```javascript
for (initialization; condition; increment/decrement) {
  statement;
}
```

### --question--

#### --text--

Quelle boucle exécute le bloc de code une fois avant de vérifier si la condition est vraie, puis continue tant que la condition reste vraie ?

#### --distractors--

Boucle `while`.

---

Boucle `for...of`.

---

Boucle `for...in`.

#### --answer--

Boucle `do...while`.

### --question--

#### --text--

Laquelle des propositions suivantes parcourt les valeurs d'un objet itérable ?

#### --distractors--

Boucle `for...in`.

---

Boucle `for`.

---

Boucle `do...while`.

#### --answer--

Boucle `for...of`.

### --question--

#### --text--

Laquelle des propositions suivantes définit le mieux une boucle infinie ?

#### --distractors--

Une boucle qui s'exécute jusqu'à ce que la condition devienne fausse.

---

Une boucle qui s'exécute une seule fois, puis s'arrête.

---

Une boucle qui s'arrête automatiquement après un nombre fixe d'itérations.

#### --answer--

Une boucle dont la condition d'arrêt n'est jamais satisfaite ou est absente.

### --question--

#### --text--

Combien de fois la boucle suivante va-t-elle s'exécuter ?

```js
for (let i = 2; i < 10; i+=2) {
  console.log(i);
}
```

#### --distractors--

5

---

9

---

10

#### --answer--

4

### --question--

#### --text--

Quelle boucle est la meilleure pour itérer sur des objets ?

#### --distractors--

Boucle `for...of`.

---

Boucle `do...while`.

---

Boucle `for`.

#### --answer--

Boucle `for...in`.

### --question--

#### --text--

Quelle est la différence entre la boucle `for...in` et la boucle `for...of` ?

#### --distractors--

La boucle `for...in` itère sur les valeurs des propriétés, tandis que la boucle `for...of` itère sur les noms des propriétés (clés).

---

Les boucles `for...in` créent de nouvelles propriétés, tandis que les boucles `for...of` modifient des propriétés existantes.

---

Les boucles `for...in` ne peuvent être utilisées qu'avec des chaînes, tandis que les boucles `for...of` peuvent être utilisées avec des chaînes et des nombres.

#### --answer--

La boucle `for...in` itère sur les noms des propriétés (clés), tandis que la boucle `for...of` itère sur les valeurs des propriétés.

### --question--

#### --text--

Laquelle de ces propositions provoquerait une boucle infinie ?

#### --distractors--

L'initialisation d'une variable.

---

Une condition de boucle qui finit par donner false.

---

Une logique d'incrémentation/décrémentation.

#### --answer--

Une condition de boucle qui retourne toujours true.

### --question--

#### --text--

Quelle instruction peut être utilisée pour sortir complètement d'une boucle ?

#### --distractors--

L'instruction `continue`.

---

L'instruction `label`.

---

L'instruction `skip`.

#### --answer--

L'instruction `break`.

### --question--

#### --text--

Quelle instruction peut être utilisée pour ignorer l'itération courante et passer à l'itération suivante ?

#### --distractors--

L'instruction `break`.

---

L'instruction `label`.

---

L'instruction `return`.

#### --answer--

L'instruction `continue`.

### --question--

#### --text--

Qu'est-ce qui est affiché dans la console avec le code suivant ?

```js
for (let i = 0; i < 25; i += 2) {
  if (i % 5 === 0) {
    continue;
  }
  if (i % 13 === 0) {
    break;
  }
  console.log(i);
}
```

#### --distractors--

`0`, `2`, `4`, `6`, `8`, `12`, `14`, `16`, `18`, `22`, `24`

---

`0`, `2`, `4`, `6`, `8`, `12`

---

`2`, `4`, `6`, `8`, `12`

#### --answer--

`2`, `4`, `6`, `8`, `12`, `14`, `16`, `18`, `22`, `24`

### --question--

#### --text--

Qu'est-ce que le code suivant afficherait dans la console ?

```javascript
for (let i = 1; i < 6; i++) {
  if (i === 4) break;
  console.log(i);
}
```

#### --distractors--

`1`, `2`, `3`, `4`, `5`, `6`

---

`1`, `2`, `3`, `4`, `5`

---

`1`, `2`, `3`, `4`

#### --answer--

`1`, `2`, `3`

### --question--

#### --text--

Qu'est-ce qui sera affiché dans la console avec le code ci-dessous ?

```javascript
const shoppingList = { tomatoes: 4, apples: 10 };
for (const item in shoppingList) {
  console.log(item);
}
```

#### --distractors--

`tomatoes - 4`, `apples - 10`

---

`4`, `10`

---

`tomatoes:4`, `apples:10`

#### --answer--

`tomatoes`, `apples`

### --question--

#### --text--

Quelle sera la sortie console du code ci-dessous ?

```javascript
for (let i = 2; i <= 13; i++) {
  if (i % 2 === 0) continue;
  console.log(i);
}
```

#### --distractors--

`2`, `4`, `6`, `8`, `10`, `12`

---

`1`, `3`, `5`, `7`, `9`, `11`, `13`

---

`1`, `3`, `5`, `7`, `9`, `11`

#### --answer--

`3`, `5`, `7`, `9`, `11`, `13`

### --question--

#### --text--

Qu'est-ce qui serait affiché dans la console avec le code ci-dessous ?

```javascript
const fruits = ["Mango", "Pineapple", "Oranges"];
for (const fruit of fruits) {
  console.log(fruit);
}
```

#### --distractors--

`0`, `1`, `2`

---

`Pineapple`, `Oranges`, `Mango`

---

`Oranges`, `Pineapple`, `Mango`

#### --answer--

`Mango`, `Pineapple`, `Oranges`

### --question--

#### --text--

Combien de fois le code ci-dessous affichera-t-il la chaîne `in the loop` ?

```javascript
let x = 0
while (x < 5) {
  console.log("in the loop");
}
```

#### --distractors--

3 fois

---

4 fois

---

5 fois

#### --answer--

Une infinité de fois
