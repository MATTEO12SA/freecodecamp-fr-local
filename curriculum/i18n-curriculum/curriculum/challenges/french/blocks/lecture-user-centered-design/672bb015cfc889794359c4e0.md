---
id: 672bb015cfc889794359c4e0
title: Quelles sont les bonnes pratiques pour concevoir des dialogues modaux ?
challengeType: 19
dashedName: what-are-best-practices-for-designing-modal-dialogs
---

# --interactive--

**NOTE** : Certains des exemples interactifs peuvent utiliser du CSS et du JavaScript que tu n'as pas encore appris. Ne t'inquiète pas si tu essaies de tout comprendre. Le but des exemples est de te montrer des aperçus de ces concepts de design pour que tu comprennes mieux comment les choses fonctionnent. Pour voir les aperçus, tu devras activer l'éditeur interactif.

Qu'est-ce qu'un modal ? C'est le type de pop-up qu'un site web peut te montrer par-dessus son contenu. HTML a un élément `dialog` que tu peux utiliser pour créer des modaux.

Le contenu derrière un modal est généralement assombri. Cela aide l'utilisateur à se concentrer visuellement sur la zone avec laquelle tu veux qu'il interagisse – dans ce cas, le modal.

C'est toujours une bonne idée de permettre à l'utilisateur de cliquer en dehors du modal pour le fermer.

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<button id="open-modal">Open Modal</button>
<dialog>
  <h2>Subscribe to our Newsletter!</h2>
  <p>Get the latest updates and offers.</p>
  <button>Subscribe</button>
  <button>Close</button>
</dialog>

<script src="index.js"></script>
```

```css
dialog {
  border: none;
  border-radius: 8px;
  padding: 20px;
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
}

dialog::backdrop {
  background: rgba(0, 0, 0, 0.5);
}
```

```js
const dialog = document.querySelector('dialog');
const closeButton = dialog.querySelector('button:last-of-type');
const openModalButton = document.getElementById('open-modal');

closeButton.addEventListener('click', () => {
  dialog.close();
});

openModalButton.addEventListener('click', () => {
  dialog.showModal();
});

// Close the modal when clicking outside of it
dialog.addEventListener('click', (event) => {
  const rect = dialog.getBoundingClientRect();
  const isInDialog = (
    event.clientX >= rect.left &&
    event.clientX <= rect.right &&
    event.clientY >= rect.top &&
    event.clientY <= rect.bottom
  );
  if (!isInDialog) {
    dialog.close();
  }
}); 

```

:::

Tu verras souvent des boutons très proéminents sur les modaux. Ils s'appellent CTAs, ou call-to-action. Tu veux qu'ils soient facilement identifiables puisque le but d'interrompre le flux de l'utilisateur avec un modal est de l'inciter à effectuer une action spécifique.

Les modaux devraient aussi avoir un bouton de fermeture. Même si tu veux vraiment que l'utilisateur clique sur tes CTAs, il est important de lui donner une option pour quitter le modal et reprendre ce qu'il faisait précédemment.

:::interactive_editor

```html
<link rel="stylesheet" href="styles.css">
<button id="open-modal">Open Modal</button>
<dialog>
  <h2>Subscribe to our Newsletter!</h2>
  <p>Get the latest updates and offers.</p>
  <button class="cta">Subscribe</button>
  <button class="close">Close</button>
</dialog>
<script src="index.js"></script>
```

```css
.cta {
  background-color: #007BFF;
  color: white;
  border: none;
  padding: 10px 20px;
  border-radius: 4px;
  cursor: pointer;
}

.close {
  background-color: transparent;
  color: #007BFF;
  border: none;
  padding: 10px 20px;
  cursor: pointer;
}
```

```js
const dialog = document.querySelector('dialog');
const closeButton = dialog.querySelector('.close');
const openModalButton = document.getElementById('open-modal');

closeButton.addEventListener('click', () => {
  dialog.close();
});

openModalButton.addEventListener('click', () => {
  dialog.showModal();
});

// Close the modal when clicking outside of it
dialog.addEventListener('click', (event) => {
  const rect = dialog.getBoundingClientRect();
  const isInDialog = (
    event.clientX >= rect.left &&
    event.clientX <= rect.right &&
    event.clientY >= rect.top &&
    event.clientY <= rect.bottom
  );
  if (!isInDialog) {
    dialog.close();
  }
});

```

:::

Il y a, bien sûr, des préoccupations d'accessibilité avec les modaux, comme gérer correctement le focus sur les éléments. Cependant, si tu utilises ces pratiques générales comme point de départ, tu auras une base solide sur laquelle construire.

# --questions--

## --text--

Quel est le but d'assombrir le contenu derrière un dialogue modal ?

## --answers--

Pour économiser la batterie sur les appareils mobiles.

### --feedback--

La leçon mentionne une raison spécifique pour cet effet visuel.

---

Pour aider les utilisateurs à identifier visuellement la zone avec laquelle ils devraient interagir.

---

Pour faire charger le site web plus rapidement.

### --feedback--

La leçon mentionne une raison spécifique pour cet effet visuel.

---

Pour empêcher les utilisateurs de voir des informations sensibles.

### --feedback--

La leçon mentionne une raison spécifique pour cet effet visuel.

## --video-solution--

2

## --text--

Que signifie CTA dans le contexte du design de modal ?

## --answers--

Click To Activate.

### --feedback--

La leçon fournit le terme complet pour cet acronyme quand elle parle des boutons proéminents dans les modaux.

---

Complete The Action.

### --feedback--

La leçon fournit le terme complet pour cet acronyme quand elle parle des boutons proéminents dans les modaux.

---

Call-to-Action.

---

Close The Application.

### --feedback--

La leçon fournit le terme complet pour cet acronyme quand elle parle des boutons proéminents dans les modaux.

## --video-solution--

3

## --text--

Pourquoi est-il important d'inclure un bouton de fermeture dans un dialogue modal ?

## --answers--

C'est requis par la loi dans certains pays.

### --feedback--

La leçon souligne l'importance de permettre aux utilisateurs de quitter le modal.

---

Pour empêcher le modal de faire planter le navigateur.

### --feedback--

La leçon souligne l'importance de permettre aux utilisateurs de quitter le modal.

---

Pour donner aux utilisateurs une option pour quitter le modal et reprendre leur activité précédente.

---

Pour rendre le modal plus professionnel.

### --feedback--

La leçon souligne l'importance de permettre aux utilisateurs de quitter le modal.

## --video-solution--

3
